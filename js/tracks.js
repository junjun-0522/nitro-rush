/* ============================================================
   NITRO RUSH - Track definitions, road/scenery construction
   ============================================================ */
(function (global) {
  'use strict';

  var UPV = new THREE.Vector3(0, 1, 0);

  // ------------------------------------------------------------------ defs
  // Layouts are explicit closed circuits: [x, z, y] control points (metres), start at index 0 heading +z.
  var TRACKS = [
    {
      id: 'coast', name: 'AZURE COAST', tag: 'BEGINNER', mood: 'coast', laps: 3,
      desc: '햇살 가득한 해안 도시. 긴 직선과 완만한 스위퍼, 언덕 위 터널, 마지막 헤어핀 안쪽 지름길.',
      pts: [[-195.9,85.2,0],[-195.9,135.2,0],[-195.9,185.2,0],[-193.5,206.3,0],[-186.5,226.4,0],[-175.2,244.4,0],[-160.1,259.5,0],[-142.1,270.8,0],[-122,277.8,0],[-100.9,280.2,0.2],[-47.9,280.2,0.7],[5.1,280.2,1.5],[58.1,280.2,2.7],[76.6,277.3,4.2],[93.4,268.7,5.4],[106.6,255.5,6.3],[115.2,238.7,6.8],[118.1,220.2,7],[118.1,170.2,7],[118.1,120.2,7],[118.1,70.2,7],[118.1,20.2,7],[121.7,-0.3,7],[132.1,-18.4,7],[148.1,-31.8,7],[164.1,-45.2,7],[174.5,-63.2,7],[178.1,-83.7,6.4],[178.1,-133.7,5],[178.1,-183.7,3],[178.1,-233.7,1.3],[173.9,-251.9,0.3],[162.3,-266.6,0],[145.4,-274.7,0],[126.8,-274.7,0],[109.9,-266.6,0],[98.3,-251.9,0],[94.1,-233.7,0],[94.1,-213.7,0],[90.7,-192.1,0],[80.7,-172.6,0.4],[65.2,-157.1,1.2],[45.7,-147.1,2.5],[24.1,-143.7,3.5],[-25.9,-143.7,3.9],[-75.9,-143.7,4],[-125.9,-143.7,3.6],[-147.5,-140.3,2.9],[-167,-130.4,1.9],[-182.5,-114.9,0.9],[-192.5,-95.4,0.3],[-195.9,-73.7,0],[-195.9,-51,0],[-195.9,-28.3,0],[-195.9,-5.6,0],[-195.9,17.1,0],[-195.9,39.8,0]],
      width: 25, narrow: [[0.555, 0.635, 22], [0.43, 0.50, 22]],
      zones: [
        { a: 0.56, b: 0.63, side: 1, extra: 22, kind: 'dirt' },    // inside of the hairpin
        { a: 0.655, b: 0.76, side: -1, extra: 18, kind: 'dirt' }   // inside of the double-left
      ],
      ramps: [{ a: 0.19, b: 0.203, h: 1.7 }],
      tunnels: [{ a: 0.32, b: 0.40 }],
      boostPads: [{ s: 0.60, lat: 19 }, { s: 0.715, lat: -18 }, { s: 0.03, lat: 0 }, { s: 0.355, lat: 4 }, { s: 0.95, lat: -4 }],
      itemRows: [0.04, 0.12, 0.21, 0.34, 0.42, 0.53, 0.66, 0.80, 0.93],
      theme: {
        sky: [0x35a7ff, 0xbfe9ff], fog: 0xbfe9ff, fogNear: 260, fogFar: 900,
        sun: 0xfff3d6, sunInt: 1.5, hemi: [0xbfe9ff, 0x6c8f4a], hemiInt: 0.75,
        road: 'asphalt', rail: 0xffffff, railEm: 0x000000, ground: 'grass'
      }
    },
    {
      id: 'neon', name: 'NEON METROPOLIS', tag: 'EXPERT', mood: 'neon', laps: 3,
      desc: '네온 불빛의 미래 도시. 헤어핀과 시케인, 하늘 위 고가도로, 네온 터널과 점프.',
      pts: [[-244.4,92.8,0],[-244.4,152.8,0],[-241,170,0],[-231.2,184.6,0],[-216.6,194.4,0],[-199.4,197.8,0],[-117.4,197.8,0],[-98.9,202.3,0],[-84.6,214.9,0],[-70.3,227.4,0],[-51.9,231.9,0],[28.1,231.9,0],[48.1,226.6,0],[62.8,211.9,0],[68.1,191.9,0],[68.1,131.9,0],[68.1,71.9,0],[73.6,54,0.7],[88.1,42.2,3.1],[106.8,40.6,5.4],[123.2,49.7,6],[131.6,66.4,5.3],[142.1,125.4,3.7],[149.5,143.4,1.6],[163.9,156.6,0.3],[182.5,162.5,0],[201.8,159.9,0],[258.2,139.4,0],[277.2,124.8,0],[284.5,101.8,0],[284.5,41.8,0],[284.5,-18.2,0],[284.5,-78.2,1.4],[284.5,-138.2,5.4],[284.5,-198.2,12.2],[280.3,-219.2,19.5],[268.4,-237.1,24.1],[250.5,-249,25.9],[229.5,-253.2,26],[177.5,-253.2,26],[125.5,-253.2,26],[73.5,-253.2,26],[21.5,-253.2,26],[-30.5,-253.2,26],[-50.5,-247.8,26],[-65.2,-233.2,25.7],[-95.2,-181.2,22.1],[-109.8,-166.6,14.2],[-129.8,-161.2,5.4],[-199.8,-161.2,0.8],[-217,-157.8,0],[-231.6,-148,0],[-241.4,-133.5,0],[-244.8,-116.2,0],[-244.8,-95.3,0],[-244.8,-74.4,0],[-244.7,-53.5,0],[-244.7,-32.6,0],[-244.6,-11.7,0],[-244.5,9.2,0],[-244.5,30.1,0],[-244.4,51,0]],
      width: 19, narrow: [[0.25, 0.31, 16], [0.05, 0.15, 17], [0.75, 0.88, 17]],
      zones: [
        { a: 0.25, b: 0.31, side: -1, extra: 20, kind: 'dirt' },
        { a: 0.33, b: 0.445, side: 1, extra: 16, kind: 'dirt' },
        { a: 0.755, b: 0.80, side: 1, extra: 14, kind: 'dirt' }
      ],
      ramps: [{ a: 0.215, b: 0.23, h: 2.4 }, { a: 0.585, b: 0.60, h: 2.0 }],
      tunnels: [{ a: 0.44, b: 0.50 }, { a: 0.62, b: 0.70 }],
      boostPads: [{ s: 0.28, lat: -16 }, { s: 0.40, lat: 14 }, { s: 0.78, lat: 12 }, { s: 0.52, lat: 0 }, { s: 0.66, lat: -3 }, { s: 0.95, lat: 0 }],
      itemRows: [0.02, 0.09, 0.16, 0.24, 0.33, 0.47, 0.64, 0.74, 0.92],
      theme: {
        sky: [0x05020f, 0x2b0f5a], fog: 0x0a0620, fogNear: 180, fogFar: 700,
        sun: 0x9fb4ff, sunInt: 0.75, hemi: [0x5a48b8, 0x101024], hemiInt: 0.8,
        road: 'asphaltNight', rail: 0x1a1a2a, railEm: 0x00e5ff, ground: 'concrete'
      }
    },
    {
      id: 'canyon', name: 'SUNSET CANYON', tag: 'ADVANCED', mood: 'canyon', laps: 3,
      desc: '석양의 붉은 협곡. 긴 고속 직선, 연속 헤어핀 스위치백, 협곡을 가로지르는 다리와 점프.',
      pts: [[-343.9,-34.5,0],[-343.9,25.5,0],[-343.9,85.5,0],[-340.5,107.1,0],[-330.5,126.6,0.3],[-315,142.1,1.6],[-295.5,152.1,3.6],[-273.9,155.5,4.8],[-217.1,155.5,5],[-160.3,155.5,5],[-103.4,155.5,5],[-46.6,155.5,5],[10.2,155.5,5],[67,155.5,5],[123.9,155.5,5],[180.7,155.5,5],[237.5,155.5,4.7],[256.7,151.7,3.2],[272.9,140.9,1],[283.7,124.6,0],[287.5,105.5,0],[287.5,25.5,0],[283.4,7.3,0],[271.7,-7.3,0],[254.9,-15.4,0],[236.2,-15.4,0],[219.3,-7.3,0],[207.7,7.3,0],[203.5,25.5,0],[203.5,95.5,0],[199.4,113.7,0],[187.7,128.3,0],[170.9,136.4,0],[152.2,136.4,0],[135.3,128.3,0],[123.7,113.7,0],[119.5,95.5,0],[119.5,50.5,0],[119.5,5.5,1.3],[116.2,-13.3,5.8],[106.7,-29.9,10.5],[92,-42.1,12],[77.4,-54.4,12],[67.8,-71,12],[64.5,-89.8,12],[64.5,-166.4,12],[61.6,-184.9,12],[53.1,-201.6,12],[39.8,-214.9,12],[23.1,-223.4,12],[4.5,-226.4,12],[-50.5,-226.4,12],[-105.5,-226.4,11],[-122.4,-222.6,6.7],[-136.1,-212.1,1.8],[-168.3,-173.8,0],[-182,-163.2,0],[-198.9,-159.5,0],[-278.9,-159.5,0],[-299,-156.3,0],[-317.1,-147.1,0],[-331.5,-132.7,0],[-340.7,-114.6,0],[-343.9,-94.5,0]],
      width: 22, narrow: [[0.41, 0.47, 18], [0.51, 0.56, 18]],
      zones: [
        { a: 0.41, b: 0.465, side: 1, extra: 20, kind: 'dirt' },
        { a: 0.51, b: 0.56, side: -1, extra: 20, kind: 'dirt' },
        { a: 0.81, b: 0.85, side: 1, extra: 16, kind: 'dirt' }
      ],
      ramps: [{ a: 0.30, b: 0.315, h: 2.0 }, { a: 0.795, b: 0.81, h: 2.2 }],
      tunnels: [{ a: 0.17, b: 0.24 }],
      boostPads: [{ s: 0.44, lat: 18 }, { s: 0.535, lat: -18 }, { s: 0.12, lat: 0 }, { s: 0.70, lat: 0 }, { s: 0.83, lat: 12 }, { s: 0.97, lat: 0 }],
      itemRows: [0.03, 0.10, 0.20, 0.28, 0.35, 0.48, 0.58, 0.70, 0.80, 0.90],
      theme: {
        sky: [0x4a2a7a, 0xffa64a], fog: 0xf0b07a, fogNear: 220, fogFar: 820,
        sun: 0xffd2a0, sunInt: 1.3, sunDir: [-0.8, 0.45, -0.4], hemi: [0xffc9a0, 0x8a5a3a], hemiInt: 0.7, exposure: 1.0,
        road: 'asphalt', roadTint: 0xd9c2a8, rail: 0xff8c42, railEm: 0x000000, ground: 'sand', skirt: 'dirt',
        tunnel: 0x9a6a45, tunnelLight: 0xffe0b0, portal: 0x8a5a3a, portalEm: 0x000000, pillar: 0xb07a50, gate: 0xffffff, gateEm: 0x000000,
        time: 'SUNSET', stars: false, seed: 555, preview: ['rgba(255,160,80,0.5)', '#5a4636', '#ffd9a0']
      }
    },
    {
      id: 'frost', name: 'FROST PEAK', tag: 'EXPERT', mood: 'frost', laps: 3,
      desc: '눈 덮인 산길. 22m 오르막과 빙판 내리막 에스, 얼음 동굴 터널, 미끄러운 노면.',
      pts: [[-284.9,-92.8,0],[-284.9,-38.7,0],[-284.9,15.3,0],[-284.9,69.4,0],[-284.9,123.5,0],[-284.9,177.5,0.3],[-281.1,196.7,0.9],[-270.3,212.9,2],[-254,223.7,3.4],[-234.9,227.5,5.2],[-189.9,227.5,7.4],[-144.9,227.5,10],[-124,220.6,12.8],[-111.4,202.6,15.3],[-112,180.6,17.4],[-132.5,124.2,19.1],[-133.2,102.2,20.4],[-120.5,84.1,21.3],[-99.6,77.2,21.8],[-40.3,77.2,22],[19,77.2,22],[78.4,77.2,22],[137.7,77.2,22],[197,77.2,22],[256.4,77.2,22],[273.6,73.8,22],[288.2,64,22],[298,49.4,22],[301.4,32.2,22],[301.4,-27.8,22],[296.3,-46.8,22],[282.4,-60.7,22],[263.4,-65.8,21.9],[244.4,-60.7,21.4],[230.5,-46.8,20.3],[225.4,-27.8,18.6],[225.4,22.2,16.3],[220.3,41.2,13.5],[206.4,55.1,10.6],[187.4,60.2,8.1],[168.4,55.1,6.3],[154.5,41.2,4.9],[149.4,22.2,4.2],[149.4,-24.5,4],[149.4,-71.1,3.7],[149.4,-117.8,3.1],[145.6,-136.9,2],[134.7,-153.2,0.9],[118.5,-164,0.3],[99.4,-167.8,0],[49.4,-167.8,0],[-0.6,-167.8,0],[-50.6,-167.8,0],[-100.6,-167.8,0],[-120.6,-173.2,0],[-135.3,-187.8,0],[-149.9,-202.4,0],[-169.9,-207.8,0],[-229.9,-207.8,0],[-250.9,-203.6,0],[-268.8,-191.7,0],[-280.7,-173.8,0],[-284.9,-152.8,0]],
      width: 20, narrow: [[0.51, 0.56, 16]],
      zones: [
        { a: 0.215, b: 0.25, side: 1, extra: 16, kind: 'dirt' },
        { a: 0.515, b: 0.55, side: 1, extra: 18, kind: 'dirt' },
        { a: 0.595, b: 0.63, side: -1, extra: 16, kind: 'dirt' }
      ],
      ramps: [{ a: 0.47, b: 0.485, h: 2.2 }, { a: 0.70, b: 0.715, h: 1.8 }],
      tunnels: [{ a: 0.36, b: 0.44 }, { a: 0.80, b: 0.85 }],
      boostPads: [{ s: 0.235, lat: 14 }, { s: 0.53, lat: 14 }, { s: 0.61, lat: -14 }, { s: 0.40, lat: 0 }, { s: 0.82, lat: 0 }, { s: 0.04, lat: 0 }],
      itemRows: [0.04, 0.10, 0.19, 0.31, 0.42, 0.57, 0.66, 0.78, 0.92],
      theme: {
        sky: [0x6fb4ff, 0xe8f4ff], fog: 0xdbeeff, fogNear: 200, fogFar: 760,
        sun: 0xffffff, sunInt: 1.4, sunDir: [0.4, 1, -0.6], hemi: [0xd8ecff, 0x9fb3c8], hemiInt: 0.8, exposure: 1.05,
        road: 'ice', roadTint: 0xffffff, rail: 0xdde8f5, railEm: 0x000000, ground: 'snow', skirt: 'snow',
        tunnel: 0xbfe0ff, tunnelLight: 0xa0f0ff, portal: 0x9fd0ff, portalEm: 0x3399ff, pillar: 0x9fb3c8, gate: 0xffffff, gateEm: 0x000000,
        time: 'DAY', stars: false, seed: 999, gripMul: 0.86, preview: ['rgba(160,220,255,0.6)', '#8fa8c0', '#ffffff']
      }
    },
    {
      id: 'seoul', name: 'SEOUL BLOSSOM', tag: 'INTERMEDIATE', mood: 'seoul', laps: 3,
      desc: '벚꽃 핀 봄날의 서울. 광화문 광장에서 출발해 한강 대교, 롯데타워 시케인, 잠수교, 남산 터널을 지나 N서울타워를 돌고 벚꽃 내리막으로.',
      pts: [[0,0,0],[0,28,0],[0,56,0],[0,84,0],[0,112,0],[0.5,128,0],[5,143.3,0],[13.7,156.6,0],[25.9,166.9,0],[40.5,173.3,0],[68.4,175.2,1],[96.4,175.2,6.2],[124.4,175.1,8],[152.4,175.1,8],[180.4,175,8],[208.4,175,8],[236.4,174.9,8],[264.4,174.8,5.9],[292.4,174.8,0.8],[308.4,174.6,0],[323.6,170.2,0],[335.9,160.2,0],[343.4,146.1,0],[344.9,118.3,0],[344.8,94.8,0],[347.9,79.2,0],[357.1,66.3,0],[368.9,55.5,0],[375.8,41.2,0],[376.7,13.3,0],[376.6,-14.7,0],[376.4,-37.7,0],[374.8,-53.6,0],[370.2,-68.9,0],[366.9,-84.5,0],[366.3,-112.5,0],[366.1,-140.5,0],[365.8,-156.5,0],[359.6,-171.1,0],[346.9,-180.5,0.1],[319.5,-185.7,1.5],[291.9,-190.3,1.5],[264.2,-194.9,1.5],[236.6,-199.5,1.5],[209,-204.1,1.5],[181.4,-208.7,1.5],[165.7,-212,1.2],[151.9,-219.8,0],[141.6,-232,0.9],[131.2,-257.9,5.5],[121.3,-284.1,10.5],[115.3,-298.9,11.9],[104.8,-310.8,12.7],[90.1,-316.8,15.4],[74.3,-315.7,18.4],[60.6,-307.7,20],[51.8,-294.5,20],[46.5,-267,16.9],[41.9,-239.4,10.1],[37.3,-211.8,4.7],[34.6,-196,4],[29.5,-180.9,3.5],[21.1,-167.3,2.6],[10.5,-155.4,1.5],[2.2,-141.7,0.5],[-2.8,-126.6,0],[-4.3,-110.6,0],[-1.9,-94.8,0],[0,-66.5,0],[0,-38.5,0],[0,-10.5,0]],
      width: 22, narrow: [[0.366, 0.414, 19], [0.744, 0.804, 18]],
      zones: [
        { a: 0.298, b: 0.340, side: 1, extra: 16, kind: 'dirt' },   // 스위퍼 안쪽
        { a: 0.535, b: 0.563, side: 1, extra: 18, kind: 'dirt' }    // 헤어핀 안쪽
      ],
      ramps: [{ a: 0.248, b: 0.259, h: 1.8 }, { a: 0.855, b: 0.864, h: 1.4 }],
      tunnels: [{ a: 0.708, b: 0.742 }],
      boostPads: [{ s: 0.21, lat: 0 }, { s: 0.43, lat: -4 }, { s: 0.55, lat: 16 }, { s: 0.61, lat: 0 }, { s: 0.83, lat: 0 }, { s: 0.96, lat: 0 }],
      itemRows: [0.05, 0.13, 0.22, 0.32, 0.43, 0.52, 0.62, 0.72, 0.83, 0.93],
      theme: {
        sky: [0x5fb2ff, 0xffe6f0], fog: 0xf3dfe8, fogNear: 260, fogFar: 950,
        sun: 0xfff6e0, sunInt: 1.4, sunDir: [-0.45, 1, -0.55], hemi: [0xdaeeff, 0x8fa870], hemiInt: 0.8, exposure: 1.05,
        road: 'asphalt', roadTint: 0xffffff, rail: 0xffffff, railEm: 0x000000, ground: 'grass', skirt: 'grass',
        tunnel: 0xb4b8c0, tunnelLight: 0xfff3b0, portal: 0xc9ccd2, portalEm: 0x000000, pillar: 0xb4b9c2, gate: 0xa8382c, gateEm: 0x000000,
        time: 'DAY', stars: false, seed: 2026, gripMul: 1, preview: ['rgba(255,160,200,0.55)', '#5a6f4a', '#ff9ec8']
      }
    },
    {
      id: 'jeju', name: 'JEJU EDU CITY', tag: 'INTERMEDIATE', mood: 'jeju', laps: 3,
      desc: '제주영어교육도시 순환로. 이노에듀타운 음식점 거리에서 출발해 삼정·BHA·KIS를 지나 곶자왈 숲 사이 북쪽 코너를 돌고, SJA·한신더휴 앞 동쪽 대로로 내려온다.',
      pts: [[0,0,0],[2.6,-39,0],[4.5,-78,0],[2.6,-117,0],[-2.6,-162.5,0.6],[-9.1,-208,1.3],[-15.6,-253.5,1.9],[-20.8,-292.5,2.5],[-25.4,-331.5,3],[-29.9,-364,3.5],[-36.4,-399.8,4],[-44.2,-438.8,4.6],[-52.6,-474.5,5.1],[-60.5,-510.3,5.6],[-67.6,-546,6],[-71.5,-575.3,6],[-63.7,-590.2,6],[-41,-592.8,6],[-11.7,-586.3,6],[24.1,-568.8,6],[59.8,-540.8,6],[92.3,-503.8,5.5],[118.3,-461.5,4.9],[139.1,-416,4.2],[153.4,-367.3,3.5],[161.8,-318.5,2.8],[164.5,-273,2.2],[161.8,-227.5,1.5],[154.1,-182,0.9],[143,-136.5,0.2],[132.6,-91,0],[124.8,-48.8,0],[111.8,-6.5,0],[96.2,35.8,0],[80.6,74.8,0],[64.4,110.5,0],[48.1,143,0],[31.2,169,0],[13,180.7,0],[-2,167.7,0],[-10.4,139.8,0],[-11.7,104,0],[-9.1,71.5,0],[-4.5,35.8,0]],
      width: 21, narrow: [[0.25, 0.33, 18]],
      zones: [
        { a: 0.33, b: 0.372, side: -1, extra: 14, kind: 'dirt' },   // 북쪽 코너 안쪽 (곶자왈 흙길)
        { a: 0.868, b: 0.905, side: -1, extra: 14, kind: 'dirt' }   // 남쪽 119센터 코너 안쪽
      ],
      ramps: [{ a: 0.5, b: 0.51, h: 1.4 }],
      tunnels: [],
      boostPads: [{ s: 0.04, lat: 0 }, { s: 0.16, lat: 0 }, { s: 0.30, lat: 0 }, { s: 0.45, lat: -3 }, { s: 0.62, lat: 0 }, { s: 0.75, lat: 3 }, { s: 0.93, lat: 0 }],
      itemRows: [0.05, 0.14, 0.24, 0.33, 0.43, 0.53, 0.63, 0.73, 0.83, 0.93],
      theme: {
        sky: [0x4fa8ff, 0xdff3ff], fog: 0xdbe9d6, fogNear: 240, fogFar: 900,
        sun: 0xfff6e0, sunInt: 1.45, sunDir: [0.35, 1, -0.6], hemi: [0xd6ecff, 0x4f8a3a], hemiInt: 0.85, exposure: 1.05,
        road: 'asphalt', roadTint: 0xffffff, rail: 0xffffff, railEm: 0x000000, ground: 'grass', skirt: 'grass',
        tunnel: 0x7a7f86, tunnelLight: 0xfff3b0, portal: 0x8a8f96, portalEm: 0x000000, pillar: 0x6f7378, gate: 0x2b6e3a, gateEm: 0x000000,
        time: 'DAY', stars: false, seed: 3301, gripMul: 1, preview: ['rgba(90,210,130,0.5)', '#3a5a3a', '#ffe066']
      }
    },
    {
      id: 'lab', name: 'NITRO LAB', tag: 'EXPERT', mood: 'lab', laps: 1,
      desc: '거대한 미래 과학실 안을 달리는 3.3km 단판 승부. 실험대 위 3연속 헤어핀과 18m 낙하 점프, 반응로 헤어핀, 시험관 에스, 그리고 구간을 건너뛰는 워프 포탈 2개.',
      pts: [[0,0,0],[0,28,0],[0,56,0],[0,84,0],[0,112,0],[0,140,0],[-0.4,156,0],[-5.2,171.2,0],[-14.9,183.7,0],[-25.6,195.5,0.1],[-31.6,210.3,1.4],[-32.5,238.2,5.9],[-32.5,266.2,11.6],[-32.4,282.2,14.5],[-29.6,297.9,16.8],[-22.7,312.3,17.9],[-12.3,324.4,18],[1,333.3,18],[16.1,338.4,18],[44,339.6,18],[72,339.7,18],[100,339.8,18],[128,339.9,18],[147,340,18],[162.5,343.5,18],[174.8,353.5,18],[182.1,367.7,18],[194.1,377.8,18],[209.8,378.4,18],[222.5,369.1,18],[226.9,341.9,18],[227.3,313.9,18],[230.7,298.6,18],[242.8,288.5,18],[258.5,288.1,18],[271.1,297.5,18],[281.1,323.5,18],[288.8,345.2,18],[298.4,357.6,18],[313.7,361.4,18],[328,355,18],[335.2,341,18],[335.8,313.1,18],[337.4,297.2,18],[344.8,283.1,17.8],[357,273,16],[372.2,268.4,12.8],[400.2,268.7,6.2],[428.2,269.1,2.1],[456.2,269.6,1.8],[472.1,267.8,1.6],[486.9,261.8,1.2],[499.6,252.2,0.9],[509.4,239.6,0.5],[515.5,224.9,0.2],[517.7,197,0],[518.3,169,0],[518.6,151,0],[521.7,135.4,0],[530,121.8,0],[542.5,112,0],[557.6,107.1,0],[573.5,107.8,0],[588.2,114,0],[599.8,124.9,0],[606.8,139.2,0],[608.3,167,0],[607.6,195,0],[607,223,0],[607.1,239,0],[611.7,254.2,0],[621.3,266.9,0],[634.7,275.4,0],[650.3,278.7,0],[666,276.4,0],[680,268.8,0],[690.4,256.8,0],[696,241.9,0],[697.2,213.9,0],[697.7,185.9,0],[698.3,157.9,0],[698.9,129.9,0],[699.4,101.9,0.5],[700,73.9,3.2],[700.6,45.9,6.5],[701.1,18,8],[701.7,-10,8],[702.3,-38,8],[702.8,-65,8],[700.4,-80.8,8],[692.6,-94.7,7.6],[680.6,-105,6.8],[665.6,-110.5,5.5],[637.7,-111.7,3],[609.7,-112.4,0.9],[581.7,-113.1,0],[565.7,-113.3,0],[550.1,-110.2,0],[536.2,-102.2,0],[525.6,-90.4,0],[515.6,-78,0],[502.2,-69.4,0],[486.7,-65.4,0],[470.8,-64.4,0],[455.8,-59.1,0],[443.2,-49.3,0],[433.8,-36.4,0],[422.1,-25.6,0],[407.6,-18.9,0],[379.8,-17.4,0],[351.8,-18.1,0],[323.8,-18.8,0],[308.9,-23.7,0],[299.8,-36.5,0],[300.1,-52.2,0],[309.8,-64.6,0],[334.4,-78.1,0],[355.5,-89.5,0],[366.4,-100.9,0],[368.3,-116.5,0],[360.5,-130.2,0],[346.1,-136.5,0],[318.1,-137.2,0],[290.1,-137.9,0],[262.2,-138.6,0],[244.7,-139,0],[229.3,-142.9,0],[217.2,-153.1,0.3],[210.8,-167.7,1.1],[211.6,-183.5,2.3],[219.2,-197.4,3.5],[232.2,-206.5,4.5],[258.7,-215.7,5],[285.2,-224.8,5],[299.7,-231.3,5],[310.1,-243.3,5],[314.2,-258.6,5],[311.1,-274.2,5],[301.4,-286.8,5],[287.2,-293.8,5],[259.3,-295.2,4],[231.3,-295.9,2],[203.3,-296.6,0.3],[175.3,-297.3,0],[159.3,-297.7,0],[143.7,-294.6,0],[130.6,-285.7,0],[121.9,-272.4,0],[119.1,-256.7,0],[115.9,-240.5,0],[109,-225.6,0],[99.6,-212.6,0],[88.6,-200.6,0],[76.6,-189.4,0],[64.4,-178.9,0],[52,-168.4,0],[39.8,-157.8,0],[28.1,-146.6,0],[17.4,-134.5,0],[8.3,-120.9,0],[2.1,-105.8,0],[0,-78,0],[0,-50,0],[0,-22,0]],
      width: 19, narrow: [[0.158, 0.262, 15], [0.60, 0.672, 15], [0.685, 0.748, 14]],
      zones: [
        { a: 0.356, b: 0.394, side: -1, extra: 16, kind: 'dirt' },   // 반응로 헤어핀 1 안쪽
        { a: 0.424, b: 0.461, side: 1, extra: 16, kind: 'dirt' },    // 반응로 헤어핀 2 안쪽
        { a: 0.692, b: 0.706, side: -1, extra: 10, kind: 'dirt' },   // 현미경 헤어핀 안쪽
        { a: 0.778, b: 0.801, side: -1, extra: 12, kind: 'dirt' },   // DNA 헤어핀 안쪽
        { a: 0.891, b: 0.906, side: 1, extra: 14, kind: 'dirt' }     // 마지막 코너 안쪽
      ],
      ramps: [{ a: 0.266, b: 0.275, h: 2.6 }, { a: 0.583, b: 0.591, h: 2.2 }, { a: 0.862, b: 0.869, h: 1.8 }],
      tunnels: [{ a: 0.472, b: 0.502 }, { a: 0.582, b: 0.600 }],
      portals: [
        { a: 0.160, lat: 5.2, r: 4.2, to: 0.226, toLat: 0 },       // 실험대 헤어핀 1·2 건너뛰기
        { a: 0.770, lat: -5.2, r: 4.2, to: 0.855, toLat: 0 }       // DNA 헤어핀 건너뛰기
      ],
      boostPads: [{ s: 0.03, lat: 0 }, { s: 0.125, lat: 0 }, { s: 0.30, lat: 0 }, { s: 0.468, lat: 0 }, { s: 0.52, lat: 3 }, { s: 0.61, lat: 0 }, { s: 0.755, lat: 0 }, { s: 0.87, lat: 0 }, { s: 0.94, lat: 0 }],
      itemRows: [0.04, 0.13, 0.24, 0.33, 0.44, 0.54, 0.63, 0.73, 0.83, 0.93],
      theme: {
        sky: [0x0a0f24, 0x16204a], fog: 0x0d1330, fogNear: 320, fogFar: 1000,
        sun: 0xbfe4ff, sunInt: 1.15, sunDir: [0.3, 1, 0.4], hemi: [0x4a7bff, 0x16203a], hemiInt: 1.25, exposure: 1.2,
        road: 'asphaltNight', roadTint: 0xb8c8ff, rail: 0x1a2240, railEm: 0x35e6ff, ground: 'concrete', skirt: 'concrete',
        tunnel: 0x151a2e, tunnelLight: 0x35e6ff, portal: 0x35e6ff, portalEm: 0x35e6ff, pillar: 0x1d2440, gate: 0x1a2240, gateEm: 0x35e6ff,
        time: 'NIGHT', stars: false, seed: 4242, gripMul: 0.94, preview: ['rgba(53,230,255,0.45)', '#101a33', '#35e6ff']
      }
    },
    {
      id: 'orbit', name: 'ORBITAL RUN', tag: 'EXPERT', mood: 'space', laps: 1, open: true, runoff: 120, cpCount: 10, lengthKm: '5.8',
      desc: '순환 없음, 출발과 결승이 다른 곳인 5.8km 우주 원정. 정거장 격납고 → 발사 터널 → 무중력 구간 → 궤도 고속도로 → 행성 궤도 → 우주선 교통 구역 → 중력 반전 터널 → 거대 정거장 외벽 → 최종 직선과 결승 아레나.',
      pts: [[0,0,0],[0,28,0],[0,56,0],[0,84,0],[0,112,0],[0.5,128,0],[4.6,143.4,0],[12.7,157.2,0],[24.1,168.3,0],[48.1,182.6,0],[72.4,196.6,0],[96.7,210.5,0],[109.7,219.8,0],[119.1,232.6,0],[124.1,247.8,0],[124,263.7,0],[118.9,278.8,0],[105,303.1,0],[91.1,327.4,0],[77.2,351.7,0],[63.3,376,0],[49.4,400.3,0],[41.6,414.3,0],[37.7,429.7,0],[42.6,457.1,0],[50,484.1,0.3],[57.5,511.1,1.1],[64.9,538.1,2.2],[72.3,565.1,3.7],[79.8,592.1,5.5],[87.2,619.1,7.4],[94.6,646.1,9.5],[102.1,673,11.7],[109.5,700,13.9],[117,727,16],[124.4,754,18.1],[131.8,781,20],[139.3,808,21.7],[146.7,835,23.1],[154.2,862,24.2],[161.6,889,24.8],[169,916,25],[176.5,943,24],[180.1,958.6,22.9],[181.6,974.5,21.5],[181,990.5,19.8],[178.3,1006.2,17.9],[173.5,1021.5,16],[166.7,1036,14],[158.1,1049.4,12],[147.7,1061.6,10.1],[135.9,1072.3,8.4],[123.2,1082,7],[111.7,1093.2,5.9],[101.8,1105.7,5.2],[93.7,1119.5,5],[87.5,1134.2,5.4],[83.3,1149.7,6.3],[81.1,1165.5,7.8],[81.1,1181.5,9.6],[83.3,1197.3,11.7],[90.4,1224.4,15.9],[97.9,1251.4,20.3],[105.3,1278.4,24.4],[111.7,1301.5,27.2],[114.6,1317.2,28.7],[114.7,1333.2,29.6],[111.9,1349,30],[106.4,1364,29.7],[98.4,1377.8,28.6],[88,1389.9,27],[75.7,1400.1,24.8],[61.7,1407.9,22.3],[46.6,1413,19.5],[30.8,1415.5,16.6],[2.9,1414.2,11.3],[-25,1411.9,6.5],[-52.9,1409.5,2.6],[-80.9,1407.2,0.4],[-108.8,1404.9,0],[-136.7,1402.6,0],[-164.6,1400.3,0],[-192.5,1398,0],[-220.4,1395.7,0],[-248.3,1393.4,0],[-276.2,1391.1,0],[-304.1,1388.8,-0.3],[-332,1386.5,-1],[-359.9,1384.1,-2.1],[-387.8,1381.8,-3.6],[-415.7,1379.5,-5.3],[-431.7,1378.2,-6.4],[-447.6,1377.6,-7.6],[-463.6,1378.3,-8.7],[-479.5,1380.2,-10],[-495.2,1383.4,-11.2],[-510.5,1387.8,-12.5],[-525.5,1393.5,-13.7],[-540,1400.3,-15],[-553.8,1408.3,-16.2],[-567,1417.3,-17.4],[-579.5,1427.4,-18.5],[-591,1438.4,-19.6],[-601.7,1450.4,-20.6],[-611.4,1463.1,-21.6],[-627.3,1486.1,-23],[-643.3,1509.1,-24.1],[-659.2,1532.1,-24.8],[-675.2,1555.2,-25],[-691.1,1578.2,-25],[-707,1601.2,-25],[-723,1624.2,-25],[-738.9,1647.2,-25],[-754,1669,-25],[-763.8,1681.7,-25],[-774.9,1693.2,-25],[-787.1,1703.5,-25],[-800.4,1712.5,-25],[-814.5,1719.9,-25],[-829.3,1725.9,-25],[-844.7,1730.2,-25],[-860.5,1732.8,-25],[-876.4,1734.8,-25],[-892,1738.1,-25],[-907.3,1742.7,-25],[-922.2,1748.7,-25],[-936.4,1756,-25],[-949.9,1764.6,-25],[-962.7,1774.3,-25],[-974.5,1785,-25],[-985.3,1796.8,-25],[-995,1809.5,-25],[-1003.5,1823.1,-25],[-1010.8,1837.3,-25],[-1016.9,1852.1,-25],[-1021.6,1867.4,-24.9],[-1024.9,1883,-24.6],[-1026.8,1898.9,-24],[-1027.3,1914.9,-23.3],[-1026.3,1930.9,-22.4],[-1024,1946.7,-21.4],[-1020.2,1962.3,-20.2],[-1015.1,1977.4,-18.8],[-1008.7,1992.1,-17.4],[-1001,2006.1,-15.9],[-992.1,2019.3,-14.3],[-982,2031.8,-12.6],[-970.9,2043.3,-10.9],[-958.8,2053.7,-9.1],[-945.8,2063.1,-7.4],[-932,2071.2,-5.6],[-917.6,2078.1,-3.9],[-902.6,2083.7,-2.2],[-887.2,2087.9,-0.5],[-871.4,2090.8,1.1],[-855.5,2092.2,2.6],[-839.5,2092.2,4],[-823.6,2090.8,5.3],[-807.8,2088,6.5],[-792.4,2083.8,7.5],[-777.4,2078.3,8.4],[-752,2066.5,9.5],[-726.7,2054.5,10],[-701.4,2042.5,10],[-676.1,2030.6,10],[-661.6,2023.7,10],[-646.2,2020,10],[-630.5,2022.6,10],[-616.5,2030.3,10],[-601.1,2034.3,10],[-585.4,2032,10],[-560,2020.1,10],[-534.7,2008.2,10],[-509.8,1996.4,10],[-497,1987,10],[-488.9,1973.3,9.9],[-486,1957.6,9.7],[-479.4,1943.1,9.3],[-467.8,1932.3,8.9],[-442.6,1920.2,8.1],[-417.3,1908.2,7.1],[-392,1896.2,5.9],[-366.6,1884.2,4.8],[-352.1,1877.5,4.1],[-336.7,1873.6,3.5],[-320.7,1874,2.8],[-305.4,1878.5,2.2],[-291.8,1886.9,1.7],[-281,1898.6,1.2],[-268.1,1923.4,0.5],[-256.1,1948.7,0.1],[-244,1973.9,0],[-231.9,1999.2,0],[-219.8,2024.4,0],[-207.8,2049.7,0],[-195.7,2075,0],[-183.6,2100.2,0],[-171.5,2125.5,0],[-159.5,2150.8,0],[-151,2164.3,0],[-139.7,2175.6,0],[-126.2,2184.1,0.5],[-111.1,2189.2,2.4],[-95.2,2190.8,5.4],[-79.4,2188.8,9.3],[-64.4,2183.2,13.7],[-51.1,2174.3,18.4],[-40.1,2162.7,23],[-30,2150.4,27.3],[-17.5,2140.5,30.9],[-3,2133.8,33.5],[12.6,2130.5,34.9],[28.6,2130.8,34.9],[44,2134.8,34],[58.2,2142.1,32.6],[70.4,2152.5,30.6],[79.8,2165.4,28.1],[92.1,2190.5,22.8],[104.2,2215.8,16.8],[116.2,2241.1,10.5],[128.3,2266.3,4.3],[140.4,2291.6,-1.3],[147.3,2306,-4],[152.7,2321,-6.4],[155.4,2336.8,-8.2],[155.3,2352.8,-9.4],[152.4,2368.5,-10],[146.7,2383.4,-9.9],[138.5,2397.1,-9.6],[128,2409.1,-9.1],[105.8,2426.2,-7.8],[83.1,2442.5,-6.2],[60.3,2458.8,-4.4],[37.6,2475.1,-2.8],[14.8,2491.4,-1.3],[-8,2507.8,-0.3],[-30.7,2524.1,0],[-44.1,2533.7,0],[-55.8,2544.6,0],[-64.2,2558.2,0],[-68.7,2573.5,0],[-69,2589.4,0],[-65.1,2604.9,0],[-50.3,2628.6,0],[-34.1,2651.4,0],[-17.8,2674.2,0],[-1.6,2697,0],[14.6,2719.9,0],[28.5,2739.4,0.2],[39,2751.5,0.6],[51.7,2761.2,1.1],[66,2768.3,1.7],[81.4,2772.3,2.5],[97.4,2773.2,3.2],[125.3,2770.6,4.6],[153.1,2767.9,6],[181,2765.1,7.1],[200.9,2763.2,7.6],[216.8,2764.1,7.9],[242.7,2774.6,8],[268.3,2786.1,8],[293.8,2797.6,8],[319.3,2809.1,8],[344.9,2820.6,8],[370.4,2832.1,7.5],[395.9,2843.5,6.6],[421.5,2855,5.4],[447,2866.5,4],[472.5,2878,2.6],[498.1,2889.5,1.4],[523.6,2901,0.5],[549.1,2912.5,0],[574.7,2924,0],[600.2,2935.4,0],[625.7,2946.9,0],[651.3,2958.4,0],[676.8,2969.9,0],[702.3,2981.4,0],[727.9,2992.9,0],[753.4,3004.4,0],[773.5,3013.4,0]],
      width: 20, narrow: [[0.04, 0.06, 16], [0.571, 0.613, 16], [0.694, 0.743, 17]],
      zones: [
        { a: 0.044, b: 0.057, side: -1, extra: 12, kind: 'dirt' },   // 격납고 코너 안쪽 정비 바닥
        { a: 0.633, b: 0.649, side: -1, extra: 12, kind: 'dirt' },   // 교통 구역 코너
        { a: 0.768, b: 0.789, side: -1, extra: 14, kind: 'dirt' },   // 정거장 외벽 코너
        { a: 0.823, b: 0.839, side: 1, extra: 14, kind: 'dirt' }
      ],
      ramps: [{ a: 0.170, b: 0.178, h: 2.4 }, { a: 0.250, b: 0.257, h: 2.0 }, { a: 0.763, b: 0.770, h: 2.2 }, { a: 0.900, b: 0.906, h: 1.8 }],
      tunnels: [{ a: 0.092, b: 0.168 }, { a: 0.68, b: 0.76 }],
      flights: [{ a: 0.178, b: 0.294, w: 70 }, { a: 0.45, b: 0.565, w: 70 }],   // 무중력 활공, 행성 궤도 활공 (도로 없음, 링 통과)
      boostPads: [{ s: 0.10, lat: -3 }, { s: 0.12, lat: 3 }, { s: 0.14, lat: -3 }, { s: 0.16, lat: 3 }, { s: 0.31, lat: 0 }, { s: 0.36, lat: 0 }, { s: 0.40, lat: 0 }, { s: 0.66, lat: 0 }, { s: 0.85, lat: 0 }, { s: 0.91, lat: 0 }, { s: 0.93, lat: 0 }, { s: 0.95, lat: 0 }],
      itemRows: [0.03, 0.07, 0.15, 0.22, 0.30, 0.38, 0.46, 0.55, 0.62, 0.71, 0.80, 0.87, 0.93],
      theme: {
        sky: [0x02040c, 0x070b22], fog: 0x05081a, fogNear: 600, fogFar: 2200,
        sun: 0xffffff, sunInt: 1.35, sunDir: [0.4, 0.8, -0.5], hemi: [0x3346aa, 0x080a14], hemiInt: 0.75, exposure: 1.1,
        road: 'energy', roadTint: 0xffffff, roadOpacity: 0.9, roadEm: 0x35e6ff, roadEmI: 0.35, roadRough: 0.4, roadMetal: 0.2, dash: false, rumble: false, railGlow: true, ringColor: 0x35e6ff,
        rail: 0x1a2240, railEm: 0x35e6ff, ground: 'concrete', skirt: 'none', noPillars: true, zoneTex: 'labFloor', zoneTint: 0x6f7a92, zoneEm: 0x102040,
        tunnel: 0x10162a, tunnelLight: 0x35e6ff, portal: 0x35e6ff, portalEm: 0x35e6ff, pillar: 0x1d2440, gate: 0x1a2240, gateEm: 0xff2f6d,
        time: 'SPACE', stars: true, seed: 7777, gripMul: 0.97, preview: ['rgba(53,230,255,0.5)', '#0a0f2a', '#ffffff']
      }
    },
    {
      id: 'sky', name: 'SKY CRYSTAL', tag: 'ADVANCED', mood: 'sky', laps: 1, open: true, runoff: 120, cpCount: 10, lengthKm: '2.9',
      desc: '구름바다 위 얼음 수정 세계를 가로지르는 포인트-투-포인트. 얼음 협곡에서 이륙해 하늘을 활공하며 팔각 부스터 링을 통과하고, 수정 다리에 착지, 얼음 동굴과 수정 정원을 지나 오로라 사이를 날아 하늘 궁전에 도착한다.',
      pts: [[0,0,0],[0,28,0],[0,56,0],[0,84,0],[0,110,0],[-2.1,125.8,0.1],[-8.2,140.5,1.3],[-18,153.1,3.4],[-30.8,162.7,6],[-43.3,172.5,8.8],[-52.8,185.4,11.3],[-58.6,200.2,13.2],[-60.3,228.1,14],[-60.3,256.1,14],[-60.3,284.1,14],[-60.3,312.1,14],[-58.2,327.9,14],[-50.8,342,14],[-39,352.6,14],[-24.2,358.5,13.7],[3.8,359.4,8.7],[31.8,359.5,0.6],[59.8,359.7,-6.8],[87.8,359.8,-10],[115.8,359.9,-8.5],[143.8,360,-4.9],[171.8,360.1,-0.4],[199.8,360.2,3.5],[227.8,360.3,5.8],[255.8,360.5,6],[271.8,360.7,5.9],[287.7,362.4,5.2],[303.3,365.8,3.8],[318.5,370.8,2.2],[333.1,377.4,0.8],[346.8,385.5,0.1],[359.7,395,0],[371.4,405.9,0],[381.9,417.9,0],[391.1,431,0],[405.1,455.3,0],[419,479.6,0],[433,503.9,0.7],[446.9,528.2,2.4],[454.8,542.1,3.4],[458.9,557.4,3.9],[456.7,573.2,4],[448.6,586.8,4],[435.8,596.3,4],[420.4,600,4],[392.4,599.7,4],[364.4,599.5,3],[336.4,599.2,-0.4],[318.9,599.1,-3.2],[303,600.7,-6.1],[287.9,605.8,-9],[274.3,614.3,-11.8],[263.1,625.6,-14.3],[254.7,639.2,-16.3],[244.6,665.3,-18],[234.8,691.5,-15.7],[225,717.8,-9.3],[215.1,744,-1.1],[205.3,770.2,6.6],[195.5,796.4,11.4],[185.7,822.6,11.6],[175.8,848.9,9.6],[166,875.1,6.4],[156.2,901.3,3.2],[146.4,927.5,0.7],[136.5,953.7,0],[126.7,979.9,0],[116.9,1006.2,0],[111.3,1021.2,0],[106.9,1036.5,0],[104.4,1052.3,0],[103.8,1068.3,0.1],[105.3,1084.2,1.5],[108.6,1099.9,3.5],[113.9,1115,4.9],[121,1129.3,5],[129.8,1142.7,5],[140.1,1154.8,5],[151.9,1165.6,5],[164.9,1174.9,5],[189,1189.2,5],[213.1,1203.4,4.6],[237.2,1217.7,1.5],[261.4,1231.9,0],[275,1240.3,0],[284.9,1252.7,0],[288.3,1268.2,0],[284.5,1283.6,0],[274.3,1295.7,0],[259.8,1302.1,0],[231.8,1302.6,0],[203.8,1302.5,0],[175.8,1302.3,0.1],[153.3,1302.2,2.1],[137.5,1300,4.7],[122.8,1293.8,7.9],[110.3,1283.9,11.4],[100.8,1271.1,14.9],[94.5,1256.4,18],[85.7,1243,20.4],[73.8,1232.4,21.8],[59.5,1225.3,21.9],[43.9,1222.3,21],[15.9,1222,17.5],[-12.1,1221.9,12.4],[-40.1,1221.7,6.5],[-68.1,1221.5,0.7],[-96.1,1221.4,-4.2],[-124.1,1221.2,-7.3],[-152.1,1221,-7.7],[-180.1,1220.9,-3.9],[-208.1,1220.7,2.2],[-236.1,1220.5,7.7],[-264.1,1220.4,10],[-280.1,1220.3,10],[-296,1218.6,10],[-311.4,1214.2,10],[-325.7,1207.1,10],[-347.7,1189.9,8.7],[-369,1171.7,6.3],[-390.3,1153.5,3.6],[-411.6,1135.4,1.2],[-432.9,1117.2,0],[-454.2,1099,0],[-475.5,1080.9,0],[-496.8,1062.7,0],[-518.2,1044.5,0],[-539.5,1026.4,0],[-560.8,1008.2,0],[-582.1,990,0],[-595.8,978.4,0]],
      width: 20, narrow: [[0.037, 0.079, 17], [0.646, 0.676, 16]],
      zones: [
        { a: 0.112, b: 0.136, side: 1, extra: 12, kind: 'dirt' },    // 수정 동굴 코너 안쪽 (눈길)
        { a: 0.646, b: 0.676, side: -1, extra: 12, kind: 'dirt' },   // 수정 정원 헤어핀 안쪽
        { a: 0.865, b: 0.887, side: -1, extra: 12, kind: 'dirt' }
      ],
      ramps: [{ a: 0.130, b: 0.138, h: 2.4 }, { a: 0.408, b: 0.415, h: 2.2 }, { a: 0.754, b: 0.761, h: 2.2 }],
      tunnels: [{ a: 0.318, b: 0.352 }],
      flights: [{ a: 0.138, b: 0.29, w: 70 }, { a: 0.415, b: 0.60, w: 80 }, { a: 0.761, b: 0.884, w: 70 }],
      boostPads: [{ s: 0.02, lat: 0 }, { s: 0.10, lat: 0 }, { s: 0.30, lat: 0 }, { s: 0.39, lat: 0 }, { s: 0.62, lat: 0 }, { s: 0.70, lat: 3 }, { s: 0.90, lat: 0 }, { s: 0.93, lat: 0 }],
      itemRows: [0.03, 0.09, 0.2, 0.3, 0.38, 0.5, 0.62, 0.7, 0.8, 0.9],
      theme: {
        sky: [0x6cc8ff, 0xeafbff], fog: 0xd6f1ff, fogNear: 480, fogFar: 2600,
        sun: 0xffffff, sunInt: 1.5, sunDir: [0.3, 1, -0.4], hemi: [0xdff6ff, 0x9fd8ff], hemiInt: 0.95, exposure: 1.12,
        road: 'crystal', roadTint: 0xffffff, roadOpacity: 0.93, roadEm: 0x66ccff, roadEmI: 0.22, roadRough: 0.2, roadMetal: 0.1, dash: false, rumble: false, railGlow: true, ringColor: 0x66d9ff,
        rail: 0xeaf8ff, railEm: 0x9fe3ff, ground: 'snow', skirt: 'none', noPillars: true, zoneTex: 'ice', zoneTint: 0xdff6ff,
        tunnel: 0xbfe0ff, tunnelLight: 0xffffff, portal: 0xdff6ff, portalEm: 0x9fe3ff, pillar: 0xbfe0ff, gate: 0xffffff, gateEm: 0x9fe3ff,
        time: 'DAY', stars: false, seed: 5150, gripMul: 0.96, preview: ['rgba(160,230,255,0.65)', '#9fd8ff', '#ffffff']
      }
    }
  ];

  // fill theme defaults (older two tracks used mood-based choices)
  function themeDefaults(th, mood) {
    var neon = mood === 'neon';
    var d = { tunnel: neon ? 0x151a2e : 0x8d8f9b, tunnelLight: neon ? 0xff3fd6 : 0xfff3b0, portal: neon ? 0x00e5ff : 0xff6b6b, portalEm: neon ? 0x00e5ff : 0x000000,
      pillar: neon ? 0x1d1f33 : 0x9aa4b2, gate: neon ? 0x2a2a44 : 0xffffff, gateEm: neon ? 0x00e5ff : 0x000000, skirt: th.ground === 'grass' ? 'grass' : 'concrete',
      stars: neon, time: neon ? 'NIGHT' : 'DAY', sunDir: neon ? [0.35, 1, 0.45] : [-0.5, 1, -0.7], exposure: neon ? 1.15 : 1.05, seed: neon ? 777 : 333, gripMul: 1, roadTint: 0xffffff,
      preview: neon ? ['rgba(143,92,255,0.5)', '#1b1d33', '#00e5ff'] : ['rgba(47,224,255,0.4)', '#3d4049', '#ffe66d'] };
    for (var k in d) if (th[k] === undefined) th[k] = d[k];
    th.mood = mood;
  }
  TRACKS.forEach(function (t) { themeDefaults(t.theme, t.mood); });

  // ------------------------------------------------------------- helpers
  function mergeGeoms(list) {
    var pos = [], nor = [], uv = [], idx = [], off = 0;
    list.forEach(function (g) {
      var p = g.getAttribute('position'), n = g.getAttribute('normal'), u = g.getAttribute('uv');
      for (var i = 0; i < p.count; i++) {
        pos.push(p.getX(i), p.getY(i), p.getZ(i));
        nor.push(n.getX(i), n.getY(i), n.getZ(i));
        uv.push(u ? u.getX(i) : 0, u ? u.getY(i) : 0);
      }
      var ix = g.getIndex();
      if (ix) for (i = 0; i < ix.count; i++) idx.push(ix.getX(i) + off);
      else for (i = 0; i < p.count; i++) idx.push(i + off);
      off += p.count;
    });
    var geo = new THREE.BufferGeometry();
    geo.setAttribute('position', new THREE.Float32BufferAttribute(pos, 3));
    geo.setAttribute('normal', new THREE.Float32BufferAttribute(nor, 3));
    geo.setAttribute('uv', new THREE.Float32BufferAttribute(uv, 2));
    geo.setIndex(idx);
    return geo;
  }

  function boxWithUV(w, h, d, uvScale) {
    var g = new THREE.BoxGeometry(w, h, d);
    var uv = g.getAttribute('uv');
    // faces: +x,-x,+y,-y,+z,-z ; 4 verts each
    var sc = [[d, h], [d, h], [w, d], [w, d], [w, h], [w, h]];
    for (var f = 0; f < 6; f++) for (var v = 0; v < 4; v++) {
      var i = f * 4 + v;
      uv.setXY(i, uv.getX(i) * sc[f][0] / uvScale, uv.getY(i) * sc[f][1] / uvScale);
    }
    return g;
  }

  /** strip along path: edgeA(j,out), edgeB(j,out) fill world positions */
  function stripGeom(path, edgeA, edgeB, vScale, range) {
    var N = path.N, a0 = 0, a1 = N, closed = true;
    if (range) { a0 = range[0]; a1 = range[1]; closed = false; }
    var count = a1 - a0 + (closed ? 1 : 1);
    var pos = new Float32Array(count * 2 * 3), nor = new Float32Array(count * 2 * 3), uv = new Float32Array(count * 2 * 2);
    var idx = [];
    var va = new THREE.Vector3(), vb = new THREE.Vector3(), n = new THREE.Vector3(), e1 = new THREE.Vector3();
    for (var k = 0; k < count; k++) {
      var j = (a0 + k) % N;
      edgeA(j, va); edgeB(j, vb);
      e1.subVectors(vb, va);
      n.crossVectors(path.T[j], e1).normalize();
      if (n.y < 0) n.negate();
      var o = k * 6;
      pos[o] = va.x; pos[o + 1] = va.y; pos[o + 2] = va.z;
      pos[o + 3] = vb.x; pos[o + 4] = vb.y; pos[o + 5] = vb.z;
      nor[o] = n.x; nor[o + 1] = n.y; nor[o + 2] = n.z; nor[o + 3] = n.x; nor[o + 4] = n.y; nor[o + 5] = n.z;
      var v = (a0 + k) * path.ds / vScale;
      uv[k * 4] = 0; uv[k * 4 + 1] = v; uv[k * 4 + 2] = 1; uv[k * 4 + 3] = v;
      if (k < count - 1) {
        var b = k * 2;
        // counter-clockwise seen from above so the front face points up
        idx.push(b, b + 2, b + 1, b + 1, b + 2, b + 3);
      }
    }
    var geo = new THREE.BufferGeometry();
    geo.setAttribute('position', new THREE.BufferAttribute(pos, 3));
    geo.setAttribute('normal', new THREE.BufferAttribute(nor, 3));
    geo.setAttribute('uv', new THREE.BufferAttribute(uv, 2));
    geo.setIndex(idx);
    return geo;
  }

  /** arch/tube geometry over stations [a,b] */
  function archGeom(path, a, b, radiusFn, K, yBase) {
    var pos = [], nor = [], uv = [], idx = [];
    var p = new THREE.Vector3(), q = new THREE.Vector3();
    var rows = 0;
    for (var j = a; j <= b; j++) {
      var jj = j % path.N, r = radiusFn(jj);
      for (var k = 0; k <= K; k++) {
        var ang = Math.PI * k / K; // 0 = right side, PI = left side
        var lat = Math.cos(ang) * r, h = Math.sin(ang) * r * 0.75 + (yBase || 0);
        p.copy(path.P[jj]).addScaledVector(path.R[jj], lat).addScaledVector(path.U[jj], h);
        q.copy(path.R[jj]).multiplyScalar(-Math.cos(ang)).addScaledVector(path.U[jj], -Math.sin(ang));
        pos.push(p.x, p.y, p.z); nor.push(q.x, q.y, q.z);
        uv.push(k / K * 3, j * path.ds / 6);
      }
      rows++;
    }
    for (var rI = 0; rI < rows - 1; rI++) for (k = 0; k < K; k++) {
      var i0 = rI * (K + 1) + k, i1 = i0 + K + 1;
      idx.push(i0, i1, i0 + 1, i0 + 1, i1, i1 + 1);
    }
    var geo = new THREE.BufferGeometry();
    geo.setAttribute('position', new THREE.Float32BufferAttribute(pos, 3));
    geo.setAttribute('normal', new THREE.Float32BufferAttribute(nor, 3));
    geo.setAttribute('uv', new THREE.Float32BufferAttribute(uv, 2));
    geo.setIndex(idx);
    return geo;
  }

  function skyDome(colTop, colBot, stars, radius) {
    var geo = new THREE.SphereGeometry(radius || 1800, 24, 12);
    var mat = new THREE.ShaderMaterial({
      uniforms: { top: { value: new THREE.Color(colTop) }, bot: { value: new THREE.Color(colBot) } },
      vertexShader: 'varying vec3 vP; void main(){ vP = position; gl_Position = projectionMatrix * modelViewMatrix * vec4(position,1.0); }',
      fragmentShader: 'uniform vec3 top; uniform vec3 bot; varying vec3 vP; void main(){ float h = normalize(vP).y; float t = smoothstep(-0.05, 0.55, h); gl_FragColor = vec4(mix(bot, top, t), 1.0); }',
      side: THREE.BackSide, depthWrite: false, fog: false
    });
    var m = new THREE.Mesh(geo, mat);
    m.renderOrder = -10;
    var g = new THREE.Group(); g.add(m);
    if (stars) {
      var rng = U.rng(99), sp = [], sc = [];
      for (var i = 0; i < 1400; i++) {
        var th = rng() * Math.PI * 2, ph = Math.acos(rng.range(0.08, 1));
        sp.push(1500 * Math.sin(ph) * Math.cos(th), 1500 * Math.cos(ph), 1500 * Math.sin(ph) * Math.sin(th));
        var b = rng.range(0.5, 1); sc.push(b, b, b + rng() * 0.2);
      }
      var sg = new THREE.BufferGeometry();
      sg.setAttribute('position', new THREE.Float32BufferAttribute(sp, 3));
      sg.setAttribute('color', new THREE.Float32BufferAttribute(sc, 3));
      var sm = new THREE.PointsMaterial({ size: 3, vertexColors: true, sizeAttenuation: false, fog: false, transparent: true, opacity: 0.9 });
      g.add(new THREE.Points(sg, sm));
    }
    return g;
  }

  // ---------------------------------------------------------- Track class
  function Track(def, scene, quality) {
    this.def = def;
    this.theme = def.theme; this.theme.mood = def.mood;
    this.quality = quality || 'high';
    var n = def.pts.length, cps = [];
    for (var i = 0; i < n; i++) {
      var p = def.pts[i], f = i / n, w = def.width;
      (def.narrow || []).forEach(function (nr) { if (f >= nr[0] && f <= nr[1]) w = nr[2]; });
      cps.push({ x: p[0], y: p[2] || 0, z: p[1], w: w });
    }
    var path = this.path = new TrackPath(cps, { open: !!def.open });
    this._buildEnvelope();
    var N = path.N;
    this.group = new THREE.Group();
    this.animated = [];
    this.open = !!def.open;                                   // point-to-point course
    this.laps = this.open ? 1 : (def.laps || 3);
    this.cpCount = def.cpCount || 10;
    this.finishS = this.open ? path.length - (def.runoff || 120) : 0;   // finish line (open tracks), run-off after it

    // ---- per-station drivable limits & surface extras -----------------
    var minLat = this.minLat = new Float32Array(N), maxLat = this.maxLat = new Float32Array(N);
    var zoneKind = this.zoneKind = new Int8Array(N); // 0 none, 1 dirt(left) 2 dirt(right) 3 both
    var hExtra = this.hExtra = new Float32Array(N);
    var tunnelMask = this.tunnelMask = new Uint8Array(N);
    for (var j = 0; j < N; j++) { minLat[j] = -path.W[j] / 2; maxLat[j] = path.W[j] / 2; }
    this.zones = (def.zones || []).map(function (z) {
      var a = Math.round(z.a * N), b = Math.round(z.b * N), ramp = 14;
      for (var j = a; j <= b; j++) {
        var jj = j % N, f = 1;
        if (j - a < ramp) f = (j - a) / ramp; else if (b - j < ramp) f = (b - j) / ramp;
        f = U.smooth(Math.max(0, Math.min(1, f)));
        var ex = z.extra * f;
        if (z.side < 0) { minLat[jj] = -path.W[jj] / 2 - ex; zoneKind[jj] |= 1; }
        else { maxLat[jj] = path.W[jj] / 2 + ex; zoneKind[jj] |= 2; }
      }
      return { a: a, b: b, side: z.side, extra: z.extra, kind: z.kind };
    });
    this.ramps = (def.ramps || []).map(function (rp) {
      var a = Math.round(rp.a * N), b = Math.round(rp.b * N);
      for (var j = a; j < b; j++) hExtra[j % N] = rp.h * (j - a) / (b - a);
      return { a: a, b: b, h: rp.h, slope: rp.h / ((b - a) * path.ds) };
    });
    this.tunnels = (def.tunnels || []).map(function (t) {
      var a = Math.round(t.a * N), b = Math.round(t.b * N);
      for (var j = a; j <= b; j++) tunnelMask[j % N] = 1;
      return { a: a, b: b };
    });
    // flight sections: no road, a wide invisible corridor the karts glide through, octagonal boost rings every ~90m
    var flightMask = this.flightMask = new Uint8Array(N), ringPads = [];
    this.flights = (def.flights || []).map(function (f) {
      var a = Math.round(f.a * N), b = Math.round(f.b * N), w = f.w || 60, ramp = 25;
      for (var j = a; j <= b; j++) {
        var jj = j % N, t = 1;
        if (j - a < ramp) t = (j - a) / ramp; else if (b - j < ramp) t = (b - j) / ramp;
        t = U.smooth(Math.max(0, Math.min(1, t)));
        var hw = path.W[jj] / 2 + (w / 2 - path.W[jj] / 2) * t;
        if (j - a >= 4 && b - j >= 4) flightMask[jj] = 1;
        minLat[jj] = Math.min(minLat[jj], -hw); maxLat[jj] = Math.max(maxLat[jj], hw);
      }
      var gap = f.gap || 90;
      for (var s = a * path.ds + 45; s < b * path.ds - 30; s += gap) ringPads.push({ s: s, lat: 0, len: 6, w: 17, ring: true });
      return { a: a, b: b, w: w };
    });
    this.boostPads = (def.boostPads || []).map(function (bp) {
      return { s: bp.s * path.length, lat: bp.lat, len: bp.len || 7, w: bp.w || 4 };
    }).concat(ringPads);

    this._buildRoad();
    this._buildScenery();
    this._buildPortals();
    if (this.open) this._buildFinish();
    scene.add(this.group);
  }

  /** open (point-to-point) tracks: finish line + gate at finishS, numbered checkpoint arches, end barrier */
  Track.prototype._buildFinish = function () {
    var path = this.path, N = path.N, th = this.theme, self = this, tex = U.tex, g = this.group, tmp = new THREE.Vector3(), tmp2 = new THREE.Vector3();
    function edge(latFn) { return function (j, out) { self.surfacePoint(j, latFn(j), out); }; }
    var half = function (j) { return path.W[j] / 2; };
    var jf = path.idxOf(this.finishS);
    var checker = tex.checker(); checker.repeat.set(6, 1);
    var line = new THREE.Mesh(stripGeom(path, edge(function (j) { return -half(j); }), edge(function (j) { return half(j); }), 1, [jf - 1, jf + 3]), new THREE.MeshBasicMaterial({ map: checker }));
    line.position.y = 0.05; g.add(line);
    var gateMat = new THREE.MeshStandardMaterial({ color: th.gate, emissive: th.gateEm, emissiveIntensity: 0.8 });
    function arch(j, label, color, big) {
      var gw = path.W[j] + 8, gh = big ? 12 : 8, grp = new THREE.Group();
      [-gw / 2, gw / 2].forEach(function (lat) { var post = new THREE.Mesh(new THREE.BoxGeometry(1, gh, 1), gateMat); post.position.set(lat, gh / 2, 0); post.castShadow = true; grp.add(post); });
      var bar = new THREE.Mesh(new THREE.BoxGeometry(gw + 1, big ? 3 : 2, 1), gateMat); bar.position.set(0, gh, 0); grp.add(bar);
      var lab = new THREE.Mesh(new THREE.BoxGeometry(gw * 0.8, big ? 2.6 : 1.7, 1.1), new THREE.MeshBasicMaterial({ map: tex.label('cpgate' + j, label, '#ffffff', color) })); lab.position.set(0, gh, 0); grp.add(lab);
      self.surfacePoint(j, 0, tmp); tmp2.copy(tmp).add(path.T[j]);
      grp.position.copy(tmp); grp.lookAt(tmp2); g.add(grp);
      return grp;
    }
    arch(Math.max(2, jf), 'FINISH', '#ff2f6d', true);
    for (var c = 1; c < this.cpCount; c++) arch(path.idxOf(this.cpS[c]), 'CHECKPOINT ' + c + ' / ' + this.cpCount, '#1a3d6d', false);
    // end barrier so nobody drives off the end of the world
    var je = N - 3; self.surfacePoint(je, 0, tmp); tmp2.copy(tmp).add(path.T[je]);
    var wall = new THREE.Mesh(new THREE.BoxGeometry(path.W[je] + 10, 6, 1.5), new THREE.MeshStandardMaterial({ color: 0x222633, emissive: th.gateEm, emissiveIntensity: 0.6, transparent: true, opacity: 0.85 }));
    wall.position.copy(tmp); wall.position.y += 3; wall.lookAt(tmp2); g.add(wall);
  };

  /** teleport portals: def.portals = [{ a, lat, r, to, toLat }] (fractions of the lap) */
  Track.prototype._buildPortals = function () {
    var path = this.path, self = this, tex = U.tex, g = this.group;
    this.portals = (this.def.portals || []).map(function (p) { return { s: p.a * path.length, lat: p.lat || 0, r: p.r || 4.5, toS: p.to * path.length, toLat: p.toLat || 0 }; });
    if (!this.portals.length) return;
    var tmp = new THREE.Vector3(), tmp2 = new THREE.Vector3(), rings = [];
    function gate(s, lat, r, color, isExit) {
      var j = path.idxOf(s), grp = new THREE.Group();
      self.surfacePoint(j, lat, tmp); tmp2.copy(tmp).add(path.T[j]);
      grp.position.set(tmp.x, tmp.y + r + 0.3, tmp.z); grp.lookAt(tmp2.x, tmp.y + r + 0.3, tmp2.z);
      var ring = new THREE.Mesh(new THREE.TorusGeometry(r + 0.6, 0.45, 10, 40), new THREE.MeshStandardMaterial({ color: color, emissive: color, emissiveIntensity: 1.4, roughness: 0.3, metalness: 0.4 }));
      grp.add(ring);
      var disc = new THREE.Mesh(new THREE.CircleGeometry(r + 0.3, 40), new THREE.MeshBasicMaterial({ map: tex.swirl(), color: color, transparent: true, opacity: isExit ? 0.35 : 0.6, blending: THREE.AdditiveBlending, depthWrite: false, side: THREE.DoubleSide }));
      grp.add(disc);
      [-1, 1].forEach(function (sd) { var col = new THREE.Mesh(new THREE.BoxGeometry(0.5, r * 2 + 1.2, 0.5), new THREE.MeshStandardMaterial({ color: 0x1a2240, emissive: color, emissiveIntensity: 0.6 })); col.position.set(sd * (r + 1.2), 0, 0); grp.add(col); });
      var pad = new THREE.Mesh(new THREE.RingGeometry(r * 0.4, r + 1, 32).rotateX(-Math.PI / 2), new THREE.MeshBasicMaterial({ color: color, transparent: true, opacity: 0.35, blending: THREE.AdditiveBlending, depthWrite: false }));
      pad.position.set(tmp.x, tmp.y + 0.08, tmp.z); g.add(pad);
      g.add(grp); rings.push({ disc: disc, ring: ring, dir: isExit ? -1 : 1 });
    }
    this.portals.forEach(function (p) { gate(p.s, p.lat, p.r, 0x35e6ff, false); gate(p.toS, p.toLat, p.r, 0xff9a3c, true); });
    this.animated.push(function (dt, t) { for (var i = 0; i < rings.length; i++) { rings[i].disc.rotation.z += dt * 2.2 * rings[i].dir; rings[i].ring.material.emissiveIntensity = 1.1 + Math.sin(t * 4 + i) * 0.4; } });
  };

  Track.prototype.dispose = function (scene) {
    scene.remove(this.group);
    this.group.traverse(function (o) {
      if (o.geometry) o.geometry.dispose();
    });
  };

  /** ground height (world y) at station idx, lateral lat (shoulders beyond the road edge are flat) */
  Track.prototype.groundY = function (idx, lat, right, center) {
    var hw = this.path.W[idx] / 2, lc = lat < -hw ? -hw : (lat > hw ? hw : lat);
    return center.y + lc * right.y + this.hExtra[idx];
  };

  /** world position of the drivable surface at station j, lateral lat */
  Track.prototype.surfacePoint = function (j, lat, out) {
    var path = this.path, hw = path.W[j] / 2, lc = lat < -hw ? -hw : (lat > hw ? hw : lat), ex = lat - lc;
    out.copy(path.P[j]).addScaledVector(path.R[j], lc);
    if (ex !== 0) { var r = path.R[j], len = Math.hypot(r.x, r.z) || 1; out.x += r.x / len * ex; out.z += r.z / len * ex; }
    out.y += this.hExtra[j];
    return out;
  };

  Track.prototype.surfaceAt = function (idx, lat) {
    var hw = this.path.W[idx] / 2;
    if (this.flightMask && this.flightMask[idx]) return 'road';
    if (lat < -hw - 0.3 || lat > hw + 0.3) return 'dirt';
    return 'road';
  };

  Track.prototype.gridSlot = function (i, total) {
    var path = this.path;
    var cols = total > 16 ? 4 : (total > 8 ? 3 : 2);
    var row = Math.floor(i / cols), col = i % cols;
    var wStart = path.W[0], spread = (wStart - 6) / cols;
    var lat = -wStart / 2 + 3 + spread * (col + 0.5);
    var s = this.open ? 14 + row * 7 + (col % 2) * 2.5 : path.length - 10 - row * 7 - (col % 2) * 2.5;
    return { s: s, lat: lat };
  };

  // ------------------------------------------------------------ road build
  Track.prototype._buildRoad = function () {
    var path = this.path, N = path.N, th = this.theme, self = this, tex = U.tex, j;
    var g = this.group, tmp = new THREE.Vector3();
    var minLat = this.minLat, maxLat = this.maxLat, hExtra = this.hExtra;

    function edge(latFn) {
      return function (j, out) { self.surfacePoint(j, latFn(j), out); };
    }
    var half = function (j) { return path.W[j] / 2; };
    var FULL = this.open ? [0, N - 1] : undefined;   // open tracks: never join the last station back to the first
    // station runs that carry a road (flight sections have none)
    var flightMask = this.flightMask, runs = [];
    if (!this.flights.length) runs = [FULL];
    else { var inRun = false, rs = 0; for (j = 0; j < N; j++) { if (!flightMask[j] && !inRun) { inRun = true; rs = j; } if (inRun && (flightMask[j] || j === N - 1)) { inRun = false; runs.push([rs, flightMask[j] ? j - 1 : j]); } } }
    function strips(fnA, fnB, vs) { return mergeGeoms(runs.map(function (r) { return stripGeom(path, fnA, fnB, vs, r); })); }
    this.roadRuns = runs;

    // road surface (asphalt by default; themes can pick crystal / energy roads, transparency and glow)
    var roadTex = tex[th.road](); roadTex.repeat.set(1, 1);
    var roadMatOpts = { map: roadTex, color: th.roadTint, roughness: th.road === 'ice' ? 0.35 : (th.roadRough !== undefined ? th.roadRough : 0.9), metalness: th.road === 'ice' ? 0.2 : (th.roadMetal !== undefined ? th.roadMetal : 0.05) };
    if (th.roadOpacity !== undefined && th.roadOpacity < 1) { roadMatOpts.transparent = true; roadMatOpts.opacity = th.roadOpacity; roadMatOpts.side = THREE.DoubleSide; }
    if (th.roadEm) { roadMatOpts.emissive = new THREE.Color(th.roadEm); roadMatOpts.emissiveIntensity = th.roadEmI || 0.4; roadMatOpts.emissiveMap = roadTex; }
    var road = new THREE.Mesh(strips(edge(function (j) { return -half(j); }), edge(function (j) { return half(j); }), 10), new THREE.MeshStandardMaterial(roadMatOpts));
    road.receiveShadow = true; g.add(road);

    // centre dashed line
    var dashTex = U.canvasTexture('dash', 32, function (c, w, h) { c.fillStyle = 'rgba(255,255,255,0)'; c.fillRect(0, 0, w, h); c.fillStyle = '#ffe66d'; c.fillRect(0, 0, w, h / 2); }, { height: 64 });
    if (th.dash !== false) {
      var center = new THREE.Mesh(strips(edge(function () { return -0.25; }), edge(function () { return 0.25; }), 12), new THREE.MeshBasicMaterial({ map: dashTex, transparent: true, depthWrite: false }));
      center.position.y = 0.03; g.add(center);
    }

    // rumble strips (both edges)
    var rumbleTex = tex.rumble();
    var rumbleMat = new THREE.MeshStandardMaterial({ map: rumbleTex, roughness: 0.7, emissive: th.railEm, emissiveIntensity: th.railEm ? 0.25 : 0 });
    if (th.rumble !== false) {
      var rumL = strips(edge(function (j) { return -half(j) - 0.9; }), edge(function (j) { return -half(j) + 0.4; }), 3);
      var rumR = strips(edge(function (j) { return half(j) - 0.4; }), edge(function (j) { return half(j) + 0.9; }), 3);
      var rum = new THREE.Mesh(mergeGeoms([rumL, rumR]), rumbleMat); rum.position.y = 0.02; g.add(rum);
    } else {   // glowing edge lines instead of rumble strips
      var edgeMat = new THREE.MeshBasicMaterial({ color: th.railEm || 0xffffff, transparent: true, opacity: 0.9 });
      var edL = strips(edge(function (j) { return -half(j) - 0.5; }), edge(function (j) { return -half(j) + 0.25; }), 3);
      var edR = strips(edge(function (j) { return half(j) - 0.25; }), edge(function (j) { return half(j) + 0.5; }), 3);
      var edm = new THREE.Mesh(mergeGeoms([edL, edR]), edgeMat); edm.position.y = 0.03; g.add(edm);
    }

    // dirt shoulders (shortcuts)
    var dirtTex = tex[th.zoneTex || 'dirt'](); dirtTex.repeat.set(2, 1);
    var dirtMat = new THREE.MeshStandardMaterial({ map: dirtTex, roughness: 1, color: th.zoneTint || 0xffffff, emissive: th.zoneEm || 0x000000, emissiveIntensity: th.zoneEm ? 0.5 : 0 });
    var dirtGeos = [];
    this.zones.forEach(function (z) {
      var inner, outer;
      if (z.side < 0) { inner = function (j) { return minLat[j]; }; outer = function (j) { return -half(j) + 0.5; }; }
      else { inner = function (j) { return half(j) - 0.5; }; outer = function (j) { return maxLat[j]; }; }
      dirtGeos.push(stripGeom(path, edge(inner), edge(outer), 8, [z.a, z.b]));
    });
    if (dirtGeos.length) { var dirt = new THREE.Mesh(mergeGeoms(dirtGeos), dirtMat); dirt.position.y = -0.01; dirt.receiveShadow = true; g.add(dirt); }

    // guard rails
    var railMat = new THREE.MeshStandardMaterial({ color: th.rail, roughness: 0.4, metalness: 0.5, emissive: th.railEm, emissiveIntensity: th.railEm ? 0.9 : 0, side: THREE.DoubleSide });
    function railEdge(latFn, h) { return function (j, out) { self.surfacePoint(j, latFn(j), out); out.y += h; }; }
    var railLo = th.railGlow ? 0.5 : 0.35, railHi = th.railGlow ? 0.8 : 0.95;
    var rl = strips(railEdge(function (j) { return minLat[j] - 0.7; }, railLo), railEdge(function (j) { return minLat[j] - 0.7; }, railHi), 4);
    var rr = strips(railEdge(function (j) { return maxLat[j] + 0.7; }, railLo), railEdge(function (j) { return maxLat[j] + 0.7; }, railHi), 4);
    var rails = new THREE.Mesh(mergeGeoms([rl, rr]), railMat); rails.castShadow = true; g.add(rails);
    // rail posts
    var postGeo = new THREE.BoxGeometry(0.25, 1.0, 0.25);
    var postCount = Math.ceil(N / 8) * 2 + 2;
    var posts = new THREE.InstancedMesh(postGeo, new THREE.MeshStandardMaterial({ color: 0x2a2f3a, roughness: 0.6 }), postCount);
    var m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(1, 1, 1), pi = 0;
    for (j = 0; j < N; j += 8) {
      if (flightMask[j]) continue;
      [minLat[j] - 0.7, maxLat[j] + 0.7].forEach(function (lat) {
        self.surfacePoint(j, lat, tmp); tmp.y += 0.5;
        m4.compose(tmp, q, sc); posts.setMatrixAt(pi++, m4);
      });
    }
    posts.count = pi; g.add(posts);

    // skirts (embankment) & pillars for elevated parts
    var groundY = -0.3;
    var skirtMat = new THREE.MeshStandardMaterial({ map: tex[th.skirt === 'none' ? 'concrete' : th.skirt](), roughness: 1, side: THREE.DoubleSide });
    var skirtL = strips(railEdge(function (j) { return minLat[j] - 1.2; }, 0), function (j, out) { self.surfacePoint(j, minLat[j] - 1.2 - Math.min(8, Math.max(1.5, path.P[j].y + hExtra[j]) * 0.8), out); out.y = groundY; }, 6);
    var skirtR = strips(railEdge(function (j) { return maxLat[j] + 1.2; }, 0), function (j, out) { self.surfacePoint(j, maxLat[j] + 1.2 + Math.min(8, Math.max(1.5, path.P[j].y + hExtra[j]) * 0.8), out); out.y = groundY; }, 6);
    if (th.skirt !== 'none') { var skirt = new THREE.Mesh(mergeGeoms([skirtL, skirtR]), skirtMat); skirt.receiveShadow = true; g.add(skirt); }
    // under-deck & pillars where high
    var pillarGeo = new THREE.CylinderGeometry(1.2, 1.5, 1, 10);
    var pillars = [];
    for (j = 0; j < N; j += 14) {
      var h = path.P[j].y + hExtra[j];
      if (h > 5 && !flightMask[j]) {
        [-path.W[j] / 3, path.W[j] / 3].forEach(function (lat) {
          // pillar top stays 0.6m under the real (banked) road surface at that lateral, so it never pokes through the deck
          self.surfacePoint(j, lat, tmp);
          var H = tmp.y - 0.6 - groundY;
          if (H < 1) return;
          var pg = pillarGeo.clone();
          pg.scale(1, H, 1);
          pg.translate(tmp.x, H / 2 + groundY, tmp.z);
          pillars.push(pg);
        });
      }
    }
    if (pillars.length && !th.noPillars) {
      var pm = new THREE.Mesh(mergeGeoms(pillars), new THREE.MeshStandardMaterial({ color: th.pillar, roughness: 0.8 }));
      pm.castShadow = true; g.add(pm);
    }

    // tunnels
    var tunMat = new THREE.MeshStandardMaterial({ color: th.tunnel, roughness: 0.9, side: THREE.DoubleSide });
    var tunGeos = [], lightGeos = [];
    var stripMat = new THREE.MeshBasicMaterial({ color: th.tunnelLight });
    this.tunnels.forEach(function (t) {
      tunGeos.push(archGeom(path, t.a, t.b, function (j) { return maxLat[j] + 3.5; }, 10, 0));
      // light ring strips every ~14 stations
      for (var j = t.a + 4; j < t.b - 2; j += 12) {
        lightGeos.push(archGeom(path, j, j + 1, function (jj) { return maxLat[jj] + 3.2; }, 10, 0));
      }
      // portal frames
      [t.a, t.b].forEach(function (j) {
        var jj = j % N;
        var frame = new THREE.Mesh(archGeom(path, jj - 2 + N, jj + 2 + N, function (k) { return maxLat[k % N] + 4.4; }, 10, 0), new THREE.MeshStandardMaterial({ color: th.portal, emissive: th.portalEm, emissiveIntensity: 0.8, side: THREE.DoubleSide }));
        g.add(frame);
      });
    });
    if (tunGeos.length) { var tun = new THREE.Mesh(mergeGeoms(tunGeos), tunMat); tun.receiveShadow = true; g.add(tun); }
    if (lightGeos.length) { g.add(new THREE.Mesh(mergeGeoms(lightGeos), stripMat)); }

    // start / finish line + gate
    var checker = tex.checker(); checker.repeat.set(6, 1);
    var line = new THREE.Mesh(stripGeom(path, edge(function (j) { return -half(j); }), edge(function (j) { return half(j); }), 1, [0, 3]), new THREE.MeshBasicMaterial({ map: checker }));
    line.position.y = 0.04; g.add(line);
    var gateMat = new THREE.MeshStandardMaterial({ color: th.gate, emissive: th.gateEm, emissiveIntensity: 0.6 });
    var gateH = 9, gw = path.W[0] + 6;
    [-gw / 2, gw / 2].forEach(function (lat) {
      var post = new THREE.Mesh(new THREE.BoxGeometry(1, gateH, 1), gateMat);
      post.position.copy(path.P[1]).addScaledVector(path.R[1], lat); post.position.y += gateH / 2; post.castShadow = true; g.add(post);
    });
    var banner = new THREE.Mesh(new THREE.BoxGeometry(gw + 1, 2.4, 1.2), new THREE.MeshStandardMaterial({ map: U.tex.label('finish', 'NITRO RUSH', '#ffffff', '#ff2f6d') }));
    banner.position.copy(path.P[1]); banner.position.y += gateH - 1.2;
    banner.quaternion.setFromRotationMatrix(new THREE.Matrix4().lookAt(new THREE.Vector3(0, 0, 0), path.T[1].clone().negate(), UPV));
    banner.castShadow = true; g.add(banner);
    var chk = new THREE.Mesh(new THREE.BoxGeometry(gw + 1, 0.8, 1.3), new THREE.MeshBasicMaterial({ map: tex.checker() }));
    chk.material.map = U.canvasTexture('checkerWide', 128, function (c, w, h) { for (var y = 0; y < 2; y++) for (var x = 0; x < 16; x++) { c.fillStyle = (x + y) % 2 ? '#111' : '#fff'; c.fillRect(x * 8, y * 8, 8, 8); } }, { nearest: true, height: 16 });
    chk.position.copy(banner.position); chk.position.y -= 1.6; chk.quaternion.copy(banner.quaternion); g.add(chk);

    // boost pads
    var padTex = U.canvasTexture('pad', 64, function (c, w, h) {
      c.fillStyle = '#1b6cff'; c.fillRect(0, 0, w, h);
      c.fillStyle = '#7dfcff';
      for (var i = 0; i < 3; i++) { var y = i * 20 + 22; c.beginPath(); c.moveTo(8, y); c.lineTo(32, y - 12); c.lineTo(56, y); c.lineTo(56, y - 6); c.lineTo(32, y - 18); c.lineTo(8, y - 6); c.fill(); }
    }, { clamp: true });
    var padMat = this.padMat = new THREE.MeshBasicMaterial({ map: padTex, transparent: true, opacity: 0.95 });
    var padGeo = new THREE.PlaneGeometry(1, 1);
    var ringMeshes = [], ringColor = th.ringColor || 0x35e6ff, ringTmp = new THREE.Vector3();
    var chevTex = tex.label('chev', '<<', '#ffe066', '#3a2a8a');
    this.boostPads.forEach(function (bp) {
      var sm = path.sample(bp.s);
      var idx = sm.idx;
      if (bp.ring) {   // octagonal flight ring (like a boost gate in the sky)
        var grp = new THREE.Group();
        grp.add(new THREE.Mesh(new THREE.TorusGeometry(9, 0.9, 6, 8), new THREE.MeshStandardMaterial({ color: ringColor, emissive: ringColor, emissiveIntensity: 1.3, roughness: 0.3, metalness: 0.3 })));
        grp.add(new THREE.Mesh(new THREE.TorusGeometry(9, 0.35, 6, 8), new THREE.MeshStandardMaterial({ color: 0xffffff, emissive: 0xffffff, emissiveIntensity: 1.0 })).translateZ(0.9));
        var disc = new THREE.Mesh(new THREE.CircleGeometry(8.6, 8), new THREE.MeshBasicMaterial({ color: ringColor, transparent: true, opacity: 0.22, blending: THREE.AdditiveBlending, depthWrite: false, side: THREE.DoubleSide })); grp.add(disc);
        var chev = new THREE.Mesh(new THREE.BoxGeometry(5, 2.2, 0.4), new THREE.MeshBasicMaterial({ map: chevTex })); chev.position.set(0, 10.6, 0); grp.add(chev);
        grp.position.copy(sm.pos).addScaledVector(sm.right, bp.lat); grp.position.y += hExtra[idx] + 3.6;
        ringTmp.copy(grp.position).add(sm.tan); grp.lookAt(ringTmp);
        g.add(grp); ringMeshes.push({ g: grp, disc: disc, ph: Math.random() * 6.28 }); bp.idx = idx; return;
      }
      var m = new THREE.Mesh(padGeo, padMat);
      m.position.copy(sm.pos).addScaledVector(sm.right, bp.lat); m.position.y += hExtra[idx] + 0.06;
      var look = new THREE.Matrix4().lookAt(new THREE.Vector3(), sm.up.clone().negate(), sm.tan);
      m.quaternion.setFromRotationMatrix(look);
      m.scale.set(bp.w, bp.len, 1);
      g.add(m);
      bp.mesh = m; bp.idx = idx;
    });

    if (ringMeshes.length) this.animated.push(function (dt, t) { for (var i = 0; i < ringMeshes.length; i++) { var r = ringMeshes[i]; r.disc.rotation.z += dt * 1.2; var sc = 1 + Math.sin(t * 3 + r.ph) * 0.03; r.g.scale.set(sc, sc, 1); } });

    // checkpoints visual (thin light beams for neon, flags for coast) - subtle
    this.cpS = [];
    if (this.open) { this.cpS.push(this.finishS); for (var c = 1; c < this.cpCount; c++) this.cpS.push(this.finishS * c / this.cpCount); }
    else for (c = 0; c < this.cpCount; c++) this.cpS.push(c * path.length / this.cpCount);
  };

  // --------------------------------------------------------------- scenery
  /** radial envelope of the layout (max distance from origin per angle) + coarse station list for distance queries */
  Track.prototype._buildEnvelope = function () {
    var path = this.path, N = path.N, bins = 72, env = new Float32Array(bins);
    var maxR = 0;
    for (var j = 0; j < N; j++) {
      var p = path.P[j], r = Math.hypot(p.x, p.z), a = Math.atan2(p.z, p.x);
      var b = Math.floor(((a / (Math.PI * 2)) % 1 + 1) % 1 * bins) % bins;
      if (r > env[b]) env[b] = r;
      if (r > maxR) maxR = r;
    }
    // fill empty bins from neighbours and smooth
    for (var k = 0; k < 3; k++) {
      var e2 = new Float32Array(bins);
      for (b = 0; b < bins; b++) {
        var l = env[(b + bins - 1) % bins], c = env[b], rgt = env[(b + 1) % bins];
        if (c === 0) c = Math.max(l, rgt);
        e2[b] = Math.max(c, (l + c + rgt) / 3);
      }
      env = e2;
    }
    this.envelopeBins = env; this.maxRadius = maxR;
    var self = this;
    this.envelope = function (ang) {
      var t = ((ang / (Math.PI * 2)) % 1 + 1) % 1 * bins, i0 = Math.floor(t) % bins, i1 = (i0 + 1) % bins, f = t - Math.floor(t);
      return env[i0] * (1 - f) + env[i1] * f;
    };
    this.trackRadius = this.envelope;
    var coarse = this.coarse = [];
    for (j = 0; j < N; j += 4) coarse.push(path.P[j]);
    this.distToPath = function (x, z) {
      var best = 1e9;
      for (var i = 0; i < coarse.length; i++) { var dx = coarse[i].x - x, dz = coarse[i].z - z, d = dx * dx + dz * dz; if (d < best) best = d; }
      return Math.sqrt(best);
    };
  };

  Track.prototype._buildScenery = function () {
    var th = this.theme, g = this.group;
    var rng = U.rng(th.seed);
    // sky dome centred on the course and big enough to contain all of it (point-to-point courses are several km long)
    var cxs = 0, czs = 0, Ps = this.path.P;
    for (var si = 0; si < Ps.length; si++) { cxs += Ps[si].x; czs += Ps[si].z; }
    cxs /= Ps.length; czs /= Ps.length;
    var ext = 0; for (si = 0; si < Ps.length; si++) ext = Math.max(ext, Math.hypot(Ps[si].x - cxs, Ps[si].z - czs));
    var sky = skyDome(th.sky[0], th.sky[1], th.stars, Math.max(1800, ext + 1500)); sky.position.set(cxs, 0, czs); g.add(sky);
    var groundY = -0.3;
    var fn = { coast: this._sceneryCoast, neon: this._sceneryNeon, canyon: this._sceneryCanyon, frost: this._sceneryFrost, seoul: this._scenerySeoul, jeju: this._sceneryJeju, lab: this._sceneryLab, space: this._scenerySpace, sky: this._scenerySky }[th.mood] || this._sceneryCoast;
    fn.call(this, rng, this.envelope, null, groundY);
  };

  function fanGeom(radiusFn, segments, y, inner) {
    var pos = [], nor = [], uv = [], idx = [];
    if (!inner) { pos.push(0, y, 0); nor.push(0, 1, 0); uv.push(0, 0); }
    for (var i = 0; i <= segments; i++) {
      var a = i / segments * Math.PI * 2, r = radiusFn(a);
      var x = Math.cos(a) * r, z = Math.sin(a) * r;
      if (inner) { var ri = inner(a); pos.push(Math.cos(a) * ri, y, Math.sin(a) * ri); nor.push(0, 1, 0); uv.push(Math.cos(a) * ri / 20, Math.sin(a) * ri / 20); }
      pos.push(x, y, z); nor.push(0, 1, 0); uv.push(x / 20, z / 20);
    }
    if (!inner) { for (i = 0; i < segments; i++) idx.push(0, i + 2, i + 1); }
    else { for (i = 0; i < segments; i++) { var b = i * 2; idx.push(b, b + 1, b + 2, b + 1, b + 3, b + 2); } }
    var geo = new THREE.BufferGeometry();
    geo.setAttribute('position', new THREE.Float32BufferAttribute(pos, 3));
    geo.setAttribute('normal', new THREE.Float32BufferAttribute(nor, 3));
    geo.setAttribute('uv', new THREE.Float32BufferAttribute(uv, 2));
    geo.setIndex(idx);
    return geo;
  }

  Track.prototype._sceneryCoast = function (rng, trackRadius, trackHeight, groundY) {
    var g = this.group, tex = U.tex, path = this.path;
    // island: grass fan to radius+22, sand ring to +55, water beyond
    var grass = new THREE.Mesh(fanGeom(function (a) { return trackRadius(a) + 24; }, 180, groundY), new THREE.MeshStandardMaterial({ map: tex.grass(), roughness: 1 }));
    grass.receiveShadow = true; g.add(grass);
    var sand = new THREE.Mesh(fanGeom(function (a) { return trackRadius(a) + 62 + Math.sin(a * 7) * 6; }, 180, groundY - 0.05, function (a) { return trackRadius(a) + 23; }), new THREE.MeshStandardMaterial({ map: tex.sand(), roughness: 1 }));
    sand.receiveShadow = true; g.add(sand);
    var waterTex = tex.water(); waterTex.repeat.set(80, 80);
    var water = new THREE.Mesh(new THREE.PlaneGeometry(3600, 3600), new THREE.MeshStandardMaterial({ map: waterTex, roughness: 0.15, metalness: 0.3, color: 0x9fdcff }));
    water.rotation.x = -Math.PI / 2; water.position.y = groundY - 0.9; g.add(water);
    this.animated.push(function (dt, t) { waterTex.offset.x = t * 0.01; waterTex.offset.y = Math.sin(t * 0.3) * 0.02; });

    // palms along both sides of the road
    var trunkGeo = new THREE.CylinderGeometry(0.25, 0.42, 7, 6); trunkGeo.translate(0, 3.5, 0);
    var frondGeo = new THREE.PlaneGeometry(5, 2.2); frondGeo.translate(2.5, 0, 0);
    var trunks = [], fronds = [];
    var tmp = new THREE.Vector3(), m4 = new THREE.Matrix4(), e = new THREE.Euler(), q = new THREE.Quaternion(), sc = new THREE.Vector3();
    var palmSpots = [];
    for (var j = 0; j < path.N; j += 10) {
      if (this.tunnelMask[j]) continue;
      if (rng() < 0.45) {
        var side = rng() < 0.5 ? -1 : 1;
        var lat = side > 0 ? this.maxLat[j] + rng.range(5, 13) : this.minLat[j] - rng.range(5, 13);
        tmp.copy(path.P[j]).addScaledVector(path.R[j], lat); tmp.y = groundY;
        palmSpots.push({ p: tmp.clone(), s: rng.range(0.8, 1.3), rot: rng() * 6.28 });
      }
    }
    var trunkMesh = new THREE.InstancedMesh(trunkGeo, new THREE.MeshStandardMaterial({ color: 0x9c6b3c, roughness: 1 }), palmSpots.length);
    var frondMesh = new THREE.InstancedMesh(frondGeo, new THREE.MeshStandardMaterial({ map: tex.palmFrond(), transparent: true, alphaTest: 0.5, side: THREE.DoubleSide, roughness: 1 }), palmSpots.length * 6);
    palmSpots.forEach(function (ps, i) {
      e.set(0, ps.rot, 0.12); q.setFromEuler(e); sc.set(ps.s, ps.s, ps.s);
      m4.compose(ps.p, q, sc); trunkMesh.setMatrixAt(i, m4);
      for (var k = 0; k < 6; k++) {
        e.set(0, k / 6 * Math.PI * 2 + ps.rot, -0.55 + rng() * 0.2); q.setFromEuler(e);
        tmp.copy(ps.p); tmp.y += 7 * ps.s;
        m4.compose(tmp, q, sc); frondMesh.setMatrixAt(i * 6 + k, m4);
      }
    });
    trunkMesh.castShadow = true; frondMesh.castShadow = true; g.add(trunkMesh); g.add(frondMesh);

    // colourful buildings inside the loop and along the coast
    var pastel = [0xff9aa2, 0xffd166, 0x9be7ff, 0xb5ead7, 0xe2c2ff, 0xfff5ba, 0xffb7b2, 0xc7ceea];
    var byVar = [[], [], [], [], []];
    var cnt = 0, tries = 0, self = this, startP = path.P[30];
    while (cnt < 110 && tries++ < 3000) {
      var a = rng() * Math.PI * 2, tr = trackRadius(a);
      var inside = rng() < 0.75;
      var r = inside ? rng.range(0, tr - 30) : rng.range(tr + 28, tr + 52);
      var w = rng.range(8, 22), d = rng.range(8, 22), h = inside ? rng.range(8, 42) : rng.range(6, 16);
      var x = Math.cos(a) * r, z = Math.sin(a) * r;
      if (self.distToPath(x, z) < 24 + Math.max(w, d) * 0.7) continue;
      if (Math.hypot(x - startP.x, z - startP.z) < 70) continue; // grandstands live here
      var geo = boxWithUV(w, h, d, 6);
      geo.translate(x, h / 2 + groundY, z);
      geo.rotateY(0); // axis aligned looks city-like
      var vi = rng.int(0, 4);
      byVar[vi].push(geo);
      cnt++;
    }
    byVar.forEach(function (list, vi) {
      if (!list.length) return;
      var mat = new THREE.MeshStandardMaterial({ map: tex.windowsDay(vi), color: pastel[vi % pastel.length], roughness: 0.8 });
      var m = new THREE.Mesh(mergeGeoms(list), mat); m.castShadow = true; m.receiveShadow = true; g.add(m);
    });

    // grandstands near start line (both sides)
    var crowdTex = U.canvasTexture('crowd', 128, function (c, w, h) {
      var r2 = U.rng(5); var cols = ['#ff4d6d', '#ffd166', '#06d6a0', '#118ab2', '#ffffff', '#f4a261'];
      c.fillStyle = '#334'; c.fillRect(0, 0, w, h);
      for (var i = 0; i < 400; i++) { c.fillStyle = r2.pick(cols); c.beginPath(); c.arc(r2() * w, r2() * h, 3, 0, 7); c.fill(); }
    });
    crowdTex.repeat.set(6, 2);
    var standMat = new THREE.MeshStandardMaterial({ map: crowdTex, roughness: 1 });
    var roofMat = new THREE.MeshStandardMaterial({ color: 0xff2f6d, roughness: 0.6 });
    for (var side = -1; side <= 1; side += 2) {
      for (var jj = 8; jj < 60; jj += 2) {
        // build stepped stands using few boxes
      }
      var j0 = 6, j1 = 56, sm0 = path.P[j0], sm1 = path.P[j1];
      var len = sm0.distanceTo(sm1);
      var mid = sm0.clone().lerp(sm1, 0.5);
      var lat = side > 0 ? this.maxLat[30] + 9 : this.minLat[30] - 9;
      mid.addScaledVector(path.R[30], lat);
      var yaw = Math.atan2(path.T[30].x, path.T[30].z);
      for (var step = 0; step < 4; step++) {
        var box = new THREE.Mesh(new THREE.BoxGeometry(len, 1.6, 3), standMat);
        box.position.copy(mid); box.position.y = groundY + 0.8 + step * 1.6;
        box.position.addScaledVector(path.R[30], side * step * 2.6);
        box.rotation.y = yaw + Math.PI / 2; box.castShadow = true; box.receiveShadow = true; g.add(box);
      }
      var roof = new THREE.Mesh(new THREE.BoxGeometry(len + 2, 0.5, 14), roofMat);
      roof.position.copy(mid).addScaledVector(path.R[30], side * 5); roof.position.y = groundY + 10;
      roof.rotation.y = yaw + Math.PI / 2; roof.castShadow = true; g.add(roof);
    }

    // clouds
    var cloudMat = new THREE.SpriteMaterial({ map: tex.smoke(), color: 0xffffff, opacity: 0.95, transparent: true, depthWrite: false, fog: false });
    for (var c = 0; c < 26; c++) {
      var a2 = rng() * Math.PI * 2, r2 = rng.range(200, 900);
      var cl = new THREE.Sprite(cloudMat);
      cl.position.set(Math.cos(a2) * r2, rng.range(90, 170), Math.sin(a2) * r2);
      var s2 = rng.range(50, 120); cl.scale.set(s2 * 1.6, s2 * 0.8, 1);
      g.add(cl);
    }
    // sun
    var sun = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xfff1b0, transparent: true, fog: false }));
    sun.position.set(-600, 500, -900); sun.scale.set(260, 260, 1); g.add(sun);

    // hot air balloons
    for (var b = 0; b < 5; b++) {
      var ba = rng() * 6.28, br = rng.range(120, 260);
      var balloon = new THREE.Group();
      var env = new THREE.Mesh(new THREE.SphereGeometry(6, 12, 10), new THREE.MeshStandardMaterial({ color: rng.pick(pastel), roughness: 0.7 }));
      var basket = new THREE.Mesh(new THREE.BoxGeometry(2, 1.5, 2), new THREE.MeshStandardMaterial({ color: 0x8b5a2b }));
      basket.position.y = -8.5; balloon.add(env); balloon.add(basket);
      balloon.position.set(Math.cos(ba) * br, rng.range(40, 80), Math.sin(ba) * br);
      g.add(balloon);
      (function (bl, ph) { this.animated.push(function (dt, t) { bl.position.y += Math.sin(t * 0.5 + ph) * 0.02; bl.position.x += Math.cos(t * 0.2 + ph) * 0.03; }); }).call(this, balloon, b);
    }
  };

  Track.prototype._sceneryNeon = function (rng, trackRadius, trackHeight, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this;
    var ground = new THREE.Mesh(new THREE.PlaneGeometry(3000, 3000), new THREE.MeshStandardMaterial({ map: (function () { var t = tex.concrete(); t.repeat.set(150, 150); return t; })(), color: 0x6a6f86, roughness: 0.9 }));
    ground.rotation.x = -Math.PI / 2; ground.position.y = groundY; ground.receiveShadow = true; g.add(ground);
    // glowing street grid lines on the ground
    var gridTex = U.canvasTexture('grid', 256, function (c, w, h) {
      c.fillStyle = 'rgba(0,0,0,0)'; c.fillRect(0, 0, w, h);
      c.strokeStyle = 'rgba(0,229,255,0.5)'; c.lineWidth = 3; c.strokeRect(2, 2, w - 4, h - 4);
    });
    gridTex.repeat.set(60, 60);
    var grid = new THREE.Mesh(new THREE.PlaneGeometry(3000, 3000), new THREE.MeshBasicMaterial({ map: gridTex, transparent: true, opacity: 0.35, depthWrite: false }));
    grid.rotation.x = -Math.PI / 2; grid.position.y = groundY + 0.02; g.add(grid);

    // dense neon skyline
    var neonCols = [0x00e5ff, 0xff2fd6, 0xffe22f, 0x8f5cff, 0x2fff9b, 0xff5f2f];
    var byVar = [[], [], [], [], []], strips = [];
    var cnt = 0, tries = 0, startP = path.P[30];
    while (cnt < 210 && tries++ < 6000) {
      var a = rng() * Math.PI * 2, tr = trackRadius(a);
      var inside = rng() < 0.5;
      var r = inside ? rng.range(0, tr - 26) : rng.range(tr + 24, tr + 170);
      var w = rng.range(8, 26), d = rng.range(8, 26), h = inside ? rng.range(15, 70) : rng.range(20, 120);
      var x = Math.cos(a) * r, z = Math.sin(a) * r;
      if (self.distToPath(x, z) < 22 + Math.max(w, d) * 0.7) continue;
      if (Math.hypot(x - startP.x, z - startP.z) < 60) continue;
      var geo = boxWithUV(w, h, d, 5);
      geo.translate(x, h / 2 + groundY, z);
      byVar[rng.int(0, 4)].push(geo);
      // neon edge strips (vertical) on two corners
      var col = rng.pick(neonCols);
      for (var c2 = 0; c2 < 2; c2++) {
        var sx = x + (c2 ? w / 2 : -w / 2), sz = z + (c2 ? d / 2 : -d / 2);
        var sg = new THREE.BoxGeometry(0.5, h, 0.5); sg.translate(sx, h / 2 + groundY, sz);
        strips.push({ geo: sg, col: col });
      }
      // roof cap strip
      var rg = new THREE.BoxGeometry(w + 0.6, 0.5, d + 0.6); rg.translate(x, h + groundY, z);
      strips.push({ geo: rg, col: col });
      cnt++;
    }
    byVar.forEach(function (list, vi) {
      if (!list.length) return;
      var mat = new THREE.MeshStandardMaterial({ map: tex.windowsNight(vi), color: 0xffffff, emissive: 0xffffff, emissiveMap: tex.windowsNight(vi), emissiveIntensity: 0.9, roughness: 0.6 });
      var m = new THREE.Mesh(mergeGeoms(list), mat); m.castShadow = false; m.receiveShadow = true; g.add(m);
    });
    // group strips by colour
    var byCol = {};
    strips.forEach(function (s) { (byCol[s.col] = byCol[s.col] || []).push(s.geo); });
    Object.keys(byCol).forEach(function (col) {
      var m = new THREE.Mesh(mergeGeoms(byCol[col]), new THREE.MeshBasicMaterial({ color: parseInt(col) }));
      g.add(m);
    });

    // billboards near the track
    for (var b = 0; b < 16; b++) {
      var j = Math.floor(rng() * path.N);
      if (this.tunnelMask[j]) continue;
      var side = rng() < 0.5 ? -1 : 1;
      var lat = side > 0 ? this.maxLat[j] + rng.range(6, 12) : this.minLat[j] - rng.range(6, 12);
      var bb = new THREE.Group();
      var panel = new THREE.Mesh(new THREE.BoxGeometry(14, 7, 0.6), new THREE.MeshBasicMaterial({ map: tex.billboard(b) }));
      panel.position.y = 9;
      var pole = new THREE.Mesh(new THREE.BoxGeometry(0.8, 9, 0.8), new THREE.MeshStandardMaterial({ color: 0x222233 }));
      pole.position.y = 4.5;
      bb.add(panel); bb.add(pole);
      bb.position.copy(path.P[j]).addScaledVector(path.R[j], lat); bb.position.y = groundY;
      bb.lookAt(path.P[j].x, groundY, path.P[j].z);
      g.add(bb);
    }

    // neon lamp posts along the road
    var lampGeo = new THREE.BoxGeometry(0.3, 6, 0.3); lampGeo.translate(0, 3, 0);
    var headGeo = new THREE.BoxGeometry(1.6, 0.3, 0.5); headGeo.translate(0, 6, 0);
    var spots = [];
    for (j = 0; j < path.N; j += 24) {
      if (this.tunnelMask[j]) continue;
      spots.push({ j: j, lat: this.maxLat[j] + 2.2 }); spots.push({ j: j, lat: this.minLat[j] - 2.2 });
    }
    var lamps = new THREE.InstancedMesh(lampGeo, new THREE.MeshStandardMaterial({ color: 0x2a2a3e }), spots.length);
    var heads = new THREE.InstancedMesh(headGeo, new THREE.MeshBasicMaterial({ color: 0x9dfbff }), spots.length);
    var m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(1, 1, 1), tmp = new THREE.Vector3(), e = new THREE.Euler();
    spots.forEach(function (sp, i) {
      tmp.copy(path.P[sp.j]).addScaledVector(path.R[sp.j], sp.lat); tmp.y += self.hExtra[sp.j];
      e.set(0, Math.atan2(path.T[sp.j].x, path.T[sp.j].z), 0); q.setFromEuler(e);
      m4.compose(tmp, q, sc); lamps.setMatrixAt(i, m4); heads.setMatrixAt(i, m4);
    });
    g.add(lamps); g.add(heads);

    // moon
    var moon = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xdfe8ff, transparent: true, fog: false }));
    moon.position.set(700, 600, -800); moon.scale.set(160, 160, 1); g.add(moon);

    // rotating searchlights
    var beamMat = new THREE.MeshBasicMaterial({ color: 0x7fe7ff, transparent: true, opacity: 0.12, depthWrite: false, side: THREE.DoubleSide, blending: THREE.AdditiveBlending });
    for (var s = 0; s < 4; s++) {
      var beam = new THREE.Mesh(new THREE.ConeGeometry(14, 260, 12, 1, true), beamMat);
      beam.geometry.translate(0, 130, 0);
      var ba = rng() * 6.28, br = rng.range(60, 200);
      beam.position.set(Math.cos(ba) * br, groundY, Math.sin(ba) * br);
      beam.rotation.z = 0.5;
      g.add(beam);
      (function (bm, ph) { self.animated.push(function (dt, t) { bm.rotation.y = t * 0.4 + ph; }); })(beam, s);
    }

    // neon pulse on rails
    var railEmTarget = 0.9;
    this.animated.push(function (dt, t) { self.padMat.opacity = 0.75 + Math.sin(t * 6) * 0.2; });
  };

  // ---------------------------------------------------------- canyon (sunset desert)
  Track.prototype._sceneryCanyon = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this;
    var sandTex = tex.sand(); sandTex.repeat.set(140, 140);
    var ground = new THREE.Mesh(new THREE.PlaneGeometry(3400, 3400), new THREE.MeshStandardMaterial({ map: sandTex, color: 0xe0a060, roughness: 1 }));
    ground.rotation.x = -Math.PI / 2; ground.position.y = groundY; ground.receiveShadow = true; g.add(ground);
    // mesas: layered sandstone blocks, big ones far out, a few inside the loop
    var mesas = [], tries = 0, count = 0;
    while (count < 70 && tries++ < 4000) {
      var a = rng() * Math.PI * 2, tr = envelope(a);
      var inside = rng() < 0.3;
      var r = inside ? rng.range(0, Math.max(0, tr - 60)) : rng.range(tr + 40, tr + 320);
      var w = rng.range(18, 90), d = rng.range(18, 90), h = inside ? rng.range(10, 30) : rng.range(15, 70);
      var x = Math.cos(a) * r, z = Math.sin(a) * r;
      if (self.distToPath(x, z) < 30 + Math.max(w, d) * 0.7) continue;
      var geo = boxWithUV(w, h, d, 12); geo.translate(x, h / 2 + groundY, z);
      mesas.push(geo);
      // stepped top
      if (rng() < 0.6) { var g2 = boxWithUV(w * 0.6, h * 0.35, d * 0.6, 12); g2.translate(x + rng.range(-w * 0.1, w * 0.1), h + h * 0.175 + groundY, z); mesas.push(g2); }
      count++;
    }
    if (mesas.length) { var mm = new THREE.Mesh(mergeGeoms(mesas), new THREE.MeshStandardMaterial({ map: tex.rock(), roughness: 1 })); mm.castShadow = true; mm.receiveShadow = true; g.add(mm); }
    // boulders near the road
    var rockGeo = new THREE.DodecahedronGeometry(1, 0);
    var spots = [];
    for (var j = 0; j < path.N; j += 9) {
      if (this.tunnelMask[j]) continue;
      if (rng() < 0.5) { var side = rng() < 0.5 ? -1 : 1; spots.push({ j: j, lat: side > 0 ? this.maxLat[j] + rng.range(4, 14) : this.minLat[j] - rng.range(4, 14), s: rng.range(0.8, 3.2), rot: rng() * 6.28 }); }
    }
    var rocks = new THREE.InstancedMesh(rockGeo, new THREE.MeshStandardMaterial({ color: 0xb5734a, roughness: 1, flatShading: true }), spots.length);
    var m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), tmp = new THREE.Vector3(), e = new THREE.Euler();
    spots.forEach(function (sp, i) {
      self.surfacePoint(sp.j, sp.lat, tmp); tmp.y = groundY + sp.s * 0.5;
      e.set(rng() * 0.5, sp.rot, 0); q.setFromEuler(e); sc.set(sp.s, sp.s * 0.7, sp.s);
      m4.compose(tmp, q, sc); rocks.setMatrixAt(i, m4);
    });
    rocks.castShadow = true; g.add(rocks);
    // cacti
    var trunkGeo = new THREE.CylinderGeometry(0.35, 0.45, 5, 7); trunkGeo.translate(0, 2.5, 0);
    var armGeo = mergeGeoms([new THREE.CylinderGeometry(0.22, 0.25, 1.4, 6).translate(1.0, 3.6, 0), new THREE.CylinderGeometry(0.22, 0.25, 1.2, 6).rotateZ(Math.PI / 2).translate(0.6, 2.9, 0), new THREE.CylinderGeometry(0.22, 0.25, 1.2, 6).translate(-0.9, 3.0, 0), new THREE.CylinderGeometry(0.22, 0.25, 1.0, 6).rotateZ(Math.PI / 2).translate(-0.5, 2.4, 0)]);
    var cs = [];
    for (j = 0; j < path.N; j += 12) { if (this.tunnelMask[j]) continue; if (rng() < 0.4) { var sd = rng() < 0.5 ? -1 : 1; cs.push({ j: j, lat: sd > 0 ? this.maxLat[j] + rng.range(5, 18) : this.minLat[j] - rng.range(5, 18), s: rng.range(0.7, 1.4), rot: rng() * 6.28 }); } }
    var cactMat = new THREE.MeshStandardMaterial({ color: 0x3f9a4a, roughness: 0.9 });
    var trunks = new THREE.InstancedMesh(trunkGeo, cactMat, cs.length), arms = new THREE.InstancedMesh(armGeo, cactMat, cs.length);
    cs.forEach(function (c, i) { self.surfacePoint(c.j, c.lat, tmp); tmp.y = groundY; e.set(0, c.rot, 0); q.setFromEuler(e); sc.set(c.s, c.s, c.s); m4.compose(tmp, q, sc); trunks.setMatrixAt(i, m4); arms.setMatrixAt(i, m4); });
    trunks.castShadow = arms.castShadow = true; g.add(trunks); g.add(arms);
    // low sun and haze
    var sun = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xffb060, transparent: true, fog: false }));
    sun.position.set(-1000, 170, -520); sun.scale.set(420, 420, 1); g.add(sun);
    var halo = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xff8a40, transparent: true, opacity: 0.35, fog: false, blending: THREE.AdditiveBlending }));
    halo.position.copy(sun.position); halo.scale.set(1100, 700, 1); g.add(halo);
    // circling birds (tiny sprites)
    for (var b = 0; b < 3; b++) {
      var bird = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0x2a1a10, transparent: true }));
      bird.scale.set(3, 1.2, 1); g.add(bird);
      (function (bs, ph) { self.animated.push(function (dt, t) { bs.position.set(Math.cos(t * 0.25 + ph) * 160, 70 + Math.sin(t * 0.5 + ph) * 8, Math.sin(t * 0.25 + ph) * 160); }); })(bird, b * 2.1);
    }
  };

  // ---------------------------------------------------------- frost (snowy peak)
  Track.prototype._sceneryFrost = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this;
    var snowTex = tex.snow(); snowTex.repeat.set(120, 120);
    var ground = new THREE.Mesh(new THREE.PlaneGeometry(3400, 3400), new THREE.MeshStandardMaterial({ map: snowTex, roughness: 0.95 }));
    ground.rotation.x = -Math.PI / 2; ground.position.y = groundY; ground.receiveShadow = true; g.add(ground);
    // distant mountains
    var mountMat = new THREE.MeshStandardMaterial({ color: 0xe6f0ff, roughness: 1, flatShading: true });
    var mounts = [];
    for (var i = 0; i < 18; i++) {
      var a = i / 18 * Math.PI * 2 + rng.range(-0.1, 0.1), r = envelope(a) + rng.range(200, 380);
      var rad = rng.range(120, 260), h = rng.range(160, 340);
      var cg = new THREE.ConeGeometry(rad, h, 7); cg.translate(Math.cos(a) * r, h / 2 + groundY, Math.sin(a) * r);
      mounts.push(cg);
    }
    g.add(new THREE.Mesh(mergeGeoms(mounts), mountMat));
    // pine forest
    var trunkGeo = new THREE.CylinderGeometry(0.25, 0.35, 3, 6); trunkGeo.translate(0, 1.5, 0);
    var folGeo = mergeGeoms([new THREE.ConeGeometry(2.4, 4.5, 8).translate(0, 4, 0), new THREE.ConeGeometry(1.8, 3.8, 8).translate(0, 6.4, 0), new THREE.ConeGeometry(1.1, 3.0, 8).translate(0, 8.4, 0)]);
    var spots = [], tries = 0;
    for (var j = 0; j < path.N; j += 6) {
      if (this.tunnelMask[j]) continue;
      if (rng() < 0.55) { var side = rng() < 0.5 ? -1 : 1; spots.push({ j: j, lat: side > 0 ? this.maxLat[j] + rng.range(5, 20) : this.minLat[j] - rng.range(5, 20), s: rng.range(0.7, 1.5), rot: rng() * 6.28, j2: true }); }
    }
    while (spots.length < 420 && tries++ < 6000) {
      var aa = rng() * Math.PI * 2, tr = envelope(aa), rr = rng() < 0.4 ? rng.range(0, Math.max(0, tr - 40)) : rng.range(tr + 25, tr + 200);
      var x = Math.cos(aa) * rr, z = Math.sin(aa) * rr;
      if (self.distToPath(x, z) < 22) continue;
      spots.push({ x: x, z: z, s: rng.range(0.7, 1.6), rot: rng() * 6.28 });
    }
    var trunks = new THREE.InstancedMesh(trunkGeo, new THREE.MeshStandardMaterial({ color: 0x5a3b22, roughness: 1 }), spots.length);
    var fol = new THREE.InstancedMesh(folGeo, new THREE.MeshStandardMaterial({ color: 0x2e7d4f, roughness: 0.9 }), spots.length);
    var m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), tmp = new THREE.Vector3(), e = new THREE.Euler();
    spots.forEach(function (sp, i) {
      if (sp.j2) { self.surfacePoint(sp.j, sp.lat, tmp); tmp.y = groundY; } else tmp.set(sp.x, groundY, sp.z);
      e.set(0, sp.rot, 0); q.setFromEuler(e); sc.set(sp.s, sp.s, sp.s);
      m4.compose(tmp, q, sc); trunks.setMatrixAt(i, m4); fol.setMatrixAt(i, m4);
    });
    trunks.castShadow = fol.castShadow = true; g.add(trunks); g.add(fol);
    // snow caps on the foliage (small white cones)
    var capGeo = new THREE.ConeGeometry(1.2, 1.2, 8); capGeo.translate(0, 9.3, 0);
    var caps = new THREE.InstancedMesh(capGeo, new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 1 }), spots.length);
    spots.forEach(function (sp, i) { fol.getMatrixAt(i, m4); caps.setMatrixAt(i, m4); });
    g.add(caps);
    // ice crystals glowing near the road
    var cryGeo = new THREE.OctahedronGeometry(1, 0); cryGeo.translate(0, 1.2, 0);
    var cspots = [];
    for (j = 0; j < path.N; j += 20) { if (this.tunnelMask[j]) continue; if (rng() < 0.6) { var sd = rng() < 0.5 ? -1 : 1; cspots.push({ j: j, lat: sd > 0 ? this.maxLat[j] + rng.range(3, 7) : this.minLat[j] - rng.range(3, 7), s: rng.range(0.8, 2.2), rot: rng() * 6.28 }); } }
    var cry = new THREE.InstancedMesh(cryGeo, new THREE.MeshStandardMaterial({ color: 0xbfe8ff, emissive: 0x66ccff, emissiveIntensity: 0.6, roughness: 0.1, metalness: 0.4, transparent: true, opacity: 0.85 }), cspots.length);
    cspots.forEach(function (c, i) { self.surfacePoint(c.j, c.lat, tmp); tmp.y = groundY; e.set(0, c.rot, 0.2); q.setFromEuler(e); sc.set(c.s, c.s * 1.6, c.s); m4.compose(tmp, q, sc); cry.setMatrixAt(i, m4); });
    g.add(cry);
    // pale sun + gentle snowfall sprites around the track centre
    var sun = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xffffff, transparent: true, fog: false }));
    sun.position.set(500, 700, -700); sun.scale.set(220, 220, 1); g.add(sun);
    var flakeMat = new THREE.SpriteMaterial({ map: tex.particle(), color: 0xffffff, transparent: true, opacity: 0.8, depthWrite: false });
    var flakes = [];
    for (var f = 0; f < 60; f++) { var fl = new THREE.Sprite(flakeMat); fl.scale.set(0.5, 0.5, 1); fl.position.set(rng.range(-500, 500), rng.range(5, 60), rng.range(-500, 500)); fl.userData.v = rng.range(1.5, 3.5); fl.userData.ph = rng() * 6.28; g.add(fl); flakes.push(fl); }
    this.animated.push(function (dt, t) {
      for (var i = 0; i < flakes.length; i++) { var fl = flakes[i]; fl.position.y -= fl.userData.v * dt; fl.position.x += Math.sin(t + fl.userData.ph) * dt * 2; if (fl.position.y < 0) fl.position.y = 60; }
    });
  };


  // ---------------------------------------------------------- seoul (spring, cherry blossoms, landmarks)
  Track.prototype._scenerySeoul = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this, N = path.N;
    var tmp = new THREE.Vector3(), m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), e = new THREE.Euler();
    function mesh(geo, mat, x, y, z) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); m.castShadow = true; m.receiveShadow = true; return m; }
    var stoneMat = new THREE.MeshStandardMaterial({ color: 0xa9a7a2, roughness: 1 });
    var graniteMat = new THREE.MeshStandardMaterial({ color: 0x8f8d88, roughness: 0.95 });
    var redMat = new THREE.MeshStandardMaterial({ color: 0xa8382c, roughness: 0.7 });
    var wallMat = new THREE.MeshStandardMaterial({ color: 0xf1e6cf, roughness: 0.9 });
    var danMat = new THREE.MeshStandardMaterial({ color: 0x1f8f73, roughness: 0.6 });
    var tileMat = new THREE.MeshStandardMaterial({ color: 0x3c4047, roughness: 0.9 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x2a2320, roughness: 0.9 });
    var woodMat = new THREE.MeshStandardMaterial({ color: 0x6b4a2b, roughness: 0.9 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0xd8ecf8, metalness: 0.2, roughness: 0.2, emissive: 0x224466, emissiveIntensity: 0.25 });

    // ---- ground + 광화문광장 pavement beside the start straight
    var grassTex = tex.grass(); grassTex.repeat.set(160, 160);
    var ground = mesh(new THREE.PlaneGeometry(3400, 3400).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ map: grassTex, roughness: 1 }), 0, groundY, 0);
    ground.castShadow = false; g.add(ground);
    function edge(latFn) { return function (j, out) { self.surfacePoint(j, latFn(j), out); }; }
    var pa = Math.round(0.925 * N), pb = N + Math.round(0.08 * N);
    var paveMat = new THREE.MeshStandardMaterial({ map: tex.concrete(), color: 0xd8d4cc, roughness: 1 });
    var plaza = new THREE.Mesh(mergeGeoms([
      stripGeom(path, edge(function (j) { return self.minLat[j] - 36; }), edge(function (j) { return self.minLat[j] - 1.6; }), 12, [pa, pb]),
      stripGeom(path, edge(function (j) { return self.maxLat[j] + 1.6; }), edge(function (j) { return self.maxLat[j] + 36; }), 12, [pa, pb])]), paveMat);
    plaza.position.y = 0.01; plaza.receiveShadow = true; g.add(plaza);

    // ---- 한강: river band along z, stone banks, 유람선
    var RX0 = 175, RX1 = 245, RC = (RX0 + RX1) / 2;
    var waterTex = tex.water(); waterTex.repeat.set(4, 120);
    var waterMat = new THREE.MeshStandardMaterial({ map: waterTex, color: 0x8ec4ea, roughness: 0.2, metalness: 0.3 });
    var water = mesh(new THREE.PlaneGeometry(RX1 - RX0, 3000).rotateX(-Math.PI / 2), waterMat, RC, groundY + 0.02, -60);
    water.castShadow = false; g.add(water);
    this.animated.push(function (dt, t) { waterTex.offset.y = t * 0.02; waterTex.offset.x = Math.sin(t * 0.3) * 0.01; });
    [RX0 - 1.5, RX1 + 1.5].forEach(function (x) { g.add(mesh(new THREE.BoxGeometry(3, 0.7, 3000), stoneMat, x, groundY + 0.35, -60)); });
    var boat = new THREE.Group();
    boat.add(mesh(new THREE.BoxGeometry(16, 2.2, 5), new THREE.MeshStandardMaterial({ color: 0xf4f6f8, roughness: 0.6 }), 0, 1.1, 0));
    boat.add(mesh(new THREE.BoxGeometry(9, 2.4, 4), new THREE.MeshStandardMaterial({ color: 0x3b7dd8, roughness: 0.6 }), -1, 3.4, 0));
    boat.add(mesh(new THREE.CylinderGeometry(0.5, 0.6, 2, 8), redMat, -4, 5.5, 0));
    boat.position.set(RC, groundY, -5); g.add(boat);
    this.animated.push(function (dt, t) { var ph = t * 0.06; boat.position.z = -5 + Math.sin(ph) * 150; boat.rotation.y = Math.cos(ph) >= 0 ? Math.PI / 2 : -Math.PI / 2; });
    // 대교 arches over the high bridge
    var arches = [];
    for (var fa = 0.185; fa < 0.25; fa += 0.016) { var ja = Math.round(fa * N); arches.push(archGeom(path, ja, ja + 2, function (jj) { return self.maxLat[jj] + 2.5; }, 12, 0)); }
    g.add(new THREE.Mesh(mergeGeoms(arches), new THREE.MeshStandardMaterial({ color: 0xf2f4f8, roughness: 0.5, side: THREE.DoubleSide })));

    // ---- traditional architecture helpers (hip roofs with flared eaves)
    function hipRoof(w, d, h) { var geo = new THREE.CylinderGeometry(Math.SQRT1_2 * 0.22, Math.SQRT1_2, 1, 4, 1); geo.rotateY(Math.PI / 4); geo.scale(w, h, d); return geo; }
    function eaveGeo(w, d) { var geo = new THREE.CylinderGeometry(Math.SQRT1_2, Math.SQRT1_2 * 0.9, 1, 4, 1); geo.rotateY(Math.PI / 4); geo.scale(w, 0.5, d); return geo; }
    var pillarGeo = new THREE.CylinderGeometry(0.45, 0.5, 1, 8);
    function pavilion(x, z, y0, W, D, tiers) {
      var grp = new THREE.Group(); grp.position.set(x, y0, z);
      var w = W, d = D, y = 0;
      for (var t = 0; t < tiers; t++) {
        var ph = t === 0 ? 5.5 : 4.5, last = t === tiers - 1;
        grp.add(mesh(new THREE.BoxGeometry(w + 1, 0.5, d + 1), stoneMat, 0, y + 0.25, 0));
        var nx = Math.max(2, Math.round(w / 6)), nz = Math.max(2, Math.round(d / 6));
        for (var i = 0; i <= nx; i++) for (var k = 0; k <= nz; k++) {
          if (i > 0 && i < nx && k > 0 && k < nz) continue;
          var pl = mesh(pillarGeo, redMat, -w / 2 + i * w / nx, y + 0.5 + ph / 2, -d / 2 + k * d / nz); pl.scale.y = ph; grp.add(pl);
        }
        grp.add(mesh(new THREE.BoxGeometry(w - 2.4, ph, d - 2.4), wallMat, 0, y + 0.5 + ph / 2, 0));
        grp.add(mesh(new THREE.BoxGeometry(w + 1.4, 0.8, d + 1.4), danMat, 0, y + 0.5 + ph + 0.4, 0));
        grp.add(mesh(eaveGeo(w + 6, d + 6), tileMat, 0, y + ph + 1.3, 0));
        var rh = last ? 4.5 : 2.8;
        grp.add(mesh(hipRoof(w + 5, d + 5, rh), tileMat, 0, y + ph + 1.55 + rh / 2, 0));
        if (last) grp.add(mesh(new THREE.BoxGeometry(w * 0.45, 0.6, 1.2), darkMat, 0, y + ph + 1.55 + rh, 0));
        y += ph + 1.55 + rh * 0.55; w *= 0.8; d *= 0.78;
      }
      return grp;
    }

    // ---- 광화문 (gate with three arches, two-tier pavilion), 해태, 광장, 경복궁 근정전, 북악산
    var GX = -46, GZ = 152;
    g.add(mesh(new THREE.BoxGeometry(46, 9, 14), graniteMat, GX, groundY + 4.5, GZ));
    g.add(mesh(new THREE.BoxGeometry(48, 0.8, 16), stoneMat, GX, groundY + 9.2, GZ));
    [[0, 6.5, 7.2], [-14, 5, 6], [14, 5, 6]].forEach(function (a) {
      var bh = a[2] - a[1] / 2;
      g.add(mesh(new THREE.BoxGeometry(a[1], bh, 1.2), darkMat, GX + a[0], groundY + bh / 2, GZ - 7.2));
      g.add(mesh(new THREE.CylinderGeometry(a[1] / 2, a[1] / 2, 1.2, 14).rotateX(Math.PI / 2), darkMat, GX + a[0], groundY + bh, GZ - 7.2));
    });
    g.add(pavilion(GX, GZ, groundY + 9.6, 38, 9, 2));
    var haetae = new THREE.MeshStandardMaterial({ color: 0xc9b98a, roughness: 0.9 });
    [-1, 1].forEach(function (sd) {
      var st = new THREE.Group(); st.position.set(GX + sd * 20, groundY, GZ - 16);
      st.add(mesh(new THREE.BoxGeometry(2.6, 1.2, 3.4), stoneMat, 0, 0.6, 0));
      st.add(mesh(new THREE.BoxGeometry(2, 1.6, 2.6), haetae, 0, 2.0, 0));
      st.add(mesh(new THREE.SphereGeometry(1.0, 10, 8), haetae, 0, 3.3, 0.9));
      g.add(st);
    });
    g.add(mesh(new THREE.BoxGeometry(64, 0.16, 44), new THREE.MeshStandardMaterial({ color: 0xcfcbc2, roughness: 1 }), GX, groundY + 0.08, GZ - 38));
    g.add(mesh(new THREE.BoxGeometry(58, 3, 42), stoneMat, GX, groundY + 1.5, GZ + 70));
    g.add(pavilion(GX, GZ + 70, groundY + 3, 40, 26, 2));
    var mtMat = new THREE.MeshStandardMaterial({ color: 0x4f7f4a, roughness: 1, flatShading: true });
    [[-70, 540, 200, 170], [140, 590, 170, 120], [-300, 500, 180, 130], [90, -820, 240, 150], [460, 600, 180, 110], [-520, -320, 200, 120]].forEach(function (m) { g.add(mesh(new THREE.ConeGeometry(m[2], m[3], 7), mtMat, m[0], groundY + m[3] / 2, m[1])); });
    // 태극기 along the plaza
    var flagMat = new THREE.MeshBasicMaterial({ map: tex.taegukgi(), side: THREE.DoubleSide });
    var poleMat = new THREE.MeshStandardMaterial({ color: 0xdddddd, metalness: 0.6, roughness: 0.4 });
    var flagGeo = new THREE.PlaneGeometry(4, 2.7); flagGeo.translate(2, 0, 0);
    var flags = [];
    for (var fi = 0; fi < 12; fi++) {
      var fx = fi < 6 ? -30 : 30, fz = 10 + (fi % 6) * 18;
      g.add(mesh(new THREE.CylinderGeometry(0.12, 0.16, 12, 6), poleMat, fx, groundY + 6, fz));
      var fl = mesh(flagGeo, flagMat, fx, groundY + 10.5, fz); fl.castShadow = false; g.add(fl); flags.push(fl);
    }
    this.animated.push(function (dt, t) { for (var i = 0; i < flags.length; i++) flags[i].rotation.y = 0.3 + Math.sin(t * 2.2 + i) * 0.3; });

    // ---- 63빌딩, 롯데월드타워 + 석촌호수, DDP
    g.add(mesh(new THREE.BoxGeometry(22, 96, 14), new THREE.MeshStandardMaterial({ color: 0xe6b640, metalness: 0.35, roughness: 0.3, emissive: 0x5a3a08, emissiveIntensity: 0.35 }), 292, groundY + 48, 232));
    var lotteTex = tex.windowsDay(3).clone(); lotteTex.repeat.set(10, 70); lotteTex.needsUpdate = true;
    g.add(mesh(new THREE.CylinderGeometry(3.5, 14, 200, 8), new THREE.MeshStandardMaterial({ map: lotteTex, color: 0xcfe4f5, roughness: 0.4, metalness: 0.1 }), 445, groundY + 100, 40));
    g.add(mesh(new THREE.ConeGeometry(3.5, 26, 8), glassMat, 445, groundY + 213, 40));
    g.add(mesh(new THREE.BoxGeometry(30, 9, 22), glassMat, 445, groundY + 4.5, 40));
    var lake = mesh(new THREE.CircleGeometry(36, 32).rotateX(-Math.PI / 2), waterMat, 452, groundY + 0.02, -52); lake.castShadow = false; g.add(lake);
    var ddp = mesh(new THREE.SphereGeometry(26, 24, 14), new THREE.MeshStandardMaterial({ color: 0xd8dbe0, metalness: 0.3, roughness: 0.35 }), 100, groundY + 4, 262); ddp.scale.set(1.7, 0.42, 1); g.add(ddp);

    // ---- 남산 + N서울타워 (inside the tower loop)
    var HX = 84.6, HZ = -282;
    g.add(mesh(new THREE.CylinderGeometry(9, 27, 17, 24), new THREE.MeshStandardMaterial({ color: 0x3f7d3a, roughness: 1 }), HX, groundY + 8.5, HZ));
    g.add(mesh(new THREE.CylinderGeometry(24, 48, 8, 24), new THREE.MeshStandardMaterial({ color: 0x4a8a44, roughness: 1 }), HX, groundY + 4, HZ));
    var twMat = new THREE.MeshStandardMaterial({ color: 0xe3e6ea, roughness: 0.5, metalness: 0.2 });
    var ty = groundY + 17;
    g.add(mesh(new THREE.CylinderGeometry(6, 8, 3, 12), stoneMat, HX, ty + 1.5, HZ));
    g.add(mesh(new THREE.CylinderGeometry(2.2, 3.4, 58, 12), twMat, HX, ty + 32, HZ));
    g.add(mesh(new THREE.CylinderGeometry(7.5, 5.5, 7, 16), twMat, HX, ty + 64.5, HZ));
    g.add(mesh(new THREE.CylinderGeometry(7.7, 7.7, 2.2, 16), glassMat, HX, ty + 66, HZ));
    g.add(mesh(new THREE.CylinderGeometry(0.5, 1.0, 42, 8), new THREE.MeshStandardMaterial({ color: 0xd8dde3, roughness: 0.5 }), HX, ty + 89, HZ));
    g.add(mesh(new THREE.SphereGeometry(0.9, 8, 6), new THREE.MeshStandardMaterial({ color: 0xff3030, emissive: 0xff2020, emissiveIntensity: 1.5 }), HX, ty + 110, HZ));

    // ---- 북촌 한옥마을 (south-west quarter, below 남산)
    var walls = [], roofs = [], bases = [], hspots = [], tries = 0;
    while (hspots.length < 75 && tries++ < 6000) {
      var hx = rng.range(-60, 160), hz = rng.range(-345, -105);
      if (self.distToPath(hx, hz) < 21 || Math.hypot(hx - HX, hz - HZ) < 48) continue;
      var ok = true; for (var hi = 0; hi < hspots.length; hi++) if (Math.hypot(hspots[hi][0] - hx, hspots[hi][1] - hz) < 17) { ok = false; break; }
      if (!ok) continue;
      hspots.push([hx, hz]);
      var w = rng.range(8, 14), d = rng.range(6, 10), rot = rng.pick([0, Math.PI / 2, 0.3, -0.3]);
      var wg = new THREE.BoxGeometry(w, 3.2, d); wg.rotateY(rot); wg.translate(hx, groundY + 1.6, hz); walls.push(wg);
      var bg = new THREE.BoxGeometry(w + 0.5, 0.9, d + 0.5); bg.rotateY(rot); bg.translate(hx, groundY + 0.45, hz); bases.push(bg);
      var band = new THREE.BoxGeometry(w + 0.2, 0.35, d + 0.2); band.rotateY(rot); band.translate(hx, groundY + 3.05, hz); bases.push(band);
      var eg = eaveGeo(w + 3.5, d + 3.5); eg.rotateY(rot); eg.translate(hx, groundY + 3.45, hz); roofs.push(eg);
      var rg = hipRoof(w + 3, d + 3, 2.6); rg.rotateY(rot); rg.translate(hx, groundY + 5.0, hz); roofs.push(rg);
    }
    if (walls.length) { g.add(mesh(mergeGeoms(walls), wallMat, 0, 0, 0)); g.add(mesh(mergeGeoms(bases), woodMat, 0, 0, 0)); g.add(mesh(mergeGeoms(roofs), tileMat, 0, 0, 0)); }

    // ---- city blocks: 잠실 (east of the river), 종로 (north-west), 명동 (inside the loop)
    var byVar = [[], [], [], [], []], pastel = [0xdfe6ee, 0xe8e0d6, 0xd6e4f0, 0xf0e6e0, 0xe2e8e2];
    function block(x, z, w, d, h) { var geo = boxWithUV(w, h, d, 6); geo.translate(x, h / 2 + groundY, z); byVar[rng.int(0, 4)].push(geo); }
    function scatter(count, xr, zr, wr, hr, margin, reject) {
      var c = 0, tr = 0;
      while (c < count && tr++ < 6000) {
        var x = rng.range(xr[0], xr[1]), z = rng.range(zr[0], zr[1]), w = rng.range(wr[0], wr[1]), d = rng.range(wr[0], wr[1]), h = rng.range(hr[0], hr[1]);
        if (self.distToPath(x, z) < margin + Math.max(w, d) * 0.6) continue;
        if (x > RX0 - 16 && x < RX1 + 16) continue;
        if (reject && reject(x, z)) continue;
        block(x, z, w, d, h); c++;
      }
    }
    scatter(70, [262, 540], [-240, 260], [12, 24], [22, 70], 30, function (x, z) { return Math.hypot(x - 445, z - 40) < 58 || Math.hypot(x - 452, z + 52) < 58 || Math.hypot(x - 292, z - 232) < 32; });
    scatter(45, [-280, 160], [120, 340], [10, 22], [10, 36], 28, function (x, z) { return Math.hypot(x - GX, z - GZ) < 72 || Math.hypot(x - GX, z - GZ - 70) < 62 || Math.hypot(x - 100, z - 262) < 62; });
    scatter(35, [30, 160], [-95, 105], [10, 20], [12, 40], 40, null);
    scatter(30, [-300, -50], [-200, 100], [10, 22], [8, 30], 30, null);
    byVar.forEach(function (list, vi) { if (!list.length) return; var mm = new THREE.Mesh(mergeGeoms(list), new THREE.MeshStandardMaterial({ map: tex.windowsDay(vi), color: pastel[vi], roughness: 0.8 })); mm.castShadow = true; mm.receiveShadow = true; g.add(mm); });

    // ---- 벚꽃: instanced trees with per-instance pink, falling petals
    var trunkGeo = new THREE.CylinderGeometry(0.28, 0.42, 3.6, 6); trunkGeo.translate(0, 1.8, 0);
    var canopyGeo = mergeGeoms([new THREE.IcosahedronGeometry(2.6, 1).translate(0, 5.2, 0), new THREE.IcosahedronGeometry(2.0, 1).translate(1.5, 4.3, 0.7), new THREE.IcosahedronGeometry(1.9, 1).translate(-1.3, 4.5, -0.9), new THREE.IcosahedronGeometry(1.6, 1).translate(0.3, 6.6, -0.4)]);
    var spots = [];
    function treeAt(j, lat, s) { self.surfacePoint(j, lat, tmp); spots.push({ x: tmp.x, y: Math.max(groundY, tmp.y - 1.4), z: tmp.z, s: s, rot: rng() * 6.28, c: rng() }); }
    for (var j = Math.round(0.806 * N); j < Math.round(0.86 * N); j += 3) { treeAt(j, self.maxLat[j] + rng.range(2.5, 4), rng.range(1.0, 1.4)); treeAt(j, self.minLat[j] - rng.range(2.5, 4), rng.range(1.0, 1.4)); }
    for (j = Math.round(0.862 * N); j < N + Math.round(0.078 * N); j += 6) { var jj = j % N; if (rng() < 0.8) treeAt(jj, self.maxLat[jj] + rng.range(5, 14), rng.range(0.8, 1.3)); if (rng() < 0.8) treeAt(jj, self.minLat[jj] - rng.range(5, 14), rng.range(0.8, 1.3)); }
    for (j = Math.round(0.414 * N); j < Math.round(0.535 * N); j += 5) { if (rng() < 0.7) treeAt(j, self.maxLat[j] + rng.range(4, 12), rng.range(0.8, 1.3)); }
    for (j = Math.round(0.674 * N); j < Math.round(0.706 * N); j += 5) { if (rng() < 0.7) treeAt(j, self.minLat[j] - rng.range(4, 10), rng.range(0.8, 1.2)); }
    tries = 0;
    while (spots.length < 340 && tries++ < 4000) {
      var tx = rng() < 0.5 ? rng.range(RX0 - 30, RX0 - 6) : rng.range(RX1 + 6, RX1 + 30), tz = rng.range(-180, 300);
      if (self.distToPath(tx, tz) < 16) continue;
      spots.push({ x: tx, y: groundY, z: tz, s: rng.range(0.7, 1.2), rot: rng() * 6.28, c: rng() });
    }
    var trunks = new THREE.InstancedMesh(trunkGeo, new THREE.MeshStandardMaterial({ color: 0x5c4030, roughness: 1 }), spots.length);
    var canopy = new THREE.InstancedMesh(canopyGeo, new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.9 }), spots.length);
    var col = new THREE.Color(), pinkA = new THREE.Color(0xffc4dc), pinkB = new THREE.Color(0xff9ec8);
    spots.forEach(function (sp, i) {
      tmp.set(sp.x, sp.y, sp.z); e.set(0, sp.rot, 0); q.setFromEuler(e); sc.set(sp.s, sp.s, sp.s);
      m4.compose(tmp, q, sc); trunks.setMatrixAt(i, m4); canopy.setMatrixAt(i, m4);
      canopy.setColorAt(i, col.copy(pinkA).lerp(pinkB, sp.c));
    });
    trunks.castShadow = canopy.castShadow = true; g.add(trunks); g.add(canopy);
    var petalMat = new THREE.SpriteMaterial({ map: tex.particle(), color: 0xffc0d8, transparent: true, opacity: 0.85, depthWrite: false });
    var petals = [];
    for (var pi = 0; pi < 150; pi++) { var pt = new THREE.Sprite(petalMat); pt.scale.set(0.45, 0.35, 1); pt.position.set(rng.range(-120, 500), rng.range(2, 45), rng.range(-380, 280)); pt.userData.v = rng.range(0.8, 1.8); pt.userData.ph = rng() * 6.28; g.add(pt); petals.push(pt); }
    this.animated.push(function (dt, t) {
      for (var i = 0; i < petals.length; i++) { var pt = petals[i]; pt.position.y -= pt.userData.v * dt; pt.position.x += Math.sin(t * 1.3 + pt.userData.ph) * dt * 2.5; pt.position.z += Math.cos(t * 0.9 + pt.userData.ph) * dt * 1.5; if (pt.position.y < 0) pt.position.y = 45; }
    });

    // ---- 한글 간판 along the 둔치 straight
    [['서울', '#ffffff', '#cd2e3a'], ['어서오세요', '#ffffff', '#0047a0'], ['한강공원', '#112233', '#ffe066'], ['NITRO RUSH', '#ffffff', '#ff2f6d']].forEach(function (sg, i) {
      var j = Math.round((0.42 + i * 0.03) * N), bb = new THREE.Group();
      bb.add(mesh(new THREE.BoxGeometry(14, 4.5, 0.5), new THREE.MeshBasicMaterial({ map: tex.label('kr' + i, sg[0], sg[1], sg[2]) }), 0, 8, 0));
      bb.add(mesh(new THREE.BoxGeometry(0.7, 6, 0.7), new THREE.MeshStandardMaterial({ color: 0x555a66 }), 0, 3, 0));
      self.surfacePoint(j, self.minLat[j] - 9, tmp); bb.position.set(tmp.x, groundY, tmp.z); bb.lookAt(path.P[j].x, groundY, path.P[j].z); g.add(bb);
    });

    // ---- sky: clouds + sun
    var cloudMat = new THREE.SpriteMaterial({ map: tex.smoke(), color: 0xffffff, opacity: 0.95, transparent: true, depthWrite: false, fog: false });
    for (var c = 0; c < 24; c++) {
      var a2 = rng() * Math.PI * 2, r2 = rng.range(300, 900), cl = new THREE.Sprite(cloudMat);
      cl.position.set(190 + Math.cos(a2) * r2, rng.range(110, 190), -70 + Math.sin(a2) * r2);
      var s2 = rng.range(50, 120); cl.scale.set(s2 * 1.6, s2 * 0.8, 1); g.add(cl);
    }
    var sun = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xfff1b0, transparent: true, fog: false }));
    sun.position.set(-500, 520, -800); sun.scale.set(240, 240, 1); g.add(sun);
  };


  // ------------------------------------------------------------- 제주영어교육도시
  Track.prototype._sceneryJeju = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this, N = path.N, def = this.def;
    var tmp = new THREE.Vector3(), m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), e = new THREE.Euler();
    function mesh(geo, mat, x, y, z) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); m.castShadow = true; m.receiveShadow = true; return m; }
    var poly = def.pts.map(function (p) { return [p[0], p[1]]; });
    function inside(x, z) {   // point in the loop polygon (map interior = city blocks)
      var c = false; for (var i = 0, j = poly.length - 1; i < poly.length; j = i++) {
        var xi = poly[i][0], zi = poly[i][1], xj = poly[j][0], zj = poly[j][1];
        if ((zi > z) !== (zj > z) && x < (xj - xi) * (z - zi) / (zj - zi) + xi) c = !c;
      } return c;
    }
    var basaltMat = new THREE.MeshStandardMaterial({ color: 0x3a3b3d, roughness: 1, flatShading: true });
    var stoneMat = new THREE.MeshStandardMaterial({ color: 0x8e9094, roughness: 1 });
    var whiteMat = new THREE.MeshStandardMaterial({ color: 0xf2f3f0, roughness: 0.85 });
    var brickMat = new THREE.MeshStandardMaterial({ map: tex.windowsDay(1), color: 0xd8b8a0, roughness: 0.9 });
    var schoolMat = new THREE.MeshStandardMaterial({ map: tex.windowsDay(2), color: 0xe4e8ea, roughness: 0.85 });
    var aptMat = new THREE.MeshStandardMaterial({ map: tex.windowsDay(0), color: 0xe9e2d8, roughness: 0.85 });
    var aptMat2 = new THREE.MeshStandardMaterial({ map: tex.windowsDay(4), color: 0xdfe3ea, roughness: 0.85 });
    var shopMats = [1, 3, 4, 0, 2].map(function (v, i) { return new THREE.MeshStandardMaterial({ map: tex.windowsDay(v), color: [0xf4e0c8, 0xd9e6f2, 0xf2d6d6, 0xe3efd8, 0xf7efc8][i], roughness: 0.85 }); });
    var roofRed = new THREE.MeshStandardMaterial({ color: 0xa8402c, roughness: 0.9 }), roofBlue = new THREE.MeshStandardMaterial({ color: 0x2f5f9e, roughness: 0.9 });
    var fieldMat = new THREE.MeshStandardMaterial({ color: 0x3c9a4a, roughness: 1 }), lineMat = new THREE.MeshBasicMaterial({ color: 0xffffff });
    var asphaltMat = new THREE.MeshStandardMaterial({ color: 0x4b4e56, roughness: 1 });
    var poleMat = new THREE.MeshStandardMaterial({ color: 0x555a66, roughness: 0.6 });

    // ---- ground
    var grassTex = tex.grass(); grassTex.repeat.set(160, 160);
    var ground = mesh(new THREE.PlaneGeometry(3400, 3400).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ map: grassTex, color: 0xd6ead0, roughness: 1 }), 0, groundY, 0);
    ground.castShadow = false; g.add(ground);
    // block streets inside the loop (지도의 가로 골목 + 가운데 세로길), flat dark strips
    function span(z) { var lo = 1e9, hi = -1e9; for (var x = -120; x <= 220; x += 2) if (inside(x, z) && self.distToPath(x, z) > 12) { lo = Math.min(lo, x); hi = Math.max(hi, x); } return hi > lo ? [lo, hi] : null; }
    var streetsZ = [-345, -254, -189, -117, -39, 26, 104], streets = [];
    streetsZ.forEach(function (z) { var sp = span(z); if (sp) { var geo = new THREE.PlaneGeometry(sp[1] - sp[0], 7).rotateX(-Math.PI / 2); geo.translate((sp[0] + sp[1]) / 2, groundY + 0.03, z); streets.push(geo); } });
    for (var mz = -345; mz < 104; mz += 4) { if (inside(53, mz) && self.distToPath(53, mz) > 12) { var mg = new THREE.PlaneGeometry(7, 4.2).rotateX(-Math.PI / 2); mg.translate(53, groundY + 0.03, mz + 2); streets.push(mg); } }
    var st = new THREE.Mesh(mergeGeoms(streets), asphaltMat); st.receiveShadow = true; g.add(st);

    // ---- signboards beside the track
    var signs = [];
    function sign(key, text, x, z, fx, fz, w, fg, bg, h) {
      w = w || 14; h = h || w * 0.28;
      var bb = new THREE.Group();
      bb.add(mesh(new THREE.BoxGeometry(w, h, 0.4), new THREE.MeshBasicMaterial({ map: tex.label('jj' + key, text, fg, bg) }), 0, 6.5 + h / 2, 0));
      bb.add(mesh(new THREE.BoxGeometry(w + 0.4, h + 0.4, 0.3), new THREE.MeshStandardMaterial({ color: 0x2a2d33 }), 0, 6.5 + h / 2, -0.1));
      [-w * 0.35, w * 0.35].forEach(function (px) { bb.add(mesh(new THREE.BoxGeometry(0.5, 6.6, 0.5), poleMat, px, 3.3, -0.1)); });
      bb.position.set(x, groundY, z); bb.lookAt(fx, groundY, fz); g.add(bb); signs.push(bb); return bb;
    }
    function trackSign(key, text, f, lat, w, fg, bg) {   // beside the road at lap fraction f, angled toward oncoming karts
      var j = Math.round(f * N) % N, jb = (j - 30 + N) % N; self.surfacePoint(j, lat, tmp);
      return sign(key, text, tmp.x, tmp.z, path.P[jb].x, path.P[jb].z, w, fg, bg);
    }
    // outside = +R (maxLat side), inside = -R (minLat side) on this clockwise loop
    function out(f, d) { var j = Math.round(f * N) % N; return self.maxLat[j] + d; }
    function inn(f, d) { var j = Math.round(f * N) % N; return self.minLat[j] - d; }
    trackSign('ino', '이노에듀타운', 0.975, inn(0.975, 8), 16, '#ffffff', '#1f6f4a');
    trackSign('ino2', '이노에듀타운 · 맛집거리', 0.03, inn(0.03, 8), 16, '#ffffff', '#c9482c');
    trackSign('sam', '삼정 G.edu 아파트', 0.06, out(0.06, 8), 16, '#ffffff', '#2c5aa0');
    trackSign('han', '한신더휴', 0.085, inn(0.085, 8), 14, '#ffffff', '#1a3d6d');
    trackSign('han2', '한신더휴', 0.665, inn(0.665, 8), 14, '#ffffff', '#1a3d6d');
    trackSign('gw', '제주곶자왈도립공원 →', 0.955, out(0.955, 8), 18, '#ffffff', '#2f6b2f');
    trackSign('raon', '← 라온 프라이빗타운', 0.925, out(0.925, 8), 18, '#3b2a1a', '#f2d8a8');
    trackSign('bha', 'BHA  Branksome Hall Asia', 0.232, inn(0.232, 8), 18, '#ffffff', '#7a1f2e');
    trackSign('kis', 'KISJ  한국국제학교', 0.285, inn(0.285, 8), 18, '#ffffff', '#123b7a');
    trackSign('kis2', 'KISJ  한국국제학교', 0.47, inn(0.47, 8), 18, '#ffffff', '#123b7a');
    trackSign('sja', 'SJA  St. Johnsbury Academy', 0.606, inn(0.606, 8), 20, '#ffffff', '#1c6b3a');
    trackSign('nor', '노리매공원', 0.595, out(0.595, 8), 14, '#ffffff', '#5a7a2a');
    trackSign('119', '영어교육도시 119센터', 0.91, out(0.91, 8), 16, '#ffffff', '#c8201a');
    trackSign('nr', 'NITRO RUSH · JEJU', 0.15, out(0.15, 8), 16, '#ffffff', '#ff2f6d');

    function nearSign(x, z, r) { for (var i = 0; i < signs.length; i++) { var sp = signs[i].position; if (Math.hypot(sp.x - x, sp.z - z) < r) return true; } return false; }

    // ---- buildings
    var blocks = [];   // rects to keep trees out: [x0,z0,x1,z1]
    var skipped = 0;
    function box(w, h, d, x, z, mat, rotY) {
      if (self.distToPath(x, z) < Math.max(w, d) * 0.5 + 13) { skipped++; return new THREE.Group(); }   // never let a building sit on the road
      var geo = boxWithUV(w, h, d, 6); if (rotY) geo.rotateY(rotY); var m = new THREE.Mesh(geo, mat); m.position.set(x, groundY + h / 2, z); m.castShadow = true; m.receiveShadow = true; g.add(m); blocks.push([x - w / 2 - 4, z - d / 2 - 4, x + w / 2 + 4, z + d / 2 + 4]); return m; }
    function field(x, z, w, d, rotY) {
      var grp = new THREE.Group(); grp.position.set(x, groundY + 0.04, z); grp.rotation.y = rotY || 0;
      var f = mesh(new THREE.PlaneGeometry(w, d).rotateX(-Math.PI / 2), fieldMat, 0, 0, 0); f.castShadow = false; grp.add(f);
      var tr = mesh(new THREE.RingGeometry(Math.min(w, d) * 0.42, Math.min(w, d) * 0.5, 24).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xc8553a, roughness: 1 }), 0, 0.01, 0); tr.castShadow = false; grp.add(tr);
      [[w * 0.9, 0.5, 0, -d * 0.38], [w * 0.9, 0.5, 0, d * 0.38], [0.5, d * 0.76, -w * 0.45, 0], [0.5, d * 0.76, w * 0.45, 0], [0.5, d * 0.76, 0, 0]].forEach(function (l) { var ln = mesh(new THREE.PlaneGeometry(l[0], l[1]).rotateX(-Math.PI / 2), lineMat, l[2], 0.02, l[3]); ln.castShadow = false; grp.add(ln); });
      g.add(grp); blocks.push([x - w / 2 - 3, z - d / 2 - 3, x + w / 2 + 3, z + d / 2 + 3]);
    }
    function tower(x, z, w, h, d, mat) { box(w, h, d, x, z, mat); box(w * 0.5, 2.2, d * 0.5, x, z, stoneMat).position.y = groundY + h + 1.1; }
    function shopRow(x0, z0, z1, faceDir, count) {   // faceDir: -1 = signs face -x (toward spine), +1 = face +x
      var names = ['흑돼지 구이', '고기국수', '치킨 & 맥주', '피자', '카페 라떼', '떡볶이 분식', '초밥', '갈치조림', '한라봉 주스', '편의점', '노래방', '돈까스', '파스타', '빙수', '족발·보쌈', '중국집', '햄버거', '해물탕'];
      var cols = [['#ffffff', '#c9482c'], ['#111111', '#ffd54f'], ['#ffffff', '#2c5aa0'], ['#ffffff', '#1f8a5a'], ['#ffffff', '#8a3fb5'], ['#111111', '#ff9aa2'], ['#ffffff', '#e55b2b'], ['#ffffff', '#00897b']];
      var step = (z1 - z0) / count;
      for (var i = 0; i < count; i++) {
        var z = z0 + step * (i + 0.5), w = 12, d = Math.min(14, step - 3), h = rng.range(8, 14);
        box(w, h, d, x0, z, shopMats[i % shopMats.length]);
        var nm = names[(i * 7 + Math.round(Math.abs(z0))) % names.length], c = cols[i % cols.length];
        var bw = 8, bh = 1.8, bd = mesh(new THREE.BoxGeometry(bw, bh, 0.3), new THREE.MeshBasicMaterial({ map: tex.label('shop' + x0 + '_' + i + '_' + z0, nm, c[0], c[1]) }), x0 + faceDir * (w / 2 + 0.2), groundY + 3.4, z);
        bd.rotation.y = faceDir > 0 ? Math.PI / 2 : -Math.PI / 2; g.add(bd);
        if (rng() < 0.5) { var bd2 = mesh(new THREE.BoxGeometry(bw, bh, 0.3), new THREE.MeshBasicMaterial({ map: tex.label('shop' + x0 + '_' + i + '_' + z0, nm, c[0], c[1]) }), x0 + faceDir * (w / 2 + 0.2), groundY + 6.4, z); bd2.rotation.y = bd.rotation.y; g.add(bd2); }
      }
    }
    // 이노에듀타운 (남쪽 블록 안쪽, 음식점 거리)
    shopRow(24, -50, 115, -1, 9);     // 스파인 옆, 간판은 도로(서쪽)를 향함
    shopRow(42, -50, 115, 1, 9);      // 가운데 세로길 서쪽
    shopRow(66, -50, 30, -1, 5);      // 가운데 세로길 동쪽
    shopRow(88, -60, 0, 1, 3);        // 동쪽 대로 옆
    box(36, 10, 22, 30, 95, shopMats[2]); // 이노에듀타운 센터
    var inoBoard = mesh(new THREE.BoxGeometry(30, 4.5, 0.5), new THREE.MeshBasicMaterial({ map: tex.label('inoRoof', '이노에듀타운', '#ffffff', '#1f6f4a') }), 30, groundY + 12.8, 95); g.add(inoBoard);
    // 한신더휴 (안쪽 중간 블록)
    [[30, -95], [60, -100], [92, -95], [105, -122], [45, -140], [80, -150], [110, -162]].forEach(function (p, i) { tower(p[0], p[1], 15, rng.range(26, 40), 12, i % 2 ? aptMat2 : aptMat); });
    // SJA (안쪽, 동쪽 대로 쪽)
    box(46, 14, 18, 110, -286, schoolMat); box(30, 10, 16, 60, -284, brickMat); box(22, 18, 22, 128, -235, schoolMat);
    field(70, -230, 60, 38, 0);
    var sjaBoard = mesh(new THREE.BoxGeometry(24, 3.6, 0.5), new THREE.MeshBasicMaterial({ map: tex.label('sjaRoof', 'SJA JEJU', '#ffffff', '#1c6b3a') }), 110, groundY + 16.5, -286); g.add(sjaBoard);
    // 공원 블록 (안쪽 -345..-300): 잔디 + 나무만
    // BHA (안쪽 서쪽, 스파인 옆)
    box(40, 12, 16, 12, -380, schoolMat); box(24, 16, 20, 42, -406, brickMat); box(18, 9, 30, 8, -428, schoolMat);
    var bhaBoard = mesh(new THREE.BoxGeometry(20, 3.4, 0.5), new THREE.MeshBasicMaterial({ map: tex.label('bhaRoof', 'BHA', '#ffffff', '#7a1f2e') }), 12, groundY + 14.4, -380); g.add(bhaBoard);
    // KIS (북쪽 블록: 고등학교 동쪽, 중학교 서쪽)
    box(44, 16, 20, 70, -445, schoolMat); box(26, 12, 26, 62, -485, brickMat); box(36, 11, 16, 8, -500, schoolMat); box(20, 14, 20, -18, -470, brickMat);
    field(8, -548, 56, 36, -0.25);
    var kisBoard = mesh(new THREE.BoxGeometry(26, 3.6, 0.5), new THREE.MeshBasicMaterial({ map: tex.label('kisRoof', 'KIS JEJU', '#ffffff', '#123b7a') }), 70, groundY + 18.6, -445); g.add(kisBoard);
    if (window.__autotest) console.log('[AUTOTEST] jeju buildings skipped(too close to road)=' + skipped);
    // 삼정 G.edu 아파트 (바깥 서쪽)
    [[-95, -20], [-120, -40], [-145, -62], [-100, -70], [-128, -95]].forEach(function (p, i) { tower(p[0], p[1], 14, rng.range(22, 30), 12, i % 2 ? aptMat : aptMat2); });
    // 꿈에그린 (바깥 남서쪽)
    [[-150, 140], [-178, 160], [-205, 150]].forEach(function (p) { tower(p[0], p[1], 14, rng.range(24, 32), 12, aptMat2); });
    // 라온 프라이빗타운 (바깥 서쪽 타운하우스)
    var houses = [], roofsR = [], roofsB = [];
    for (var hi = 0; hi < 26; hi++) {
      var hx = -250 + (hi % 6) * 18 + rng.range(-3, 3), hz = 20 + Math.floor(hi / 6) * 20 + rng.range(-3, 3);
      var hg = new THREE.BoxGeometry(10, 5.5, 8); hg.translate(hx, groundY + 2.75, hz); houses.push(hg);
      var rg = new THREE.CylinderGeometry(0, 7.4, 3, 4, 1); rg.rotateY(Math.PI / 4); rg.scale(1, 1, 0.8); rg.translate(hx, groundY + 7, hz); (hi % 2 ? roofsR : roofsB).push(rg);
      blocks.push([hx - 8, hz - 7, hx + 8, hz + 7]);
    }
    g.add(mesh(mergeGeoms(houses), whiteMat, 0, 0, 0)); g.add(mesh(mergeGeoms(roofsR), roofRed, 0, 0, 0)); g.add(mesh(mergeGeoms(roofsB), roofBlue, 0, 0, 0));
    // 119센터 (남쪽 코너 바깥)
    box(16, 7, 12, -32, 146, whiteMat); box(4, 12, 4, -24, 152, new THREE.MeshStandardMaterial({ color: 0xc8201a, roughness: 0.7 }));
    // 노리매공원 (동쪽 바깥): 연못 + 정자
    var waterTex = tex.water(); waterTex.repeat.set(3, 3);
    var pond = mesh(new THREE.CircleGeometry(22, 24).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ map: waterTex, color: 0x8ec4ea, roughness: 0.2, metalness: 0.3 }), 235, groundY + 0.03, -267); pond.castShadow = false; g.add(pond);
    this.animated.push(function (dt, t) { waterTex.offset.x = t * 0.02; });
    blocks.push([205, -297, 265, -237]);
    box(6, 4, 6, 262, -240, new THREE.MeshStandardMaterial({ color: 0x6b4a2b, roughness: 0.9 })); g.add(mesh(new THREE.CylinderGeometry(0, 6, 2.5, 4, 1).rotateY(Math.PI / 4), roofRed, 262, groundY + 5.2, -240));
    // 유채꽃밭 (동쪽 바깥)
    var canola = mesh(new THREE.PlaneGeometry(90, 60).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xf2d43a, roughness: 1 }), 245, groundY + 0.02, -80); canola.castShadow = false; g.add(canola);
    blocks.push([195, -115, 295, -45]);
    // 감귤밭 (서쪽 바깥, 스파인 북쪽 구간 옆)
    var orchard = [], oranges = [];
    for (var ox = -130; ox <= -60; ox += 9) for (var oz = -330; oz <= -210; oz += 9) {
      if (self.distToPath(ox, oz) < 16) continue;
      var og = new THREE.SphereGeometry(2.6, 7, 5); og.translate(ox + rng.range(-1, 1), groundY + 2.4, oz + rng.range(-1, 1)); orchard.push(og);
      for (var k = 0; k < 4; k++) { var fr = new THREE.SphereGeometry(0.28, 5, 4); var a = rng() * 6.28, b = rng() * 2; fr.translate(ox + Math.cos(a) * 2.3, groundY + 2.4 + Math.sin(b) * 1.6, oz + Math.sin(a) * 2.3); oranges.push(fr); }
    }
    g.add(mesh(mergeGeoms(orchard), new THREE.MeshStandardMaterial({ color: 0x2f7a35, roughness: 1 }), 0, 0, 0));
    g.add(mesh(mergeGeoms(oranges), new THREE.MeshStandardMaterial({ color: 0xff8c1a, roughness: 0.6 }), 0, 0, 0));
    blocks.push([-136, -336, -54, -204]);

    // ---- 돌담 (현무암 담) along the orchard and the outer edge of the north spine
    var stones = [];
    function wallLine(x0, z0, x1, z1) {
      var len = Math.hypot(x1 - x0, z1 - z0), n = Math.round(len / 1.3);
      for (var i = 0; i <= n; i++) { var t = i / n; var x = x0 + (x1 - x0) * t, z = z0 + (z1 - z0) * t; if (self.distToPath(x, z) < 12.5) continue; var sg = new THREE.BoxGeometry(rng.range(0.9, 1.4), rng.range(0.7, 1.1), rng.range(0.8, 1.2)); sg.rotateY(rng() * 0.6); sg.translate(x, groundY + 0.45, z); stones.push(sg); var s2 = new THREE.BoxGeometry(rng.range(0.7, 1.1), rng.range(0.5, 0.8), rng.range(0.7, 1.0)); s2.rotateY(rng() * 0.6); s2.translate(x + rng.range(-0.3, 0.3), groundY + 1.15, z); stones.push(s2); }
    }
    wallLine(-136, -336, -54, -336); wallLine(-136, -204, -54, -204); wallLine(-136, -336, -136, -204);
    for (var j = Math.round(0.2 * N); j < Math.round(0.33 * N); j += 1) { if (j % 1 === 0) { self.surfacePoint(j, self.maxLat[j] + 4.5, tmp); var sg2 = new THREE.BoxGeometry(rng.range(0.9, 1.3), rng.range(0.7, 1.0), rng.range(0.8, 1.2)); sg2.translate(tmp.x, Math.max(groundY, tmp.y - 0.2) + 0.4, tmp.z); stones.push(sg2); } }
    for (j = Math.round(0.905 * N); j < N + Math.round(0.02 * N); j += 1) { var jj = j % N; self.surfacePoint(jj, self.maxLat[jj] + 4.5, tmp); var sg3 = new THREE.BoxGeometry(rng.range(0.9, 1.3), rng.range(0.7, 1.0), rng.range(0.8, 1.2)); sg3.translate(tmp.x, groundY + 0.4, tmp.z); stones.push(sg3); }
    g.add(mesh(mergeGeoms(stones), basaltMat, 0, 0, 0));
    // 돌하르방 at the start line
    [self.maxLat[0] + 6, self.minLat[0] - 6].forEach(function (lat) {
      self.surfacePoint(0, lat, tmp); var h = new THREE.Group(); h.position.set(tmp.x, groundY, tmp.z);
      h.add(mesh(new THREE.CylinderGeometry(1.4, 1.6, 4.2, 9), basaltMat, 0, 2.1, 0));
      h.add(mesh(new THREE.SphereGeometry(1.35, 10, 8), basaltMat, 0, 4.9, 0));
      h.add(mesh(new THREE.CylinderGeometry(1.6, 1.3, 0.9, 10), basaltMat, 0, 6.2, 0));
      h.add(mesh(new THREE.SphereGeometry(0.4, 6, 5), basaltMat, -0.5, 5.1, 1.15)); h.add(mesh(new THREE.SphereGeometry(0.4, 6, 5), basaltMat, 0.5, 5.1, 1.15));
      h.add(mesh(new THREE.CylinderGeometry(0.22, 0.28, 1.1, 6).rotateX(Math.PI / 2), basaltMat, 0, 4.5, 1.4));
      h.add(mesh(new THREE.BoxGeometry(1.2, 0.5, 0.9), basaltMat, -0.9, 2.6, 1.2)); h.add(mesh(new THREE.BoxGeometry(1.2, 0.5, 0.9), basaltMat, 0.9, 1.8, 1.2));
      h.lookAt(path.P[0].x, groundY, path.P[0].z); g.add(h);
    });

    // ---- 곶자왈 forest (instanced broadleaf trees) + palms along the spine
    var trunkGeo = new THREE.CylinderGeometry(0.3, 0.5, 4.2, 6); trunkGeo.translate(0, 2.1, 0);
    var canopyGeo = mergeGeoms([new THREE.IcosahedronGeometry(3.0, 1).translate(0, 5.8, 0), new THREE.IcosahedronGeometry(2.3, 1).translate(1.8, 4.8, 0.8), new THREE.IcosahedronGeometry(2.2, 1).translate(-1.6, 5.0, -1.0), new THREE.IcosahedronGeometry(1.9, 1).translate(0.4, 7.4, -0.5)]);
    function blocked(x, z) { for (var i = 0; i < blocks.length; i++) { var b = blocks[i]; if (x > b[0] && x < b[2] && z > b[1] && z < b[3]) return true; } return false; }
    var spots = [], tries = 0;
    while (spots.length < 900 && tries++ < 30000) {
      var tx = rng.range(-340, 400), tz = rng.range(-780, 330);
      var inn2 = inside(tx, tz);
      if (self.distToPath(tx, tz) < (inn2 ? 15 : 13)) continue;
      if (blocked(tx, tz) || nearSign(tx, tz, 30)) continue;
      if (inn2 && rng() < 0.75) continue;                 // blocks are mostly built up
      if (inn2 && tz > -60 && tz < 120 && tx > 5 && tx < 100) continue;   // 이노에듀타운
      spots.push({ x: tx, y: groundY, z: tz, s: rng.range(0.8, 1.5), rot: rng() * 6.28, c: rng() });
    }
    // dense line of trees just beside the narrow forest section of the spine
    function roadTree(j, lat, s0, s1) { self.surfacePoint(j, lat, tmp); if (blocked(tmp.x, tmp.z) || nearSign(tmp.x, tmp.z, 34)) return; spots.push({ x: tmp.x, y: Math.max(groundY, tmp.y - 1.5), z: tmp.z, s: rng.range(s0, s1), rot: rng() * 6.28, c: rng() }); }
    for (j = Math.round(0.2 * N); j < Math.round(0.345 * N); j += 3) {
      roadTree(j, self.maxLat[j] + rng.range(7, 12), 1.0, 1.5);
      if (rng() < 0.6) roadTree(j, self.minLat[j] - rng.range(6, 10), 0.9, 1.3);
    }
    for (j = Math.round(0.36 * N); j < Math.round(0.6 * N); j += 4) roadTree(j, self.maxLat[j] + rng.range(6, 14), 0.9, 1.5);
    var trunks = new THREE.InstancedMesh(trunkGeo, new THREE.MeshStandardMaterial({ color: 0x4a3a2a, roughness: 1 }), spots.length);
    var canopy = new THREE.InstancedMesh(canopyGeo, new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.95 }), spots.length);
    var col = new THREE.Color(), gA = new THREE.Color(0x2c6b2f), gB = new THREE.Color(0x5aa441);
    spots.forEach(function (sp, i) { tmp.set(sp.x, sp.y, sp.z); e.set(0, sp.rot, 0); q.setFromEuler(e); sc.set(sp.s, sp.s, sp.s); m4.compose(tmp, q, sc); trunks.setMatrixAt(i, m4); canopy.setMatrixAt(i, m4); canopy.setColorAt(i, col.copy(gA).lerp(gB, sp.c)); });
    trunks.castShadow = canopy.castShadow = true; g.add(trunks); g.add(canopy);
    // palms along 이노에듀타운 street and the east boulevard
    var pTrunk = new THREE.CylinderGeometry(0.25, 0.42, 7, 6); pTrunk.translate(0, 3.5, 0);
    var pFrond = new THREE.PlaneGeometry(5, 2.2); pFrond.translate(2.5, 0, 0);
    var palms = [];
    function palmAt(j, lat) { self.surfacePoint(j, lat, tmp); if (nearSign(tmp.x, tmp.z, 22)) return; palms.push({ p: new THREE.Vector3(tmp.x, Math.max(groundY, tmp.y - 1.2), tmp.z), s: rng.range(0.85, 1.25), rot: rng() * 6.28 }); }
    for (j = Math.round(0.92 * N); j < N + Math.round(0.19 * N); j += 22) { var j2 = j % N; palmAt(j2, self.maxLat[j2] + 3.2); palmAt(j2, self.minLat[j2] - 3.2); }
    for (j = Math.round(0.56 * N); j < Math.round(0.86 * N); j += 24) { palmAt(j, self.maxLat[j] + 3.2); if (j % 48 === 0) palmAt(j, self.minLat[j] - 3.2); }
    var pt = new THREE.InstancedMesh(pTrunk, new THREE.MeshStandardMaterial({ color: 0x9c6b3c, roughness: 1 }), palms.length);
    var pf = new THREE.InstancedMesh(pFrond, new THREE.MeshStandardMaterial({ map: tex.palmFrond(), transparent: true, alphaTest: 0.5, side: THREE.DoubleSide, roughness: 1 }), palms.length * 6);
    palms.forEach(function (ps, i) {
      e.set(0, ps.rot, 0.1); q.setFromEuler(e); sc.set(ps.s, ps.s, ps.s); m4.compose(ps.p, q, sc); pt.setMatrixAt(i, m4);
      for (var k = 0; k < 6; k++) { e.set(0, k / 6 * Math.PI * 2 + ps.rot, -0.55 + rng() * 0.2); q.setFromEuler(e); tmp.copy(ps.p); tmp.y += 7 * ps.s; m4.compose(tmp, q, sc); pf.setMatrixAt(i * 6 + k, m4); }
    });
    pt.castShadow = pf.castShadow = true; g.add(pt); g.add(pf);

    // ---- 한라산, 오름, 풍력발전기, 구름, 해
    var mtMat = new THREE.MeshStandardMaterial({ color: 0x4d7a55, roughness: 1, flatShading: true });
    g.add(mesh(new THREE.ConeGeometry(620, 230, 9), mtMat, 480, groundY + 115, -1060));
    g.add(mesh(new THREE.ConeGeometry(120, 26, 9), mtMat, 480, groundY + 230 + 12, -1060));
    [[-320, -460, 95, 42], [340, -680, 75, 30], [310, 260, 85, 34], [-420, 40, 70, 26], [520, -300, 110, 44]].forEach(function (m) { g.add(mesh(new THREE.CylinderGeometry(m[2] * 0.35, m[2], m[3], 12), new THREE.MeshStandardMaterial({ color: 0x5c8f4a, roughness: 1, flatShading: true }), m[0], groundY + m[3] / 2, m[1])); });
    var rotors = [];
    [[-330, -170], [-400, -40], [-360, 210], [380, 90], [420, -480]].forEach(function (p) {
      var wt = new THREE.Group(); wt.position.set(p[0], groundY, p[1]);
      wt.add(mesh(new THREE.CylinderGeometry(1.0, 1.8, 44, 8), whiteMat, 0, 22, 0));
      wt.add(mesh(new THREE.BoxGeometry(3, 2.4, 6), whiteMat, 0, 44.5, 0));
      var rot = new THREE.Group(); rot.position.set(0, 44.5, 3.4);
      for (var b = 0; b < 3; b++) { var bl = mesh(new THREE.BoxGeometry(1.3, 19, 0.35), whiteMat, 0, 9.5, 0); var pv = new THREE.Group(); pv.rotation.z = b * Math.PI * 2 / 3; pv.add(bl); rot.add(pv); }
      wt.add(rot); wt.lookAt(0, groundY, 0); g.add(wt); rotors.push({ r: rot, v: rng.range(0.8, 1.3) });
    });
    this.animated.push(function (dt, t) { for (var i = 0; i < rotors.length; i++) rotors[i].r.rotation.z += dt * rotors[i].v; });
    var cloudMat = new THREE.SpriteMaterial({ map: tex.smoke(), color: 0xffffff, opacity: 0.95, transparent: true, depthWrite: false, fog: false });
    for (var c = 0; c < 22; c++) { var a2 = rng() * Math.PI * 2, r2 = rng.range(300, 900), cl = new THREE.Sprite(cloudMat); cl.position.set(50 + Math.cos(a2) * r2, rng.range(120, 200), -220 + Math.sin(a2) * r2); var s2 = rng.range(50, 120); cl.scale.set(s2 * 1.6, s2 * 0.8, 1); g.add(cl); }
    var sun = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xfff1b0, transparent: true, fog: false }));
    sun.position.set(400, 520, -700); sun.scale.set(240, 240, 1); g.add(sun);
  };


  // ------------------------------------------------------------- NITRO LAB (giant futuristic science room)
  Track.prototype._sceneryLab = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this, N = path.N;
    var tmp = new THREE.Vector3(), m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), e = new THREE.Euler();
    function mesh(geo, mat, x, y, z) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); m.castShadow = true; m.receiveShadow = true; return m; }
    function emis(color, i, extra) { var o = { color: color, emissive: color, emissiveIntensity: i === undefined ? 0.9 : i, roughness: 0.3, metalness: 0.2 }; if (extra) for (var k in extra) o[k] = extra[k]; return new THREE.MeshStandardMaterial(o); }
    var CYAN = 0x35e6ff, ORANGE = 0xff9a3c, PURPLE = 0xb388ff, GREEN = 0x7bffb0, YELLOW = 0xffe066, PINK = 0xff5c8a;
    var steelMat = new THREE.MeshStandardMaterial({ color: 0x8a94a8, roughness: 0.35, metalness: 0.7 });
    var darkMat = new THREE.MeshStandardMaterial({ color: 0x1b2136, roughness: 0.6, metalness: 0.4 });
    var whiteMat = new THREE.MeshStandardMaterial({ color: 0xdfe6f2, roughness: 0.5, metalness: 0.1 });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0xbfe8ff, roughness: 0.05, metalness: 0.1, transparent: true, opacity: 0.28, side: THREE.DoubleSide, depthWrite: false });
    var CX = 335, CZ = 40;   // room centre

    // ---- floor / walls / ceiling
    var floorTex = tex.labFloor(); floorTex.repeat.set(70, 70);
    var floor = mesh(new THREE.PlaneGeometry(2600, 2600).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ map: floorTex, roughness: 0.5, metalness: 0.3, emissive: 0x0b1a33, emissiveIntensity: 0.4 }), CX, groundY + 0.01, CZ);
    floor.castShadow = false; g.add(floor);
    var R = 780, WH = 300;
    var wallMat = new THREE.MeshStandardMaterial({ color: 0x151b33, roughness: 0.8, metalness: 0.2 });
    [[CX, CZ - R, 0], [CX, CZ + R, Math.PI], [CX - R, CZ, Math.PI / 2], [CX + R, CZ, -Math.PI / 2]].forEach(function (w, i) {
      var wall = mesh(new THREE.PlaneGeometry(R * 2, WH), wallMat, w[0], groundY + WH / 2, w[1]); wall.rotation.y = w[2]; wall.castShadow = false; g.add(wall);
      // glowing trim lines + windows to the void
      for (var yb = 30; yb < WH; yb += 60) { var trim = mesh(new THREE.BoxGeometry(R * 2, 0.8, 0.8), emis(i % 2 ? PURPLE : CYAN, 1.2), w[0], groundY + yb, w[1]); trim.rotation.y = w[2]; trim.castShadow = false; g.add(trim); }
    });
    // posters: periodic table (south wall), formula board (north wall), big screens (east/west)
    var poster = mesh(new THREE.PlaneGeometry(360, 200), new THREE.MeshBasicMaterial({ map: tex.periodic() }), CX - 150, groundY + 150, CZ - R + 1); g.add(poster);
    var board = mesh(new THREE.PlaneGeometry(320, 160), new THREE.MeshBasicMaterial({ map: tex.formula() }), CX + 200, groundY + 140, CZ + R - 1); board.rotation.y = Math.PI; g.add(board);
    [['NITRO LAB · SECTOR 7', CYAN], ['WARP GATE ONLINE', ORANGE], ['EXPERIMENT 42', GREEN], ['1 LAP · NO MERCY', PINK]].forEach(function (t, i) {
      var sx = i < 2 ? CX - R + 1 : CX + R - 1, sz = CZ - 250 + (i % 2) * 500;
      var scr = mesh(new THREE.PlaneGeometry(260, 65), new THREE.MeshBasicMaterial({ map: tex.label('lab' + i, t[0], '#' + t[1].toString(16).padStart(6, '0'), '#0b1226') }), sx, groundY + 120, sz);
      scr.rotation.y = i < 2 ? Math.PI / 2 : -Math.PI / 2; g.add(scr);
    });
    var ceil = mesh(new THREE.PlaneGeometry(R * 2, R * 2).rotateX(Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0x0b0f22, roughness: 1 }), CX, groundY + WH, CZ); ceil.castShadow = false; g.add(ceil);
    var panels = [];
    for (var px = -600; px <= 600; px += 200) for (var pz = -600; pz <= 600; pz += 200) { var pg = new THREE.BoxGeometry(70, 1.5, 12); pg.translate(CX + px, groundY + WH - 1, CZ + pz); panels.push(pg); }
    var panelMesh = mesh(mergeGeoms(panels), emis(0xdff4ff, 1.6), 0, 0, 0); panelMesh.castShadow = false; g.add(panelMesh);

    // ---- lab table under the elevated section (road y=18 from f 0.10 to 0.27)
    var TX = 200, TZ = 322, TW = 360, TD = 150, TH = 17.2;
    g.add(mesh(new THREE.BoxGeometry(TW, 2.2, TD), whiteMat, TX, groundY + TH - 1.1, TZ));
    g.add(mesh(new THREE.BoxGeometry(TW + 2, 0.6, TD + 2), emis(CYAN, 1.0), TX, groundY + TH - 2.4, TZ));
    [[-1, -1], [1, -1], [-1, 1], [1, 1]].forEach(function (c) { g.add(mesh(new THREE.CylinderGeometry(4, 5, TH - 2.2, 10), steelMat, TX + c[0] * (TW / 2 - 12), groundY + (TH - 2.2) / 2, TZ + c[1] * (TD / 2 - 12))); });
    // table-top props: microscope, book stack, petri dishes
    (function microscope(x, z, y0, s) {
      var mg = new THREE.Group(); mg.position.set(x, y0, z); mg.scale.set(s, s, s);
      mg.add(mesh(new THREE.CylinderGeometry(6, 8, 1.5, 16), darkMat, 0, 0.75, 0));
      var arm = mesh(new THREE.BoxGeometry(2.2, 14, 2.2), steelMat, -3.5, 8, 0); arm.rotation.z = 0.35; mg.add(arm);
      mg.add(mesh(new THREE.CylinderGeometry(1.4, 1.8, 10, 12), whiteMat, 1.2, 12, 0));
      mg.add(mesh(new THREE.CylinderGeometry(1.0, 1.4, 3, 12), darkMat, 1.2, 6, 0));
      mg.add(mesh(new THREE.BoxGeometry(7, 0.6, 5), steelMat, 1.2, 3.6, 0));
      mg.add(mesh(new THREE.CylinderGeometry(0.4, 0.4, 3, 8), emis(GREEN, 1.3), 1.2, 2.2, 0));
      g.add(mg);
    })(70, 385, groundY + TH, 2.2);
    [[110, 285, 0xff5c8a], [95, 268, 0x7bffb0], [125, 272, 0xb388ff]].forEach(function (b, i) { g.add(mesh(new THREE.BoxGeometry(34 - i * 4, 6, 22), new THREE.MeshStandardMaterial({ color: b[2], roughness: 0.7 }), b[0], groundY + TH + 3 + i * 6, b[1])); });
    [[300, 385], [320, 372], [260, 388]].forEach(function (pd) { g.add(mesh(new THREE.CylinderGeometry(7, 7, 1, 20), glassMat, pd[0], groundY + TH + 0.5, pd[1])); g.add(mesh(new THREE.CylinderGeometry(4, 4, 0.6, 14), emis(GREEN, 1.2), pd[0], groundY + TH + 0.7, pd[1])); });
    // warning sign before the table drop
    var bubbles = [];
    function neonSign(key, text, x, z, fx, fz, color, w) {
      w = w || 18; var grp = new THREE.Group();
      grp.add(mesh(new THREE.BoxGeometry(w, w * 0.25, 0.4), new THREE.MeshBasicMaterial({ map: tex.label('labsign' + key, text, '#ffffff', '#' + color.toString(16).padStart(6, '0')) }), 0, 8, 0));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.6, w * 0.25 + 0.6, 0.2), new THREE.MeshStandardMaterial({ color: 0x0e1224, emissive: color, emissiveIntensity: 0.22, roughness: 0.5 }), 0, 8, -0.32));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.8, 0.25, 0.25), emis(color, 1.4), 0, 8 + w * 0.125 + 0.3, -0.2));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.8, 0.25, 0.25), emis(color, 1.4), 0, 8 - w * 0.125 - 0.3, -0.2));
      grp.add(mesh(new THREE.BoxGeometry(0.6, 7, 0.6), steelMat, 0, 3.5, -0.1));
      grp.position.set(x, groundY, z); grp.lookAt(fx, groundY, fz); g.add(grp); return grp;
    }
    function trackSign(key, text, f, lat, color, w) { var j = Math.round(f * N) % N, jb = (j - 30 + N) % N; self.surfacePoint(j, lat, tmp); var sg = neonSign(key, text, tmp.x, tmp.z, path.P[jb].x, path.P[jb].z, color, w); sg.position.y = tmp.y - 0.4; return sg; }
    trackSign('drop', 'DANGER · 18m DROP', 0.255, self.maxLat[Math.round(0.255 * N)] + 7, PINK, 16);
    trackSign('warp1', 'WARP GATE →', 0.148, self.maxLat[Math.round(0.148 * N)] + 6, ORANGE, 16);
    trackSign('warp2', 'WARP GATE →', 0.758, self.minLat[Math.round(0.758 * N)] - 6, ORANGE, 16);
    trackSign('reactor', 'REACTOR CORE', 0.345, self.minLat[Math.round(0.345 * N)] - 6, CYAN, 18);
    trackSign('tube', 'SPECIMEN TUBES', 0.595, self.maxLat[Math.round(0.595 * N)] + 6, GREEN, 18);
    trackSign('dna', 'DNA SEQUENCER', 0.75, self.maxLat[Math.round(0.75 * N)] + 6, PURPLE, 18);
    trackSign('final', 'FINAL SECTOR', 0.915, self.maxLat[Math.round(0.915 * N)] + 6, YELLOW, 18);
    trackSign('lab', 'NITRO LAB', 0.985, self.minLat[Math.round(0.985 * N)] - 6, CYAN, 16);

    // ---- giant beakers with glowing liquid + rising bubbles
    var bubbleMat = new THREE.SpriteMaterial({ map: tex.particle(), color: 0xffffff, transparent: true, opacity: 0.7, depthWrite: false, blending: THREE.AdditiveBlending });
    function beaker(x, z, r, h, color) {
      g.add(mesh(new THREE.CylinderGeometry(r, r * 0.92, h, 28, 1, true), glassMat, x, groundY + h / 2, z));
      g.add(mesh(new THREE.TorusGeometry(r + 0.6, 1.2, 8, 32).rotateX(Math.PI / 2), glassMat, x, groundY + h, z));
      var liq = mesh(new THREE.CylinderGeometry(r - 0.8, r * 0.92 - 0.8, h * 0.6, 28), emis(color, 0.8, { transparent: true, opacity: 0.85 }), x, groundY + h * 0.3, z); g.add(liq);
      for (var i = 0; i < 14; i++) { var b = new THREE.Sprite(bubbleMat); var a = rng() * 6.28, rr = rng() * (r - 3); b.position.set(x + Math.cos(a) * rr, groundY + rng() * h * 0.6, z + Math.sin(a) * rr); b.scale.set(rng.range(1, 3), rng.range(1, 3), 1); b.userData = { v: rng.range(2, 5), top: groundY + h * 0.6, bot: groundY + 1 }; g.add(b); bubbles.push(b); }
      // label strip
      g.add(mesh(new THREE.BoxGeometry(r * 1.2, h * 0.12, 0.3), new THREE.MeshBasicMaterial({ map: tex.label('bk' + x, (Math.round(h * 20)) + ' mL', '#ffffff', '#1a2240') }), x, groundY + h * 0.75, z + r + 0.2));
    }
    beaker(350, 120, 32, 76, CYAN);
    beaker(-160, 110, 28, 64, GREEN);
    beaker(200, 480, 36, 84, PURPLE);
    beaker(620, -240, 30, 70, ORANGE);
    beaker(880, 120, 26, 60, PINK);
    // Erlenmeyer flasks
    [[-140, -230, 34, 80, YELLOW], [800, 330, 40, 96, GREEN]].forEach(function (f) {
      g.add(mesh(new THREE.CylinderGeometry(f[2] * 0.3, f[2], f[3] * 0.75, 28, 1, true), glassMat, f[0], groundY + f[3] * 0.375, f[1]));
      g.add(mesh(new THREE.CylinderGeometry(f[2] * 0.3, f[2] * 0.3, f[3] * 0.3, 20, 1, true), glassMat, f[0], groundY + f[3] * 0.9, f[1]));
      g.add(mesh(new THREE.CylinderGeometry(f[2] * 0.62, f[2] - 0.8, f[3] * 0.3, 28), emis(f[4], 0.8, { transparent: true, opacity: 0.85 }), f[0], groundY + f[3] * 0.15, f[1]));
    });

    // ---- specimen tube rack beside the S-curves (south of them) + tube tunnel decor
    var rackX = 480, rackZ = -200;
    g.add(mesh(new THREE.BoxGeometry(220, 4, 30), darkMat, rackX, groundY + 2, rackZ));
    g.add(mesh(new THREE.BoxGeometry(220, 2, 30), steelMat, rackX, groundY + 40, rackZ));
    [CYAN, GREEN, PINK, PURPLE, ORANGE, YELLOW, CYAN, GREEN].forEach(function (c, i) {
      var x = rackX - 96 + i * 27.5;
      g.add(mesh(new THREE.CylinderGeometry(8, 8, 60, 20, 1, true), glassMat, x, groundY + 34, rackZ));
      g.add(mesh(new THREE.SphereGeometry(8, 20, 12, 0, Math.PI * 2, Math.PI / 2, Math.PI / 2), glassMat, x, groundY + 4, rackZ));
      g.add(mesh(new THREE.CylinderGeometry(7, 7, 30 + (i % 3) * 8, 20), emis(c, 0.8, { transparent: true, opacity: 0.85 }), x, groundY + 4 + 15 + (i % 3) * 4, rackZ));
      g.add(mesh(new THREE.CylinderGeometry(8.5, 8.5, 3, 20), steelMat, x, groundY + 64, rackZ));
    });

    // ---- reactor core (inside hairpin 1) + reactor tower outside the bulge
    var coreGroups = [];
    function core(x, z, y, r) {
      var cg = new THREE.Group(); cg.position.set(x, y, z);
      cg.add(mesh(new THREE.SphereGeometry(r, 20, 14), emis(CYAN, 1.6)));
      [[1.6, 0], [2.1, 1.1], [2.6, 2.2]].forEach(function (rr) { var ring = mesh(new THREE.TorusGeometry(r * rr[0], r * 0.09, 8, 40), emis(ORANGE, 1.2)); ring.rotation.x = rr[1]; ring.userData.ax = rr[1]; cg.add(ring); });
      g.add(cg); coreGroups.push(cg); return cg;
    }
    core(563.6, 152, groundY + 22, 6);
    core(651.8, 233.5, groundY + 22, 6);
    g.add(mesh(new THREE.CylinderGeometry(38, 44, 130, 24), darkMat, 760, groundY + 65, 130));
    for (var rb = 12; rb < 130; rb += 24) g.add(mesh(new THREE.TorusGeometry(42, 1.6, 8, 40).rotateX(Math.PI / 2), emis(CYAN, 1.3), 760, groundY + rb, 130));
    core(760, 130, groundY + 150, 14);

    // ---- DNA helices: giant one inside the loop, small one in the DNA hairpin
    var helices = [];
    function dna(x, z, r, h, n) {
      var hg = new THREE.Group(); hg.position.set(x, groundY, z);
      var sph = new THREE.SphereGeometry(r * 0.16, 10, 8), rung = new THREE.CylinderGeometry(r * 0.05, r * 0.05, r * 2, 6).rotateZ(Math.PI / 2);
      var ma = emis(CYAN, 1.2), mb = emis(PINK, 1.2), mr = new THREE.MeshStandardMaterial({ color: 0xdfe6f2, emissive: 0x3350aa, emissiveIntensity: 0.6 });
      for (var i = 0; i < n; i++) {
        var a = i / n * Math.PI * 4, y = 4 + (i / n) * h;
        hg.add(mesh(sph, ma, Math.cos(a) * r, y, Math.sin(a) * r)); hg.add(mesh(sph, mb, -Math.cos(a) * r, y, -Math.sin(a) * r));
        var rg = mesh(rung, mr, 0, y, 0); rg.rotation.y = -a; hg.add(rg);
      }
      g.add(hg); helices.push(hg);
    }
    dna(470, 100, 16, 130, 34);
    dna(245.5, -174, 6, 40, 16);
    dna(-120, 320, 12, 100, 28);

    // ---- robot arms along the long east straight
    var arms = [];
    [[755, 20], [755, -40], [755, 80]].forEach(function (p, i) {
      var base = new THREE.Group(); base.position.set(p[0], groundY, p[1]);
      base.add(mesh(new THREE.CylinderGeometry(5, 6, 4, 12), darkMat, 0, 2, 0));
      var j1 = new THREE.Group(); j1.position.y = 4; base.add(j1);
      j1.add(mesh(new THREE.BoxGeometry(3, 26, 3), steelMat, 0, 13, 0));
      var j2 = new THREE.Group(); j2.position.y = 26; j1.add(j2);
      j2.add(mesh(new THREE.BoxGeometry(2.4, 22, 2.4), whiteMat, 0, 11, 0));
      j2.add(mesh(new THREE.SphereGeometry(2.4, 10, 8), emis(ORANGE, 1.2), 0, 22, 0));
      j1.rotation.z = -0.6; j2.rotation.z = 1.2;
      g.add(base); arms.push({ j1: j1, j2: j2, ph: i * 2.1 });
    });

    // ---- holographic rings above the start straight and the finish straight, drones, laser fence
    var holos = [];
    for (var j = Math.round(0.005 * N); j < Math.round(0.045 * N); j += 14) {
      var ring = mesh(new THREE.TorusGeometry(14, 0.35, 6, 48), emis(CYAN, 1.4, { transparent: true, opacity: 0.6 }), path.P[j].x, path.P[j].y + 8.5, path.P[j].z);
      ring.rotation.x = Math.PI / 2; ring.castShadow = false; g.add(ring); holos.push(ring);
    }
    for (j = Math.round(0.93 * N); j < Math.round(0.985 * N); j += 14) {
      var ring2 = mesh(new THREE.TorusGeometry(14, 0.35, 6, 48), emis(PURPLE, 1.4, { transparent: true, opacity: 0.6 }), path.P[j].x, path.P[j].y + 8.5, path.P[j].z);
      ring2.rotation.x = Math.PI / 2; ring2.castShadow = false; g.add(ring2); holos.push(ring2);
    }
    var drones = [];
    for (var d = 0; d < 8; d++) {
      var dr = new THREE.Group();
      dr.add(mesh(new THREE.BoxGeometry(3, 1, 3), darkMat, 0, 0, 0));
      dr.add(mesh(new THREE.SphereGeometry(0.6, 8, 6), emis([CYAN, PINK, GREEN, ORANGE][d % 4], 1.6), 0, -0.7, 0));
      [[-1.6, -1.6], [1.6, -1.6], [-1.6, 1.6], [1.6, 1.6]].forEach(function (rp) { dr.add(mesh(new THREE.CylinderGeometry(1.1, 1.1, 0.15, 10), steelMat, rp[0], 0.6, rp[1])); });
      var fj = Math.round(rng() * N); dr.userData = { j: fj, side: rng() < 0.5 ? -1 : 1, h: rng.range(6, 14), v: rng.range(8, 16) * (rng() < 0.5 ? -1 : 1) };
      g.add(dr); drones.push(dr);
    }
    var lasers = [];
    for (j = Math.round(0.47 * N); j < Math.round(0.55 * N); j += 2) {
      if (j % 40 === 0) { self.surfacePoint(j, self.maxLat[j] + 3, tmp); var post = mesh(new THREE.BoxGeometry(0.6, 4, 0.6), darkMat, tmp.x, tmp.y + 2, tmp.z); g.add(post); }
    }
    for (var ls = 0; ls < 2; ls++) {
      var j0 = Math.round(0.47 * N), j1 = Math.round(0.55 * N); self.surfacePoint(j0, self.maxLat[j0] + 3, tmp); var a0 = tmp.clone(); self.surfacePoint(j1, self.maxLat[j1] + 3, tmp);
      var len = a0.distanceTo(tmp), beam = mesh(new THREE.CylinderGeometry(0.12, 0.12, len, 5), emis(ls ? PINK : CYAN, 2.0), 0, 0, 0);
      beam.position.copy(a0).lerp(tmp, 0.5); beam.position.y += 1.2 + ls * 1.6; beam.lookAt(tmp.x, beam.position.y, tmp.z); beam.rotateX(Math.PI / 2); beam.castShadow = false; g.add(beam); lasers.push(beam);
    }
    // giant pencil + ruler on the floor
    var pencil = new THREE.Group(); pencil.position.set(420, groundY + 7, -380);
    pencil.add(mesh(new THREE.CylinderGeometry(7, 7, 300, 6).rotateZ(Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xffc933, roughness: 0.7 }), 0, 0, 0));
    pencil.add(mesh(new THREE.ConeGeometry(7, 26, 6).rotateZ(-Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xe8cfa0, roughness: 0.8 }), 163, 0, 0));
    pencil.add(mesh(new THREE.ConeGeometry(2.6, 9, 6).rotateZ(-Math.PI / 2), darkMat, 176, 0, 0));
    pencil.add(mesh(new THREE.CylinderGeometry(7.2, 7.2, 14, 6).rotateZ(Math.PI / 2), steelMat, -152, 0, 0));
    pencil.add(mesh(new THREE.CylinderGeometry(6.6, 6.6, 12, 6).rotateZ(Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xff7aa8, roughness: 0.9 }), -165, 0, 0));
    pencil.rotation.y = 0.2; g.add(pencil);
    g.add(mesh(new THREE.BoxGeometry(24, 2, 380), new THREE.MeshStandardMaterial({ color: 0xffe99a, roughness: 0.8 }), -200, groundY + 1, -60));
    for (var rk = -180; rk <= 180; rk += 20) g.add(mesh(new THREE.BoxGeometry(rk % 100 === 0 ? 12 : 6, 0.3, 1.2), darkMat, -206 + (rk % 100 === 0 ? 3 : 0), groundY + 2.2, -60 + rk));

    // ---- animation
    this.animated.push(function (dt, t) {
      for (var i = 0; i < bubbles.length; i++) { var b = bubbles[i]; b.position.y += b.userData.v * dt; if (b.position.y > b.userData.top) b.position.y = b.userData.bot; }
      for (i = 0; i < coreGroups.length; i++) { var cg = coreGroups[i]; cg.children[0].scale.setScalar(1 + Math.sin(t * 3 + i) * 0.08); for (var c = 1; c < cg.children.length; c++) { cg.children[c].rotation.y += dt * (0.6 + c * 0.35); cg.children[c].rotation.x = cg.children[c].userData.ax + Math.sin(t * 0.7 + c) * 0.3; } }
      for (i = 0; i < helices.length; i++) helices[i].rotation.y += dt * 0.35;
      for (i = 0; i < arms.length; i++) { arms[i].j1.rotation.z = -0.6 + Math.sin(t * 0.8 + arms[i].ph) * 0.35; arms[i].j2.rotation.z = 1.2 + Math.cos(t * 1.1 + arms[i].ph) * 0.5; arms[i].j1.rotation.y = Math.sin(t * 0.5 + arms[i].ph) * 0.6; }
      for (i = 0; i < holos.length; i++) { holos[i].rotation.z += dt * 0.8; holos[i].material.opacity = 0.35 + Math.sin(t * 2 + i) * 0.25; }
      for (i = 0; i < drones.length; i++) { var dr = drones[i], u = dr.userData; u.j = ((u.j + u.v * dt / path.ds) % N + N) % N; var jj = Math.floor(u.j); self.surfacePoint(jj, u.side * (path.W[jj] / 2 + 6), tmp); dr.position.set(tmp.x, tmp.y + u.h + Math.sin(t * 2 + i) * 1.2, tmp.z); dr.rotation.y += dt; }
      for (i = 0; i < lasers.length; i++) lasers[i].material.emissiveIntensity = 1.4 + Math.sin(t * 9 + i * 2) * 0.6;
    });
  };


  // ------------------------------------------------------------- ORBITAL RUN (point-to-point through space)
  Track.prototype._scenerySpace = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this, N = path.N;
    var tmp = new THREE.Vector3(), tmp2 = new THREE.Vector3(), m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), e = new THREE.Euler();
    function mesh(geo, mat, x, y, z) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); m.castShadow = true; m.receiveShadow = true; return m; }
    function emis(color, i, extra) { var o = { color: color, emissive: color, emissiveIntensity: i === undefined ? 0.9 : i, roughness: 0.3, metalness: 0.2 }; if (extra) for (var k in extra) o[k] = extra[k]; return new THREE.MeshStandardMaterial(o); }
    var CYAN = 0x35e6ff, ORANGE = 0xff9a3c, PURPLE = 0xb388ff, GREEN = 0x7bffb0, PINK = 0xff5c8a, WHITE = 0xe8f0ff;
    var hullMat = new THREE.MeshStandardMaterial({ color: 0x8a94a8, roughness: 0.45, metalness: 0.7 });
    var hullDark = new THREE.MeshStandardMaterial({ color: 0x2a3246, roughness: 0.6, metalness: 0.5 });
    var panelMat = new THREE.MeshStandardMaterial({ color: 0x223a7a, roughness: 0.3, metalness: 0.6, emissive: 0x102050, emissiveIntensity: 0.4, side: THREE.DoubleSide });
    var glassMat = new THREE.MeshStandardMaterial({ color: 0xbfe8ff, roughness: 0.05, metalness: 0.1, transparent: true, opacity: 0.22, side: THREE.DoubleSide, depthWrite: false });
    var J = function (f) { return Math.max(0, Math.min(N - 1, Math.round(f * N))); };
    function edge(latFn, dy) { return function (j, out) { self.surfacePoint(j, latFn(j), out); if (dy) out.y += dy; }; }
    function strip(latA, latB, dyA, dyB, f0, f1, mat, vs) { var m = new THREE.Mesh(stripGeom(path, edge(latA, dyA), edge(latB, dyB), vs || 8, [J(f0), J(f1)]), mat); m.receiveShadow = true; g.add(m); return m; }
    var minLat = this.minLat, maxLat = this.maxLat;
    function faceAlong(o, j) { o.lookAt(o.position.x + path.T[j].x, o.position.y, o.position.z + path.T[j].z); return o; }

    // ---- starfield + nebulae + sun (fog-free)
    var starPos = [], starCol = [], cx = -130, cz = 1500, cy = 0;
    for (var i = 0; i < 6000; i++) { var th = rng() * 6.283, ph = Math.acos(rng() * 2 - 1), r = 2600 + rng() * 600; starPos.push(cx + r * Math.sin(ph) * Math.cos(th), cy + r * Math.cos(ph), cz + r * Math.sin(ph) * Math.sin(th)); var c = 0.6 + rng() * 0.4, tint = rng(); starCol.push(c, c * (0.85 + tint * 0.15), c * (0.8 + tint * 0.2)); }
    var starGeo = new THREE.BufferGeometry(); starGeo.setAttribute('position', new THREE.Float32BufferAttribute(starPos, 3)); starGeo.setAttribute('color', new THREE.Float32BufferAttribute(starCol, 3));
    var stars = new THREE.Points(starGeo, new THREE.PointsMaterial({ size: 4.5, vertexColors: true, sizeAttenuation: true, fog: false, transparent: true, opacity: 0.95 })); g.add(stars);
    var nebMat = function (c) { return new THREE.SpriteMaterial({ map: tex.smoke(), color: c, transparent: true, opacity: 0.35, depthWrite: false, blending: THREE.AdditiveBlending, fog: false }); };
    // nebulae + sun sit on the sky dome (far from every part of the course, so they never read as nearby surfaces)
    var SR = 2800;
    [[0x5b2bd6, -0.9, 0.25, -0.35, 1500], [0x1f5fbf, 0.6, -0.05, 0.8, 1400], [0xd2408a, 0.2, 0.55, -0.8, 1200], [0x2bb5c9, -0.5, -0.2, 0.85, 1100]].forEach(function (n) {
      var sp = new THREE.Sprite(nebMat(n[0])); var v = new THREE.Vector3(n[1], n[2], n[3]).normalize().multiplyScalar(SR); sp.position.set(cx + v.x, cy + v.y, cz + v.z); sp.scale.set(n[4], n[4] * 0.7, 1); g.add(sp);
    });
    var sun = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xfff4d6, transparent: true, fog: false })); sun.position.set(cx + 2100, cy + 1400, cz - 1500); sun.scale.set(560, 560, 1); g.add(sun);

    // ---- 1. space station interior (f 0 .. 0.088): corridor floor, walls, ceiling, light strips, docked shuttles
    var wallMat = new THREE.MeshStandardMaterial({ map: tex.labFloor(), color: 0xb8c4dc, roughness: 0.5, metalness: 0.5, side: THREE.DoubleSide });
    var floorTex = tex.labFloor(); var floorMat = new THREE.MeshStandardMaterial({ map: floorTex, color: 0x9aa6c0, roughness: 0.6, metalness: 0.4, side: THREE.DoubleSide });
    strip(function (j) { return minLat[j] - 16; }, function (j) { return maxLat[j] + 16; }, -0.15, -0.15, 0, 0.09, floorMat, 16);
    strip(function (j) { return minLat[j] - 16; }, function (j) { return minLat[j] - 16; }, 0, 14, 0, 0.09, wallMat, 14);
    strip(function (j) { return maxLat[j] + 16; }, function (j) { return maxLat[j] + 16; }, 0, 14, 0, 0.09, wallMat, 14);
    strip(function (j) { return minLat[j] - 16; }, function (j) { return maxLat[j] + 16; }, 14, 14, 0, 0.09, new THREE.MeshStandardMaterial({ color: 0x1b2136, roughness: 0.8, side: THREE.DoubleSide }), 16);
    strip(function (j) { return minLat[j] - 15.6; }, function (j) { return minLat[j] - 15.6; }, 5.5, 6.3, 0, 0.09, emis(CYAN, 1.4), 8);
    strip(function (j) { return maxLat[j] + 15.6; }, function (j) { return maxLat[j] + 15.6; }, 5.5, 6.3, 0, 0.09, emis(CYAN, 1.4), 8);
    for (var j = 6; j < J(0.088); j += 10) { self.surfacePoint(j, 0, tmp); var lamp = mesh(new THREE.BoxGeometry(path.W[j] + 20, 0.3, 1.2), emis(WHITE, 1.6), tmp.x, tmp.y + 13.6, tmp.z); faceAlong(lamp, j); lamp.castShadow = false; g.add(lamp); }
    function shuttle(x, y, z, yaw, s) {
      var sh = new THREE.Group(); sh.position.set(x, y, z); sh.rotation.y = yaw; sh.scale.setScalar(s);
      sh.add(mesh(new THREE.CylinderGeometry(1.4, 1.8, 7, 10).rotateX(Math.PI / 2), hullMat, 0, 1.8, 0));
      sh.add(mesh(new THREE.ConeGeometry(1.4, 3, 10).rotateX(Math.PI / 2), hullMat, 0, 1.8, 5));
      sh.add(mesh(new THREE.BoxGeometry(7, 0.25, 2.2), hullDark, 0, 1.4, -1));
      sh.add(mesh(new THREE.SphereGeometry(0.9, 8, 6), emis(ORANGE, 1.6), 0, 1.8, -3.7));
      [[-1.5, 0.4], [1.5, 0.4]].forEach(function (lp) { sh.add(mesh(new THREE.BoxGeometry(0.3, 1.2, 0.3), hullDark, lp[0], 0.6, lp[1])); });
      g.add(sh); return sh;
    }
    for (j = 20; j < J(0.083); j += 46) { self.surfacePoint(j, minLat[j] - 9.5, tmp); shuttle(tmp.x, tmp.y, tmp.z, Math.atan2(path.T[j].x, path.T[j].z) + 0.4, 1.1); if (j % 92 === 20) { self.surfacePoint(j, maxLat[j] + 9.5, tmp); shuttle(tmp.x, tmp.y, tmp.z, Math.atan2(path.T[j].x, path.T[j].z) - 0.4, 1.0); } }
    function sign(key, text, f, lat, color, w, dy) {
      w = w || 16; var j = J(f), jb = Math.max(0, j - 30), grp = new THREE.Group();
      grp.add(mesh(new THREE.BoxGeometry(w, w * 0.25, 0.4), new THREE.MeshBasicMaterial({ map: tex.label('sp' + key, text, '#ffffff', '#' + color.toString(16).padStart(6, '0')) }), 0, 7, 0));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.8, 0.25, 0.25), emis(color, 1.4), 0, 7 + w * 0.125 + 0.3, -0.1));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.8, 0.25, 0.25), emis(color, 1.4), 0, 7 - w * 0.125 - 0.3, -0.1));
      grp.add(mesh(new THREE.BoxGeometry(0.5, 6.5, 0.5), hullDark, 0, 3.2, -0.1));
      self.surfacePoint(j, lat, tmp); grp.position.set(tmp.x, tmp.y - 0.3 + (dy || 0), tmp.z); grp.lookAt(path.P[jb].x, grp.position.y, path.P[jb].z); g.add(grp); return grp;
    }
    sign('hangar', 'HANGAR 01 · DEPARTURE', 0.012, maxLat[J(0.012)] + 6, CYAN, 18);
    sign('air', 'AIRLOCK → LAUNCH TUNNEL', 0.078, minLat[J(0.078)] - 6, ORANGE, 18);

    // ---- 2. launch tunnel (0.09 .. 0.17): speed rings
    var rings = [];
    for (j = J(0.10); j < J(0.165); j += 9) { self.surfacePoint(j, 0, tmp); var rg = mesh(new THREE.TorusGeometry(path.W[j] / 2 + 2.2, 0.3, 6, 40), emis([CYAN, PURPLE][(j / 9 | 0) % 2], 1.5), tmp.x, tmp.y + 2.5, tmp.z); faceAlong(rg, j); rg.castShadow = false; g.add(rg); rings.push(rg); }
    sign('launch', 'LAUNCH SEQUENCE · HOLD BOOST', 0.095, maxLat[J(0.095)] + 5, PINK, 20, 3);

    // ---- 3. zero-gravity (0.176 .. 0.296): energy bridge glow, asteroids + debris drifting, gravity generators
    var glowMat = new THREE.MeshBasicMaterial({ color: CYAN, transparent: true, opacity: 0.16, blending: THREE.AdditiveBlending, depthWrite: false, side: THREE.DoubleSide });
    strip(function (j) { return minLat[j] - 1; }, function (j) { return maxLat[j] + 1; }, 0.08, 0.08, 0.176, 0.296, glowMat, 8);
    var rockGeo = new THREE.IcosahedronGeometry(1, 1), rocks = [], rockCount = 260;
    var rockMesh = new THREE.InstancedMesh(rockGeo, new THREE.MeshStandardMaterial({ color: 0x8b8f9c, roughness: 1, flatShading: true }), rockCount);
    for (i = 0; i < rockCount; i++) {
      var jr = J(0.176 + rng() * 0.12), side = rng() < 0.5 ? -1 : 1; self.surfacePoint(jr, side * (path.W[jr] / 2 + rng.range(14, 90)), tmp);
      rocks.push({ p: tmp.clone().add(new THREE.Vector3(0, rng.range(-40, 50), 0)), s: rng.range(1.5, 9), rot: new THREE.Vector3(rng(), rng(), rng()).multiplyScalar(0.6), ph: rng() * 6.28, amp: rng.range(1, 4) });
    }
    rockMesh.castShadow = true; g.add(rockMesh);
    for (j = J(0.19); j < J(0.29); j += 40) { self.surfacePoint(j, maxLat[j] + 8, tmp); var gen = mesh(new THREE.TorusGeometry(4, 0.6, 8, 24), emis(GREEN, 1.3), tmp.x, tmp.y + 6, tmp.z); gen.userData.spin = 1; g.add(gen); rings.push(gen); g.add(mesh(new THREE.CylinderGeometry(0.5, 0.8, 6, 8), hullDark, tmp.x, tmp.y + 3, tmp.z)); }
    sign('zerog', 'ZERO-G SECTOR · GRAVITY OFF', 0.18, minLat[J(0.18)] - 6, GREEN, 20);

    // ---- 4. orbital highway (0.296 .. 0.445): planet below, moon, satellites
    var planet = mesh(new THREE.SphereGeometry(760, 48, 32), new THREE.MeshStandardMaterial({ color: 0x3f7fd6, roughness: 0.9, emissive: 0x0a2a5a, emissiveIntensity: 0.35 }), -520, -1050, 1300); planet.castShadow = false; g.add(planet);
    var cloudsP = mesh(new THREE.SphereGeometry(772, 48, 32), new THREE.MeshStandardMaterial({ map: tex.smoke(), color: 0xffffff, transparent: true, opacity: 0.35, roughness: 1, depthWrite: false }), -520, -1050, 1300); cloudsP.castShadow = false; g.add(cloudsP);
    var atmo = mesh(new THREE.SphereGeometry(800, 48, 32), new THREE.MeshBasicMaterial({ color: 0x6fb8ff, transparent: true, opacity: 0.08, side: THREE.BackSide, depthWrite: false }), -520, -1050, 1300); g.add(atmo);
    var moon = mesh(new THREE.SphereGeometry(130, 28, 20), new THREE.MeshStandardMaterial({ color: 0xc9c4b8, roughness: 1 }), 700, 260, 1500); moon.castShadow = false; g.add(moon);
    function satellite(x, y, z, s) {
      var st = new THREE.Group(); st.position.set(x, y, z); st.scale.setScalar(s);
      st.add(mesh(new THREE.BoxGeometry(4, 4, 6), hullMat, 0, 0, 0));
      st.add(mesh(new THREE.BoxGeometry(22, 0.2, 6), panelMat, -14, 0, 0)); st.add(mesh(new THREE.BoxGeometry(22, 0.2, 6), panelMat, 14, 0, 0));
      st.add(mesh(new THREE.CylinderGeometry(0.1, 2.5, 2.5, 10), hullDark, 0, 3.2, 0));
      st.add(mesh(new THREE.SphereGeometry(0.6, 6, 5), emis(PINK, 1.8), 0, -2.8, 0));
      g.add(st); st.userData.spin = rng.range(0.1, 0.3); rings.push(st); return st;
    }
    for (j = J(0.30); j < J(0.445); j += 55) { var sd = (j / 55 | 0) % 2 ? 1 : -1; self.surfacePoint(j, sd * (path.W[j] / 2 + rng.range(30, 70)), tmp); satellite(tmp.x, tmp.y + rng.range(10, 40), tmp.z, rng.range(1.2, 2.2)); }
    sign('orbit', 'ORBITAL HIGHWAY · 300 KM/H ZONE', 0.30, maxLat[J(0.30)] + 6, CYAN, 22);
    // 5. planet orbit (0.445 .. 0.571): ringed gas giant inside the 200° arc
    var GX = -847, GZ = 1912;
    var giant = mesh(new THREE.SphereGeometry(105, 40, 28), new THREE.MeshStandardMaterial({ color: 0xd9a066, roughness: 0.9, emissive: 0x4a2a10, emissiveIntensity: 0.3 }), GX, -60, GZ); giant.castShadow = false; g.add(giant);
    [[0xc98a55, 0.35], [0xf0d0a0, 0.55], [0xb07040, 0.75]].forEach(function (b, i) { var band = mesh(new THREE.CylinderGeometry(106 * Math.sqrt(1 - b[1] * b[1]), 106 * Math.sqrt(1 - b[1] * b[1]), 8, 40, 1, true), new THREE.MeshStandardMaterial({ color: b[0], roughness: 1, side: THREE.DoubleSide }), GX, -60 + 105 * b[1] * (i % 2 ? -1 : 1), GZ); band.castShadow = false; g.add(band); });
    var pring = mesh(new THREE.RingGeometry(125, 165, 64).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xe8d8b8, roughness: 0.9, side: THREE.DoubleSide, transparent: true, opacity: 0.75 }), GX, -40, GZ); pring.rotation.z = 0.12; pring.castShadow = false; g.add(pring);
    for (i = 0; i < 3; i++) { var mo = mesh(new THREE.SphereGeometry(rng.range(8, 16), 12, 10), new THREE.MeshStandardMaterial({ color: 0xc0c4d0, roughness: 1, emissive: 0x303848, emissiveIntensity: 0.6 }), GX, rng.range(40, 120), GZ); mo.userData = { orbit: true, r: rng.range(230, 330), ph: rng() * 6.28, v: rng.range(0.05, 0.12), cx: GX, cz: GZ }; g.add(mo); rings.push(mo); }
    sign('giant', 'GAS GIANT KEPLER-9 · ORBIT LANE', 0.455, maxLat[J(0.455)] + 6, ORANGE, 22);

    // ---- 6. spacecraft traffic (0.571 .. 0.674): ships crossing over the track, beacons
    var ships = [];
    function ship(color) {
      var s2 = new THREE.Group();
      s2.add(mesh(new THREE.BoxGeometry(3, 1.6, 12), hullMat, 0, 0, 0)); s2.add(mesh(new THREE.ConeGeometry(1.6, 4, 8).rotateX(Math.PI / 2), hullMat, 0, 0, 8));
      s2.add(mesh(new THREE.BoxGeometry(10, 0.3, 3), hullDark, 0, 0, -3)); s2.add(mesh(new THREE.SphereGeometry(1.1, 8, 6), emis(color, 2.0), 0, 0, -6.4));
      s2.add(mesh(new THREE.SphereGeometry(0.4, 6, 5), emis(0xff3030, 2), -5, 0.3, -3)); s2.add(mesh(new THREE.SphereGeometry(0.4, 6, 5), emis(0x30ff60, 2), 5, 0.3, -3));
      g.add(s2); return s2;
    }
    for (i = 0; i < 14; i++) {
      var jt = J(0.575 + (i / 14) * 0.095); self.surfacePoint(jt, 0, tmp);
      var dir = new THREE.Vector3(path.R[jt].x, 0, path.R[jt].z).normalize().multiplyScalar(i % 2 ? 1 : -1);
      ships.push({ m: ship([CYAN, ORANGE, PURPLE, GREEN][i % 4]), c: tmp.clone(), dir: dir, h: rng.range(9, 26), span: rng.range(120, 220), v: rng.range(25, 60), ph: rng() * 6.28 });
    }
    for (j = J(0.575); j < J(0.67); j += 18) { [minLat[j] - 4, maxLat[j] + 4].forEach(function (lat, si) { self.surfacePoint(j, lat, tmp); var bc = mesh(new THREE.SphereGeometry(0.5, 8, 6), emis(si ? 0xff3030 : 0x30ff60, 2), tmp.x, tmp.y + 2.5, tmp.z); bc.userData.blink = si; g.add(bc); rings.push(bc); g.add(mesh(new THREE.CylinderGeometry(0.12, 0.2, 2.4, 6), hullDark, tmp.x, tmp.y + 1.2, tmp.z)); }); }
    sign('traffic', 'TRAFFIC ZONE · WATCH FOR SHIPS', 0.572, minLat[J(0.572)] - 6, PINK, 22);

    // ---- 7. gravity-flip tunnel (0.68 .. 0.76): rotating gravity rings, warning
    for (j = J(0.685); j < J(0.758); j += 6) { self.surfacePoint(j, 0, tmp); var gr = mesh(new THREE.TorusGeometry(path.W[j] / 2 + 5.2, 0.5, 6, 36, Math.PI * 1.2), emis([PURPLE, PINK][(j / 6 | 0) % 2], 1.6), tmp.x, tmp.y + 1.5, tmp.z); faceAlong(gr, j); gr.userData.roll = ((j / 6 | 0) % 2 ? 1 : -1) * 2.2; g.add(gr); rings.push(gr); }
    sign('grav', 'GRAVITY FLIP · BRACE', 0.676, maxLat[J(0.676)] + 6, PURPLE, 18);

    // ---- 8. massive station exterior (0.768 .. 0.899): hull plates, giant ring, docking arms, solar arrays, towers
    strip(function (j) { return minLat[j] - 60; }, function (j) { return maxLat[j] + 60; }, -0.4, -0.4, 0.766, 0.9, new THREE.MeshStandardMaterial({ map: tex.labFloor(), color: 0x6f7a92, roughness: 0.5, metalness: 0.7, side: THREE.DoubleSide }), 40);
    var bigRing = mesh(new THREE.TorusGeometry(300, 26, 16, 64), hullMat, 60, 120, 2560); bigRing.rotation.y = 0.9; bigRing.castShadow = false; g.add(bigRing);
    var ringLights = mesh(new THREE.TorusGeometry(300, 27.5, 8, 64, Math.PI * 2), emis(CYAN, 1.2, { transparent: true, opacity: 0.35 }), 60, 120, 2560); ringLights.rotation.y = 0.9; ringLights.castShadow = false; g.add(ringLights);
    g.add(mesh(new THREE.CylinderGeometry(40, 40, 260, 24), hullMat, 60, 120, 2560).rotateZ(Math.PI / 2));
    for (i = 0; i < 6; i++) { var aa = i / 6 * Math.PI * 2; g.add(mesh(new THREE.BoxGeometry(8, 8, 300), hullDark, 60 + Math.cos(aa) * 150 * Math.cos(0.9), 120 + Math.sin(aa) * 150, 2560 - Math.cos(aa) * 150 * Math.sin(0.9)).rotateY(0.9)); }
    for (j = J(0.78); j < J(0.895); j += 30) { var sd2 = (j / 30 | 0) % 2 ? 1 : -1; self.surfacePoint(j, sd2 * (path.W[j] / 2 + rng.range(28, 52)), tmp); var tw = mesh(new THREE.BoxGeometry(rng.range(8, 16), rng.range(20, 70), rng.range(8, 16)), hullMat, tmp.x, tmp.y + 10, tmp.z); tw.position.y += tw.geometry.parameters.height / 2 - 10; g.add(tw); var wl = mesh(new THREE.BoxGeometry(tw.geometry.parameters.width + 0.4, 0.5, tw.geometry.parameters.depth + 0.4), emis(ORANGE, 1.4), tmp.x, tmp.y + tw.geometry.parameters.height - 2, tmp.z); g.add(wl); }
    for (i = 0; i < 10; i++) { var js = J(0.775 + i * 0.012), sd3 = i % 2 ? 1 : -1; self.surfacePoint(js, sd3 * (path.W[js] / 2 + 70 + (i % 3) * 25), tmp); var sp2 = mesh(new THREE.BoxGeometry(60, 0.4, 22), panelMat, tmp.x, tmp.y + 6, tmp.z); sp2.rotation.z = sd3 * 0.35; g.add(sp2); }
    sign('station', 'STATION HELIOS · OUTER HULL', 0.772, minLat[J(0.772)] - 6, CYAN, 22);

    // ---- 9. final straight + finish arena (0.899 .. 1): orbital gate, holo rings, stands, floodlights, dome
    var jg = J(0.907); self.surfacePoint(jg, 0, tmp); var ogate = mesh(new THREE.TorusGeometry(30, 3, 12, 48), emis(PINK, 1.3), tmp.x, tmp.y + 18, tmp.z); faceAlong(ogate, jg); ogate.castShadow = false; g.add(ogate);
    for (j = J(0.915); j < J(0.975); j += 12) { self.surfacePoint(j, 0, tmp); var hr = mesh(new THREE.TorusGeometry(16, 0.35, 6, 48), emis([CYAN, PINK][(j / 12 | 0) % 2], 1.4, { transparent: true, opacity: 0.6 }), tmp.x, tmp.y + 9.5, tmp.z); hr.rotation.x = Math.PI / 2; hr.userData.spin = 0.8; hr.castShadow = false; g.add(hr); rings.push(hr); }
    var jf = path.idxOf(this.finishS);
    for (j = jf - 90; j < jf + 40; j += 12) { [minLat[j] - 16.5, maxLat[j] + 16.5].forEach(function (lat, si) { self.surfacePoint(j, lat, tmp); var st = mesh(new THREE.BoxGeometry(12, 6, 11), hullDark, tmp.x, tmp.y + 3, tmp.z); st.lookAt(path.P[j].x, tmp.y + 3, path.P[j].z); g.add(st); var seat = mesh(new THREE.BoxGeometry(12, 0.6, 11), emis(si ? CYAN : PINK, 0.8), tmp.x, tmp.y + 6.3, tmp.z); seat.lookAt(path.P[j].x, tmp.y + 6.3, path.P[j].z); g.add(seat); }); }
    for (j = jf - 80; j < jf + 40; j += 30) { [minLat[j] - 22, maxLat[j] + 22].forEach(function (lat) { self.surfacePoint(j, lat, tmp); g.add(mesh(new THREE.CylinderGeometry(0.6, 0.9, 26, 8), hullDark, tmp.x, tmp.y + 13, tmp.z)); g.add(mesh(new THREE.BoxGeometry(6, 2.5, 1.5), emis(WHITE, 2.2), tmp.x, tmp.y + 26, tmp.z)); }); }
    self.surfacePoint(jf + 20, 0, tmp);
    // arena: glowing landing-pad rings under the finish + a halo of light pylons around it
    [[120, PINK], [90, CYAN], [60, PINK]].forEach(function (rr, i) { var ar = mesh(new THREE.TorusGeometry(rr[0], 1.6, 8, 72).rotateX(Math.PI / 2), emis(rr[1], 1.2), tmp.x, tmp.y - 6 - i * 2, tmp.z); ar.castShadow = false; g.add(ar); });
    for (i = 0; i < 16; i++) { var pa = i / 16 * Math.PI * 2, px = tmp.x + Math.cos(pa) * 120, pz = tmp.z + Math.sin(pa) * 120; if (self.distToPath(px, pz) < 16) continue; g.add(mesh(new THREE.CylinderGeometry(0.8, 1.2, 40, 8), hullDark, px, tmp.y + 14, pz)); g.add(mesh(new THREE.SphereGeometry(2.2, 10, 8), emis(i % 2 ? PINK : CYAN, 2), px, tmp.y + 35, pz)); }
    sign('arena', 'FINISH ARENA · WELCOME', 0.94, maxLat[J(0.94)] + 6, PINK, 20);
    sign('final', 'FINAL SECTOR · FULL THROTTLE', 0.902, minLat[J(0.902)] - 6, CYAN, 22);

    // ---- animation
    var dq = new THREE.Quaternion();
    this.animated.push(function (dt, t) {
      for (var i = 0; i < rings.length; i++) { var r = rings[i], u = r.userData; if (u.roll) r.rotateZ(dt * u.roll); else if (u.spin) r.rotation.y += dt * u.spin; else if (u.blink !== undefined) r.material.emissiveIntensity = (Math.sin(t * 6 + (u.blink ? 3 : 0)) > 0 ? 2.2 : 0.2); else if (u.orbit) { u.ph += dt * u.v; r.position.set(u.cx + Math.cos(u.ph) * u.r, r.position.y, u.cz + Math.sin(u.ph) * u.r); } else { r.rotation.z += dt * 1.5; } }
      for (i = 0; i < rocks.length; i++) { var rk = rocks[i]; e.set(rk.rot.x * t, rk.rot.y * t, rk.rot.z * t); q.setFromEuler(e); tmp.copy(rk.p); tmp.y += Math.sin(t * 0.5 + rk.ph) * rk.amp; sc.setScalar(rk.s); m4.compose(tmp, q, sc); rockMesh.setMatrixAt(i, m4); }
      rockMesh.instanceMatrix.needsUpdate = true;
      for (i = 0; i < ships.length; i++) { var sh = ships[i], u2 = Math.sin(t * sh.v / sh.span + sh.ph); sh.m.position.copy(sh.c).addScaledVector(sh.dir, u2 * sh.span); sh.m.position.y = sh.c.y + sh.h; var fwd = Math.cos(t * sh.v / sh.span + sh.ph) >= 0 ? 1 : -1; sh.m.rotation.y = Math.atan2(sh.dir.x * fwd, sh.dir.z * fwd); }
      cloudsP.rotation.y += dt * 0.01; moon.rotation.y += dt * 0.02;
    });
  };


  // ------------------------------------------------------------- SKY CRYSTAL (bright ice world above a cloud sea)
  Track.prototype._scenerySky = function (rng, envelope, _unused, groundY) {
    var g = this.group, tex = U.tex, path = this.path, self = this, N = path.N;
    var tmp = new THREE.Vector3(), tmp2 = new THREE.Vector3(), m4 = new THREE.Matrix4(), q = new THREE.Quaternion(), sc = new THREE.Vector3(), e = new THREE.Euler();
    function mesh(geo, mat, x, y, z) { var m = new THREE.Mesh(geo, mat); m.position.set(x, y, z); m.castShadow = true; m.receiveShadow = true; return m; }
    function emis(color, i, extra) { var o = { color: color, emissive: color, emissiveIntensity: i === undefined ? 0.9 : i, roughness: 0.3, metalness: 0.1 }; if (extra) for (var k in extra) o[k] = extra[k]; return new THREE.MeshStandardMaterial(o); }
    var CY = 0x66d9ff, WH = 0xffffff, PK = 0xff9ad5, PU = 0xb388ff;
    var iceMat = new THREE.MeshStandardMaterial({ color: 0xdff6ff, roughness: 0.25, metalness: 0.1, emissive: 0x3a86c8, emissiveIntensity: 0.15 });
    var snowMat = new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.9 });
    var rockMat = new THREE.MeshStandardMaterial({ color: 0x8fb4d8, roughness: 0.9, flatShading: true });
    var crystalMat = new THREE.MeshStandardMaterial({ color: 0x9fe8ff, roughness: 0.1, metalness: 0.1, transparent: true, opacity: 0.8, emissive: 0x2fa4e8, emissiveIntensity: 0.45, flatShading: true });
    var crystalPink = new THREE.MeshStandardMaterial({ color: 0xffc4ec, roughness: 0.1, transparent: true, opacity: 0.8, emissive: 0xd050a8, emissiveIntensity: 0.4, flatShading: true });
    var minLat = this.minLat, maxLat = this.maxLat, flightMask = this.flightMask;
    var J = function (f) { return Math.max(0, Math.min(N - 1, Math.round(f * N))); };
    function faceAlong(o, j) { o.lookAt(o.position.x + path.T[j].x, o.position.y, o.position.z + path.T[j].z); return o; }
    var cx = -90, cz = 650;

    // ---- cloud sea below everything + fluffy cloud sprites + god rays
    var cloudTex = tex.smoke();
    var sea = mesh(new THREE.PlaneGeometry(4000, 4000).rotateX(-Math.PI / 2), new THREE.MeshStandardMaterial({ color: 0xf4fbff, roughness: 1, transparent: true, opacity: 0.92 }), cx, -150, cz); sea.castShadow = false; sea.receiveShadow = false; g.add(sea);
    var cloudMat = new THREE.SpriteMaterial({ map: cloudTex, color: 0xffffff, transparent: true, opacity: 0.95, depthWrite: false });
    var puffs = [];
    for (var i = 0; i < 90; i++) { var cl = new THREE.Sprite(cloudMat); cl.position.set(cx + rng.range(-1300, 1300), rng.range(-150, -50), cz + rng.range(-1000, 1100)); var s2 = rng.range(180, 420); cl.scale.set(s2 * 1.5, s2 * 0.7, 1); cl.userData.v = rng.range(1, 3); g.add(cl); puffs.push(cl); }
    // small clouds drifting right beside the flight corridors (sense of speed while gliding)
    this.flights.forEach(function (fl) { for (var jj = fl.a; jj < fl.b; jj += 10) { var sdC = rng() < 0.5 ? -1 : 1; self.surfacePoint(jj, sdC * rng.range(fl.w / 2 + 6, fl.w / 2 + 60), tmp); var pc = new THREE.Sprite(cloudMat); pc.position.set(tmp.x, tmp.y + rng.range(-30, 12), tmp.z); var s4 = rng.range(18, 50); pc.scale.set(s4 * 1.6, s4 * 0.8, 1); pc.userData.v = rng.range(0.5, 1.5); g.add(pc); puffs.push(pc); } });
    for (i = 0; i < 20; i++) { var hc = new THREE.Sprite(new THREE.SpriteMaterial({ map: cloudTex, color: 0xffffff, transparent: true, opacity: 0.8, depthWrite: false, fog: false })); hc.position.set(cx + rng.range(-1400, 1400), rng.range(260, 420), cz + rng.range(-1000, 1200)); var s3 = rng.range(140, 320); hc.scale.set(s3 * 1.7, s3 * 0.6, 1); g.add(hc); }
    var rayMat = new THREE.MeshBasicMaterial({ color: 0xffffff, transparent: true, opacity: 0.10, blending: THREE.AdditiveBlending, depthWrite: false, side: THREE.DoubleSide, fog: false });
    var rays = [];
    for (i = 0; i < 16; i++) { var ray = new THREE.Mesh(new THREE.PlaneGeometry(rng.range(20, 60), 900), rayMat); ray.position.set(cx + rng.range(-900, 900), 250, cz + rng.range(-700, 900)); ray.rotation.set(0, rng() * 6.28, rng.range(-0.25, 0.25)); ray.userData.ph = rng() * 6.28; g.add(ray); rays.push(ray); }

    // ---- aurora ribbons high in the sky
    var auroraTex = U.canvasTexture('aurora', 512, function (c, w, h) { var gr = c.createLinearGradient(0, 0, 0, h); gr.addColorStop(0, 'rgba(120,255,200,0)'); gr.addColorStop(0.3, 'rgba(120,255,200,0.55)'); gr.addColorStop(0.6, 'rgba(180,120,255,0.5)'); gr.addColorStop(1, 'rgba(255,120,220,0)'); c.fillStyle = gr; c.fillRect(0, 0, w, h); c.globalCompositeOperation = 'destination-in'; for (var x = 0; x < w; x += 4) { var a = 0.4 + 0.6 * Math.abs(Math.sin(x * 0.05) * Math.cos(x * 0.013)); c.fillStyle = 'rgba(255,255,255,' + a.toFixed(2) + ')'; c.fillRect(x, 0, 4, h); } }, { height: 128 });
    var auroras = [];
    [[-400, 300, 500, 0.4], [200, 340, 1200, -0.3], [-100, 380, 900, 0.9]].forEach(function (a) { var au = new THREE.Mesh(new THREE.PlaneGeometry(1800, 160, 60, 1), new THREE.MeshBasicMaterial({ map: auroraTex, transparent: true, opacity: 0.85, blending: THREE.AdditiveBlending, depthWrite: false, side: THREE.DoubleSide, fog: false })); au.position.set(a[0], a[1], a[2]); au.rotation.y = a[3]; var pos = au.geometry.attributes.position; for (var vi = 0; vi < pos.count; vi++) pos.setZ(vi, Math.sin(pos.getX(vi) * 0.004) * 90); g.add(au); auroras.push(au); });

    // ---- ice station platform at the start
    self.surfacePoint(J(0.02), 0, tmp);
    var plat = mesh(new THREE.BoxGeometry(90, 8, 150), iceMat, tmp.x, tmp.y - 4.6, tmp.z); faceAlong(plat, J(0.02)); g.add(plat);
    var platRim = mesh(new THREE.BoxGeometry(92, 1.2, 152), emis(CY, 1.0), tmp.x, tmp.y - 8.8, tmp.z); faceAlong(platRim, J(0.02)); platRim.castShadow = false; g.add(platRim);
    [[-32, -50], [32, -50], [-32, 50], [32, 50]].forEach(function (o) { g.add(mesh(new THREE.CylinderGeometry(1.2, 1.6, 30, 8), iceMat, tmp.x + o[0], tmp.y + 6, tmp.z + o[1])); g.add(mesh(new THREE.OctahedronGeometry(3, 0), crystalMat, tmp.x + o[0], tmp.y + 24, tmp.z + o[1])); });
    function sign(key, text, f, lat, color, w) {
      w = w || 16; var j = J(f), jb = Math.max(0, j - 30), grp = new THREE.Group();
      grp.add(mesh(new THREE.BoxGeometry(w, w * 0.25, 0.4), new THREE.MeshBasicMaterial({ map: tex.label('sk' + key, text, '#ffffff', '#' + color.toString(16).padStart(6, '0')) }), 0, 7, 0));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.8, 0.3, 0.3), emis(WH, 1.2), 0, 7 + w * 0.125 + 0.3, -0.1));
      grp.add(mesh(new THREE.BoxGeometry(w + 0.8, 0.3, 0.3), emis(WH, 1.2), 0, 7 - w * 0.125 - 0.3, -0.1));
      grp.add(mesh(new THREE.BoxGeometry(0.5, 6.5, 0.5), iceMat, 0, 3.2, -0.1));
      self.surfacePoint(j, lat, tmp); grp.position.set(tmp.x, tmp.y - 0.3, tmp.z); grp.lookAt(path.P[jb].x, grp.position.y, path.P[jb].z); g.add(grp); return grp;
    }
    sign('st', 'ICE STATION · SKY CRYSTAL', 0.012, maxLat[J(0.012)] + 6, 0x2a7fc8, 20);
    sign('canyon', 'FROST CANYON', 0.05, minLat[J(0.05)] - 5, 0x2a7fc8, 14);
    sign('takeoff1', 'TAKE OFF ↑ FLY THROUGH THE RINGS', 0.118, maxLat[J(0.118)] + 6, 0xd050a8, 22);
    sign('landing1', 'LANDING · CRYSTAL BRIDGE', 0.296, minLat[J(0.296)] - 5, 0x2a7fc8, 18);
    sign('cave', 'ICE CAVE', 0.312, maxLat[J(0.312)] + 5, 0x2a7fc8, 12);
    sign('takeoff2', 'TAKE OFF ↑ CLOUD SEA', 0.402, minLat[J(0.402)] - 6, 0xd050a8, 18);
    sign('landing2', 'LANDING · CRYSTAL GARDEN', 0.61, maxLat[J(0.61)] + 5, 0x2a7fc8, 18);
    sign('takeoff3', 'TAKE OFF ↑ AURORA FLIGHT', 0.748, maxLat[J(0.748)] + 6, 0xd050a8, 18);
    sign('palace', 'SKY PALACE · FINISH AHEAD', 0.9, minLat[J(0.9)] - 6, 0x2a7fc8, 20);

    // ---- frost canyon walls (0.03 .. 0.13) + crystal clusters everywhere near the road
    var walls = [], spires = [], spiresP = [];
    function crystalCluster(x, y, z, n, s) { for (var k = 0; k < n; k++) { var h = rng.range(6, 18) * s, r = rng.range(1.2, 3) * s; var cg = new THREE.ConeGeometry(r, h, 5); cg.translate(0, h / 2, 0); cg.rotateX(rng.range(-0.3, 0.3)); cg.rotateZ(rng.range(-0.3, 0.3)); cg.translate(x + rng.range(-4, 4) * s, y, z + rng.range(-4, 4) * s); (rng() < 0.8 ? spires : spiresP).push(cg); } }
    for (var j = J(0.03); j < J(0.13); j += 12) { [-1, 1].forEach(function (sd) { var lat = sd > 0 ? maxLat[j] + rng.range(7, 12) : minLat[j] - rng.range(7, 12); self.surfacePoint(j, lat, tmp); var h = rng.range(22, 55), w = rng.range(10, 18); var wg = new THREE.BoxGeometry(w, h, 14); wg.rotateY(Math.atan2(path.T[j].x, path.T[j].z)); wg.translate(tmp.x, tmp.y + h / 2 - 8, tmp.z); walls.push(wg); crystalCluster(tmp.x, tmp.y + h - 8, tmp.z, 3, 0.8); }); }
    var wallMesh = mesh(mergeGeoms(walls), iceMat, 0, 0, 0); g.add(wallMesh);
    // crystal garden (0.60 .. 0.76) + around the cave hairpin
    for (j = J(0.60); j < J(0.76); j += 9) { var sd2 = (j % 18 === 0) ? 1 : -1; var lat2 = sd2 > 0 ? maxLat[j] + rng.range(5, 14) : minLat[j] - rng.range(5, 14); self.surfacePoint(j, lat2, tmp); crystalCluster(tmp.x, tmp.y - 1, tmp.z, 4, rng.range(0.6, 1.3)); }
    for (j = J(0.31); j < J(0.40); j += 10) { self.surfacePoint(j, maxLat[j] + rng.range(6, 12), tmp); crystalCluster(tmp.x, tmp.y - 1, tmp.z, 3, 1.0); self.surfacePoint(j, minLat[j] - rng.range(6, 12), tmp); crystalCluster(tmp.x, tmp.y - 1, tmp.z, 2, 1.0); }
    if (spires.length) g.add(mesh(mergeGeoms(spires), crystalMat, 0, 0, 0));
    if (spiresP.length) g.add(mesh(mergeGeoms(spiresP), crystalPink, 0, 0, 0));

    // ---- floating ice islands (rock + snow cap + crystals), kept clear of the corridors
    var isl = [], caps = [], tries = 0, islands = [];
    var flightSt = []; this.flights.forEach(function (fl) { for (var jj = fl.a; jj < fl.b; jj += 15) flightSt.push(jj); });
    while (islands.length < 90 && tries++ < 8000) {
      var ix, iz, iy;
      if (islands.length < 50 && flightSt.length) {   // most islands hug the flight corridors so the glide feels busy
        var js = flightSt[(rng() * flightSt.length) | 0], sdI = rng() < 0.5 ? -1 : 1; self.surfacePoint(js, sdI * rng.range(55, 190), tmp); ix = tmp.x; iz = tmp.z; iy = tmp.y + rng.range(-70, 40);
      } else { ix = cx + rng.range(-900, 900); iz = cz + rng.range(-700, 900); iy = rng.range(-60, 50); }
      if (self.distToPath(ix, iz) < 55) continue;
      var ir = rng.range(14, 42);
      islands.push([ix, iy, iz, ir]);
      var rg = new THREE.IcosahedronGeometry(ir, 1); rg.scale(1, 0.75, 1); rg.translate(ix, iy - ir * 0.25, iz); isl.push(rg);
      var cg = new THREE.IcosahedronGeometry(ir * 0.98, 1); cg.scale(1, 0.32, 1); cg.translate(ix, iy + ir * 0.1, iz); caps.push(cg);
      crystalCluster(ix, iy + ir * 0.3, iz, 3 + (rng() * 4 | 0), ir / 18);
    }
    g.add(mesh(mergeGeoms(isl), rockMat, 0, 0, 0)); g.add(mesh(mergeGeoms(caps), snowMat, 0, 0, 0));
    if (spires.length) { var extra = spires.splice(0); g.add(mesh(mergeGeoms(extra), crystalMat, 0, 0, 0)); }
    if (spiresP.length) { var extraP = spiresP.splice(0); g.add(mesh(mergeGeoms(extraP), crystalPink, 0, 0, 0)); }

    // ---- ice arches over the road, landing strips, floating decor rings in flight corridors
    for (j = J(0.08); j < J(0.11); j += 22) { self.surfacePoint(j, 0, tmp); var arch = mesh(new THREE.TorusGeometry(path.W[j] / 2 + 4, 1.4, 8, 24, Math.PI), iceMat, tmp.x, tmp.y + 0.5, tmp.z); faceAlong(arch, j); g.add(arch); }
    [0.293, 0.603, 0.887].forEach(function (f, i) { var jl = J(f); self.surfacePoint(jl, 0, tmp); var strip = mesh(new THREE.PlaneGeometry(path.W[jl] + 4, 40).rotateX(-Math.PI / 2), new THREE.MeshBasicMaterial({ color: CY, transparent: true, opacity: 0.35, blending: THREE.AdditiveBlending, depthWrite: false }), tmp.x, tmp.y + 0.12, tmp.z); faceAlong(strip, jl); strip.castShadow = false; g.add(strip); });
    var decoRings = [];
    this.flights.forEach(function (fl) { for (var jj = fl.a + 30; jj < fl.b - 20; jj += 24) { var side = (jj / 24 | 0) % 2 ? 1 : -1; self.surfacePoint(jj, side * (fl.w / 2 + rng.range(8, 30)), tmp); var dr = mesh(new THREE.TorusGeometry(rng.range(3, 7), 0.5, 6, 24), emis(rng() < 0.5 ? CY : PK, 1.2), tmp.x, tmp.y + rng.range(-10, 25), tmp.z); dr.rotation.set(rng() * 3, rng() * 3, 0); dr.userData.v = rng.range(0.3, 0.9); g.add(dr); decoRings.push(dr); } });

    // ---- sky palace at the finish + stands
    var jf = path.idxOf(this.finishS); self.surfacePoint(jf + 30, 0, tmp);
    var PX = tmp.x, PY = tmp.y, PZ = tmp.z, fwd = path.T[jf];
    function tower(dx, dz, r, h) { var x = PX + dx, z = PZ + dz; g.add(mesh(new THREE.CylinderGeometry(r, r * 1.1, h, 12), iceMat, x, PY + h / 2 - 6, z)); g.add(mesh(new THREE.ConeGeometry(r * 1.3, r * 2.2, 12), crystalMat, x, PY + h - 6 + r * 1.1, z)); for (var k = 6; k < h - 6; k += 8) g.add(mesh(new THREE.TorusGeometry(r + 0.3, 0.35, 6, 16).rotateX(Math.PI / 2), emis(CY, 1.2), x, PY + k - 6, z)); }
    var side = new THREE.Vector3(-fwd.z, 0, fwd.x);
    tower(fwd.x * 90 + side.x * 40, fwd.z * 90 + side.z * 40, 9, 70); tower(fwd.x * 90 - side.x * 40, fwd.z * 90 - side.z * 40, 9, 70);
    tower(fwd.x * 130, fwd.z * 130, 16, 110); tower(fwd.x * 60 + side.x * 70, fwd.z * 60 + side.z * 70, 6, 45); tower(fwd.x * 60 - side.x * 70, fwd.z * 60 - side.z * 70, 6, 45);
    g.add(mesh(new THREE.BoxGeometry(120, 10, 90), iceMat, PX + fwd.x * 110, PY - 11, PZ + fwd.z * 110));
    for (j = jf - 90; j < jf + 40; j += 12) { [minLat[j] - 16.5, maxLat[j] + 16.5].forEach(function (lat, si) { self.surfacePoint(j, lat, tmp); var st = mesh(new THREE.BoxGeometry(12, 6, 11), iceMat, tmp.x, tmp.y + 3, tmp.z); st.lookAt(path.P[j].x, tmp.y + 3, path.P[j].z); g.add(st); var seat = mesh(new THREE.BoxGeometry(12, 0.6, 11), emis(si ? CY : PK, 0.8), tmp.x, tmp.y + 6.3, tmp.z); seat.lookAt(path.P[j].x, tmp.y + 6.3, path.P[j].z); g.add(seat); }); }
    self.surfacePoint(jf + 20, 0, tmp);
    [[120, PK], [90, CY], [60, WH]].forEach(function (rr, i) { var ar = mesh(new THREE.TorusGeometry(rr[0], 1.6, 8, 72).rotateX(Math.PI / 2), emis(rr[1], 1.2), tmp.x, tmp.y - 6 - i * 2, tmp.z); ar.castShadow = false; g.add(ar); });

    // ---- snowflakes
    var flakeMat = new THREE.SpriteMaterial({ map: tex.particle(), color: 0xffffff, transparent: true, opacity: 0.9, depthWrite: false });
    var flakes = [];
    for (i = 0; i < 220; i++) { var fk = new THREE.Sprite(flakeMat); fk.scale.set(0.5, 0.5, 1); fk.position.set(cx + rng.range(-800, 800), rng.range(-40, 80), cz + rng.range(-600, 800)); fk.userData = { v: rng.range(1.5, 3.5), ph: rng() * 6.28 }; g.add(fk); flakes.push(fk); }

    var sunSp = new THREE.Sprite(new THREE.SpriteMaterial({ map: tex.particle(), color: 0xfffbe6, transparent: true, fog: false })); sunSp.position.set(cx + 900, 900, cz - 1200); sunSp.scale.set(520, 520, 1); g.add(sunSp);

    this.animated.push(function (dt, t) {
      for (var i = 0; i < flakes.length; i++) { var f = flakes[i]; f.position.y -= f.userData.v * dt; f.position.x += Math.sin(t + f.userData.ph) * dt * 2; if (f.position.y < -60) f.position.y = 80; }
      for (i = 0; i < puffs.length; i++) puffs[i].position.x += puffs[i].userData.v * dt;
      for (i = 0; i < rays.length; i++) rays[i].material.opacity = 0.06 + Math.sin(t * 0.6 + rays[i].userData.ph) * 0.04;
      for (i = 0; i < auroras.length; i++) auroras[i].material.map.offset.x = t * 0.01 * (i + 1);
      for (i = 0; i < decoRings.length; i++) { decoRings[i].rotation.y += dt * decoRings[i].userData.v; decoRings[i].position.y += Math.sin(t * 0.8 + i) * dt * 0.6; }
    });
  };

  Track.prototype.update = function (dt, t) {
    for (var i = 0; i < this.animated.length; i++) this.animated[i](dt, t);
    if (this.padMat) this.padMat.map.offset.y = -(t * 1.5) % 1;
  };

  global.TRACKS = TRACKS;
  global.Track = Track;
})(window);
